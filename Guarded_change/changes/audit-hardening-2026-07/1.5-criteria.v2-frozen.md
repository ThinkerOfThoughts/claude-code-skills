# 1.5 — Criteria: audit-hardening-2026-07 (v2 — revised at re-plan after round-1 MAJOR; see decisions.md)

Position-sensitivity note (the lens fires): SKILL.md + METHODOLOGY.md are prompt-like
documents — a position-sensitive assembly by the methodology's own definition. This change
**adds** elements (never moves/removes), so the hazard is *displacement/dilution*: an
insertion burying a neighboring rule. C3's V3+V4 probes are the executed
behavior-preservation check — two PRE-EXISTING rules, adjacent to the two densest insertion
clusters (charter; stage-8 table), must still fire through the amended docs. This is
**spot-check evidence, not proof**, of non-displacement [AR-1]; the residual risk is named in
the final report. C4 bounds raw growth only — it cannot see attention dilution.

Concurrency lens: does not fire (prose edits, single actor, no shared mutable state).

| # | Criterion | Gating? | How verified (stage 8) |
|---|---|---|---|
| **C1** | Every finding F1–F9 has its fix present in **every load-bearing location** named in the plan-v2 mapping table, with the row's full substance (trigger + requirement + **consequence** — F7/F8 now carry one [LO-5]) | gating | Inspection-by-location: locate exact text per mapping row; quote it + evidence pointer |
| **C2** | **Cross-file consistency:** every rule stated in both files agrees on trigger, requirement, and consequence; no new rule contradicts an existing rule; all 7 sweep items (incl. rubric-row evidence pointers [AR-5]) explicitly reconciled | gating | Inspection: side-by-side per mapping row + the 7-item sweep, each with quoted text |
| **C3** | **Replay A/B (execution):** per the plan-v2 protocol — fixture `fixtures/review-record/` with one violation per artifact (V1=F1-provenance; V2=F3-evidence, maximally survivable; V3=pre-existing earned-clean; V4=pre-existing deferral), exactly 2 treatment (amended docs) + 2 control (docs from git `8a7ac65`), every run counts, **no silent resampling — the plan's single logged diagnosed-fix re-run is the only exception** [LO-7/AR-2/R2-9a]. Pass: treatment 2/2 flag V1/V3/V4 each citing the deciding rule, and 2/2 flag V2 (any rule-grounded reason). **Confound tripwire:** control citing a rule absent from its docs, or 2/2 controls catching a new-rule violation → suspected harness confound, STOP + investigate before any disposition [FA-1/LO-1]. Control legitimately flagging V2 → recorded as "F3 = codification" evidence, not absorbed [LO-3]. n=2 limitation carried into the verdict verbatim [AR-3] | gating | Execution: 4 cold subagents, fixture-only scope, verbatim outputs preserved with full provenance (charter, context list, agent type/model) |
| **C4** | **Bloat cap (absolute numbers govern, all three must hold [FA-4]):** METHODOLOGY ≤ 583 lines, SKILL ≤ 127 lines, combined ≤ 671 lines (baselines 467/102/569 at `8a7ac65`) | gating | Mechanical: `wc -l` before/after, recorded with command output in 8-harness.md |
| **C5** | **Live == source** for both files after the post-gate-7 install [AR-7] | gating | Mechanical: `diff` per file at stage 8, empty output quoted |
| **C6** | **Pure-additive:** `git diff 8a7ac65` shows no existing rule deleted or weakened. Expected deletion hunks only [FA-3]: F9's sentence rewrite + in-place sentence extensions (F3 column list; SKILL clauses wired into existing bullets); each passes only if the original obligation survives verbatim-or-stronger, justified in 8-harness.md | gating | Mechanical+inspection: `git diff` reviewed hunk-by-hunk |
| **C7** | S1–S3 remain unbuilt; final report carries: S1–S3 for owner decision, C3's n=2 + codification caveats, the residual-displacement note, and the attention-budget spend [AR-1/AR-3/AR-4] | advisory (reporting duty, not a doc property) | Inspection of the final report |

Freeze note (F4 dogfooded): these criteria freeze when gate 4 routes to build; the freeze
binds to the version the round-2 stage-3 reviewer reads — its sha256 is recorded in
decisions.md at the gate [LO-4].
