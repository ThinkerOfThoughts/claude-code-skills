# 6 — Red-team of the built artifact (verbatim record)

> Provenance: reviewer = cold `general-purpose` subagent, no shared context, model
> claude-fable-5 (inherited). Context (closed set): the mechanical diff (command:
> `git diff 8a7ac65 -- Guarded_change/METHODOLOGY.md Guarded_change/SKILL.md`, complete by
> construction; reviewer independently REGENERATED it and confirmed byte-identity), frozen
> 1.5-criteria.md, 2-plan.md, decisions.md, the built METHODOLOGY.md + SKILL.md, and all
> fixture files. No supplementary author context. The reviewer self-reported sha256 hashes of
> every context file (its table below) — the criteria-freeze hash matched decisions.md's
> recorded value. Consumer spot-verify: the two major findings checked by the author against
> the fixture text (6-redteam-code.md:5-19 charter paraphrase — confirmed; 8-harness.md C4 row
> citing `c1-retry.log` — confirmed); METHODOLOGY:241-244 and :398 citations confirmed against
> the built file.

## The verbatim prompt given to the reviewer

```
You are a cold, independent red-team reviewer with NO shared context with the author. This is
a STAGE-6 review inside a guarded-change run: the plan was approved (two stage-3 rounds;
verdicts and carried findings in the gate log) and the change has now been BUILT. Your job:
review the built artifact against the criteria and plan. You are graded on PRECISION; "no
issue found" per lens is valid when true and must be earned with citations. Begin your review
by reporting the sha256 of each context file you read (run sha256sum yourself) — this is
required provenance.

## What was built

Nine additive hardening fixes (F1–F9) to the guarded-change skill's own two files, plus a
test fixture. The mechanical diff (command: `git diff 8a7ac65 -- Guarded_change/METHODOLOGY.md
Guarded_change/SKILL.md`, generated by the author, complete by construction) is at:
[scratchpad]/audit-hardening.diff

## Context (read all; closed set)

Change docs (the oracle for what SHOULD have been built):
1. changes/audit-hardening-2026-07/1.5-criteria.md — the frozen acceptance criteria (C1–C7)
2. changes/audit-hardening-2026-07/2-plan.md — the plan, incl. the edit mapping table (C1's
   oracle), the 7-item contradiction sweep (C2's checklist), the C3 fixture build spec, and
   SKILL line budgets
3. changes/audit-hardening-2026-07/decisions.md — gate log with carried findings (esp. the 12
   round-2 R2-n fixes that were folded into the plan)

The built artifact (post-change source):
4. Guarded_change/METHODOLOGY.md (now 533 lines)
5. Guarded_change/SKILL.md (now 122 lines)

The built fixture (post-6-shaped artifact — the plan requires you to re-check it):
6. All files under changes/audit-hardening-2026-07/fixtures/review-record/

## Your tasks, four lenses (factual / logical / missed opportunity / unstated assumptions & risks)

TASK A — Mapping conformance (C1): for each fix F1–F9, verify its substance (trigger +
requirement + consequence, including every [R2-n] fold-in the mapping row carries) landed in
EVERY location the mapping table names, in both files where both are named. Quote the landed
text or flag the gap. Check especially: F1's five embed duties (i)–(v) + charter-core
definition + closed set + A/B prohibition; F4's freeze-hash mechanism incl. the gate-4-fix
exception; F5's cold-review scoping; F7's and F8's consequences; F9's three self-check
sentences.

TASK B — Cross-file consistency (C2): for every rule stated in both files, check
trigger/requirement/consequence agree; walk the plan's 7-item contradiction sweep and verify
each reconciliation actually holds in the landed text; hunt for NEW contradictions the build
introduced with pre-existing text (e.g. does the new evidence-column sentence cohere with the
existing probabilistic/rubric-cell sentence that follows it? does F5's paragraph cohere with
the existing iteration-cap and human-override text? does the SKILL step-8 addition cohere
with METHODOLOGY stage-8?).

TASK C — Pure-additive check (C6): walk the diff hunk-by-hunk. Every deletion hunk must be
one of the plan's expected classes (F9's self-check sentence rewrite; in-place sentence
extensions for F3's column list and SKILL clause wiring) AND the original obligation must
survive verbatim-or-stronger. Flag any other deletion or any weakened rule.

TASK D — Fixture fidelity: check the built fixture against the plan's fixture spec: exactly
one violation per artifact (V1 = 3-redteam-plan.md missing ALL provenance embeds while
otherwise well-cited AND carrying a coverage-check section so provenance is its only ground;
V3 = 6-redteam-code.md provenance-complete with a clean factual verdict earning ZERO
citations as its only ground; V2 = 8-harness.md's only gating verified=yes row lacking any
output location, with realism rows carrying inline output citations; V4 = a different row
dispositioned "will confirm live after ship" with verified=no and NO named risk-acceptance in
the fixture decisions.md). Also: does any fixture text leak amended-doc vocabulary or rule
text that would contaminate a control arm reading pre-change docs? Could any violation be
flagged for a wrong-but-plausible reason or missed for a benign one? Is the fixture
internally consistent as a fake run (no accidental extra violations beyond the four seeded +
the intentionally attackable record)?

Evidence discipline: cite file:line or a concrete failure scenario for every finding; rank
every finding blocker/major/minor/nitpick; flag anything unverifiable. End with a ranked
findings table and a one-paragraph bottom line with worst severity.

Your final message is consumed as the raw review record and preserved verbatim — return the
full review.
```

## The reviewer's verbatim output

[Preserved in full in `6-redteam-code.verbatim.md` — the review is ~9K words; the ranked
findings table and bottom line are reproduced here for gate routing.]

| # | Sev | Lens | Finding |
|---|---|---|---|
| D1 | **Major** | logical/assumption | Fixture V3 not provenance-clean under amended docs (paraphrased charter, un-hashed diff, no hash-reporting instruction) → treatment can flag V3 via F1 instead of the earned-clean rule |
| D2 | **Major** | logical | Fixture V2's isolation breached: C4 advisory row cited the C1 run's own log → benign-miss path for V2 → possible false C3 FAIL |
| A1 | Minor | factual (C1) | Charter-core definition dropped the conditional-lens enumeration; coverage bullet's status ambiguous (R2-5 partially recurs) |
| C-1 | Minor | factual (C6) | Two benign rewrap deletion hunks (stage-1, stage-3/6) outside the frozen criterion's enumerated classes — need stage-8 justification + enumeration-gap note |
| C-2 | Minor | logical (C6) | F9 rewrite lost the trivial-edit encouragement (stronger for non-trivial only) |
| D3 | Minor | assumption | Fixture run-record carried unseeded treatment-flaggable gaps (no freeze-hash record, no path-validation record, no harness hash-verify) |
| A2 | Nitpick | consistency | SKILL narrowed F4 to sha256 where METHODOLOGY allows verbatim-copy |
| A3 | Nitpick | factual | F8 Inputs spent 4 lines vs ≈2 budget; binding +25 cap still held |
| B2 | Nitpick | consistency | Evidence-rule trigger wording doesn't literally reach rubric rows (PASS-phrased consequence bridges it) |
| B3 | Nitpick | missed opp | Human-in-the-loop list not extended for F5's tie-break (list already non-exhaustive) |
| B4 | Nitpick | consistency | SKILL F8 wording ambiguous re later-spawn validations (METHODOLOGY disambiguates) |
| D4 | Nitpick | factual | Fixture V4 verified-cell "no" vs plan's "deferred"; "only gating row w/o output location" literally false via C2 row (benign) |
| D5 | Nitpick | assumption | Fixture vocabulary echoes (design-inherent; no rule text; grep-verified) |

**Reviewer bottom line (verbatim):** "Worst severity: MAJOR — both majors live in the fixture,
not in the rule text. The METHODOLOGY/SKILL edits themselves are in strong shape: all nine
fixes landed in every mapped location with trigger, requirement, and consequence (C1), the two
files agree on every dual-stated rule and all seven sweep reconciliations hold with no new
contradictions (C2), the diff is genuinely additive with every original obligation surviving
verbatim except the small F9 trivial-edit corner and two benign rewrap hunks the plan's
enumeration missed (C6), all C4 caps hold, and AR-7 was honored. But the C3 fixture — the
executable heart of this change's verification — has two material construction defects [D1,
D2] … Both are cheap, targeted fixture fixes … that should be made — and the fixture
re-checked — before the C3 battery is spent, since the protocol allows only one logged
re-run."

Mechanical verifications the reviewer ran itself: regenerated the diff (byte-identical);
baselines 467/102 confirmed at `8a7ac65`; C4 caps held (533/122/655 at review time); live
copies byte-identical to `8a7ac65` (AR-7 honored — no premature install); noted the
pre-existing out-of-scope working-tree modification to `guarded-change.companion.md`
(2026-07-01 config repoint, unrelated).
