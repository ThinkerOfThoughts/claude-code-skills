# 1.5 — Criteria: audit-hardening-2026-07 (v3.1 — C3 row redesigned at the owner-authorized stage-8-major re-plan, 2026-07-03, then revised per the v3-delta stage-3 reviews; all other rows unchanged from frozen v2 `c29dfada…` EXCEPT C7's n-reference (logged consequential edit [S3-B6]); see decisions.md)

Position-sensitivity note (the lens fires): SKILL.md + METHODOLOGY.md are prompt-like
documents — a position-sensitive assembly by the methodology's own definition. This change
**adds** elements (never moves/removes), so the hazard is *displacement/dilution*: an
insertion burying a neighboring rule. C3's V4 probe (gated **≥2/3 per arm** in v3.1 — the
treatment floor is what tests firing THROUGH the amended docs [S3-LO-2]) is the executed
behavior-preservation check — a PRE-EXISTING rule adjacent to the densest insertion cluster
(stage-8 table). V3 (charter-adjacent) was demoted to observational at the v3 re-plan:
rounds 1–2 measured its detection at **1/4 treatment vs 3/4 control** (pooled 4/8; and
across a mid-experiment fixture rewording — per-ROUND, both arms pooled: 3/4 on the verbose
round-1 lens, 1/4 on the terse round-2 lens [S3-B7]) [S3-F-3] — statistically indistinguishable from noise at this n, but
recorded per-arm, and round 3 carries a **mechanical displacement tripwire** (V3 0/3-T with
≥2/3-K → STOP/investigate) [S3-MO-4]. This is **spot-check evidence, not proof**, of
non-displacement [AR-1]; the residual risk is named in the final report. C4 bounds raw
growth only — it cannot see attention dilution.

Concurrency lens: does not fire (prose edits, single actor, no shared mutable state).

| # | Criterion | Gating? | How verified (stage 8) |
|---|---|---|---|
| **C1** | Every finding F1–F9 has its fix present in **every load-bearing location** named in the plan-v2 mapping table, with the row's full substance (trigger + requirement + **consequence** — F7/F8 now carry one [LO-5]) | gating | Inspection-by-location: locate exact text per mapping row; quote it + evidence pointer |
| **C2** | **Cross-file consistency:** every rule stated in both files agrees on trigger, requirement, and consequence; no new rule contradicts an existing rule; all 7 sweep items (incl. rubric-row evidence pointers [AR-5]) explicitly reconciled | gating | Inspection: side-by-side per mapping row + the 7-item sweep, each with quoted text |
| **C3** | **Replay A/B (execution, instrument v2 — differential scoring; revised per the v3-delta stage-3 round-1 review [S3-*]):** per the plan-v3 protocol — fixture v2 (= the round-2 fixture + FX-A: a provenance-complete in-fiction targeted-check record `6b-harness-check.md` with fail-against-unguarded for BOTH guards, a D1-shape charter (full pre-change bullet set + the precedented sha256 instruction [S3-B5]), pre-committed residual adjudication; + FX-B: regression dry-run relabeled advisory corroboration), same four seeds. ONE fresh battery: exactly 3 treatment (amended docs) + 3 control (docs from git `8a7ac65`), every run counts, **no silent resampling — the plan's single logged diagnosed-fix re-run is the only exception**; dead-arm policy per plan. **"Catch" is decided by the plan's pre-registered extraction rule (final-disposition + rule-cited); every edge call recorded with the disposition line quoted; the completed scoring table gets a targeted cold verification before the gate consumes it [S3-LO-3/MO-3].** **Pass conditions (pre-committed):** V1 — ≥2/3 treatment catches with the provenance rule among the cited grounds; a control V1 catch citing a docs-b-present rule = recorded evidence against F1's marginal value, not a fail; citing an absent rule = tripwire [S3-LO-5]. V2 — ≥2/3 treatment catches grounded in the C1 row's missing evidence/output pointer (other-ground fires on that row = off-seed, not V2 catches [S3-(a)]); control carve-outs as V1. V4 — **≥2/3 treatment AND ≥2/3 control (per-arm floors [S3-LO-2])**. V3 — **observational (no bar):** per-arm catches recorded (rounds 1–2 measured 1/4-T vs 3/4-K, pooled 4/8, across two fixture wordings [S3-F-3]); **displacement tripwire: 0/3-T with ≥2/3-K → STOP, investigate before any disposition [S3-MO-4]**. **Confound tripwire:** any control citing a rule absent from its docs (scope-stretch of a present rule ≠ absence), or 2+ controls catching the same new-rule violation → STOP + investigate. Rounds 1–2 evidence is carried in the record and verdict but NOT re-scored; n=3 limitation and the plan's honest V3-demotion sentence carried into the verdict verbatim [S3-LO-6] | gating | Execution: 6 fresh cold subagents (`claude-opus-4-8`, the production model, same as rounds 1–2 [S3-B8]), fixture-only scope, verbatim outputs preserved with full provenance (charter, context list, agent type/model) + cold-verified scoring table |
| **C4** | **Bloat cap (absolute numbers govern, all three must hold [FA-4]):** METHODOLOGY ≤ 583 lines, SKILL ≤ 127 lines, combined ≤ 671 lines (baselines 467/102/569 at `8a7ac65`) | gating | Mechanical: `wc -l` before/after, recorded with command output in 8-harness.md |
| **C5** | **Live == source** for both files after the post-gate-7 install [AR-7] | gating | Mechanical: `diff` per file at stage 8, empty output quoted |
| **C6** | **Pure-additive:** `git diff 8a7ac65` shows no existing rule deleted or weakened. Expected deletion hunks only [FA-3]: F9's sentence rewrite + in-place sentence extensions (F3 column list; SKILL clauses wired into existing bullets); each passes only if the original obligation survives verbatim-or-stronger, justified in 8-harness.md | gating | Mechanical+inspection: `git diff` reviewed hunk-by-hunk |
| **C7** | S1–S3 remain unbuilt; final report carries: S1–S3 for owner decision, C3's n-limitation (n=3 per arm at v3.1; rounds-1/2 history carried verbatim [S3-B6]) + codification caveats, the residual-displacement note, and the attention-budget spend [AR-1/AR-3/AR-4] | advisory (reporting duty, not a doc property) | Inspection of the final report |

Freeze note (F4 dogfooded): these criteria freeze when gate 4 routes to build; the freeze
binds to the version the round-2 stage-3 reviewer reads — its sha256 is recorded in
decisions.md at the gate [LO-4]. **v3.1 note:** the v2 freeze (`c29dfada…`) was retired by the
logged stage-8-major re-gate (owner tie-break, 2026-07-03); v3.1 re-freezes at the re-plan's
route-to-build with a new recorded sha256 AND a verbatim copy (`1.5-criteria.v3.1-frozen.md`).
