# Sub-orchestrator B — held context (input set handed at the first split)
- OWN subtree: tree/branch-B/plan.md + branch-B's own children skeletons
- Inter-branch SEAMS (from ROOT): the A<->B contract only
- Child _status roll-ups under branch-B
EXCLUDED: branch-A's internal plan detail; the whole-tree detail.
