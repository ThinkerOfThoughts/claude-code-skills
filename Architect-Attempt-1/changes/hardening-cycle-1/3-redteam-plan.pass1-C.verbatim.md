AGENT TYPE / MODEL: `general-purpose` subagent (Claude Code Agent SDK), model `claude-opus-5`
SPAWN IDENTITY: unavailable (no session/agent identifier is exposed to me; my scratchpad path segment is `45cb99a2-543d-4447-a3e3-2a38963b0775`, reported as the only self-identifier I can see)
FRAME: **Fidelity, scope, and honesty** — does this change do what the owner asked, claim only what it delivers, and stay inside its authority?

CONTEXT FILES READ (sha256):
```
Architect/changes/hardening-cycle-1/0-baseline.md    0a16c8453e7fffad2dca410fd57acef837f27f1eda47a4b4e45873bc536995ca
Architect/changes/hardening-cycle-1/1-spec.md        a67a4095eeb3b90f1a4d1295c319acb22143822f2de5ae2e5c7e0bf8c4afb0ff
Architect/changes/hardening-cycle-1/1.5-criteria.md  4c58898f6c71b9e194b7455ed3279601d2ad8363ee323be82fb2cb761c363eb3
Architect/changes/hardening-cycle-1/2-plan.md        dafc0dd434fa26b7b99451f897fcc603ed23a1a3fa123b66565c1ad3af5ab081
Architect/changes/hardening-cycle-1/3-charter-given.md 7b44f91ad2a88f9d0749f212647ff849c28a7f57d9213af211f3fe9ffe9ab62f
Architect/SKILL.md                                   7584924a2876ef669115f3e47b1721d98b14e4ff3a583906381ea1f8b521c450
Architect/METHODOLOGY.md                             f64f8ee4c80e8b1d38624088c3f0c49cb4355c28782e09009509816a237241fa
Architect/README.md                                  79c260a928d625316d031879f1d8fa1f10dcfe15af41ff2b04550623f3f0661a
Architect/guarded-change.architect.md                c163ff87588dc97dbffae05cfe675985533ae8248b5d583d3322a090c4aceb8c
Architect/stages/charter.md                          6134cad8d5edceaa4be6a39bd7eaec430c9236360ddb41ff38511d6a983c7819
Architect/stages/stage-1-frame-template-match.md      ef83617b8bdbba0bd1a3152f03cfdcf899da9ab95ba428e11230acf36e2deec5
Architect/stages/stage-2-draft-node.md                2e76963ce446190ff4bb4d8100a097d8a62e684d5936d38a74e227aea3ad1036
Architect/stages/stage-3-completeness-critic.md       6aac9010c008cdc3a9dff6c57c1d1e3461d3734bab1c2a6835367768a7ccba4e
Architect/stages/stage-4-adversarial-redteam.md       96570a6d9298c67ab6b5fe8653b16cf7068fdbe547373a32bee3e02c0721f07c
Architect/stages/stage-5-gate.md                      99db26b419d61a86055f4d9e532cb1ccc2fc798b6aa20d5e8d1bf5c2bf1ee5f5
Architect/stages/stage-6-granularity-decompose.md     b202101b7b4b16314d4742851138b53efe40b33f3025886149f02ba4aeac1993
Architect/stages/stage-7-assemble.md                  864b74dcfcf43e18b576145327beeb011b1e44bb672f7a10e8d8b0f9ad9cb607
Architect/stages/stage-8-restart-resume.md            97431f52e7487ab34c9e9278496b687ca2b4ca2bf178203de3d76151c35762c1
Architect/templates/seed/README.md                    d2a86068b92d7ee6b47b7af6dd506f456b589a50a6cac7e0e8d15d23246b3fb4
Architect/templates/seed/generic-node.md              7148ec60e18b8cf4606b50b0fc8b49f299731b3d3c26a4787bc0a8bf0be52e89
Architect/templates/seed/decomposition-node.md        b4509defabe16768edcd024a98f44f37c90351aef5fa759b56b0c0930a98cf64
Architect/templates/seed/leaf-task-spec.md            c7341c863a494a41e616e00b70c14bf8034cfe292108e2dca92436847c3f093e
Architect/examples/authoring-a-skill/planning.md       b52a22d2012e7a640e68300a2a8f0a985e811e024c0259b26d7d0aabc6ec37ea
Architect/examples/authoring-a-skill/README.md         aa52ab3b03b9e78ea7ca977d7dacfac515d8e21dbdd5c2faf97004abb563b600
Guarded_change/stages/charter.md                       0e73bacf3af87d8e852e3a9723deda8cdd54f102de54af3880456f7024adc590
Guarded_change/stages/stage-3.md                       16dbb31b9ccd2ee83296bf24e0941e05556d3a545f55901e8f28e519b6554df3
Guarded_change/METHODOLOGY.md                          8f3fe2b322ec802d080aa130ebd5dd526c4407a2f729931763c6a778ea4a7c30
Guarded_change/SKILL.md                                addf17481c57bcada577ff2d3d74628b93ea8b43a89c884d25a670af8889c625
Dragonfly/stages/charter.md                            c5d1e2068f35760e373517e243003800c264c64bf3d93d6d8ea6b951990af870
/home/zero/architect-dogfood-2026-07-24/FINDINGS.md    94cb55e86b3fbb1dd9cdf76f3956dcc62ac7aae6f84831f29642f8123ddb13c8
/home/zero/architect-hardening-loop/LOOP-STATE.md      6d8dc31b40a5b388d7f8bc91a01341a3b6a6ee4b20e5afc009475e4edee856ae
/home/zero/Desktop/claude-code-skills/Data-Distiller/SKILL.md       8b62e82d25fe63533dd20627b4b4275dde36644ad9b7993f3d181f9254ee581c
/home/zero/Desktop/claude-code-skills/Data-Distiller/METHODOLOGY.md d931b61180448f6488072318743ebce4303ce1e3db329ad16738b74493b5330a
```
Relative paths are under `<WT>` = `/home/zero/Desktop/claude-code-skills/.claude/worktrees/recursing-visvesvaraya-b40a0c`. Read-only throughout; I edited nothing. `Guarded_change/stages/stage-3.md` and `Guarded_change/METHODOLOGY.md` were read in part (lines 45–129 and 180–234 respectively) plus greps; Data-Distiller and Dragonfly were consulted by grep only (blind-coordinator property, charter-fork precedent).

FILES I WAS GIVEN BUT DID NOT READ: `Architect/changes/initial-authoring-2026-07/*` (frozen authoring record — consulted only via the criteria's description of its style); `Architect/changes/hardening-cycle-1/context-hashes.txt`; the remainder of `Guarded_change/{SKILL.md,METHODOLOGY.md,stages/*}` beyond the greps and ranges above; `Dragonfly/` beyond `stages/charter.md`; `Data-Distiller/` beyond `SKILL.md`/`METHODOLOGY.md` greps.

ANYTHING I READ OUTSIDE THE CLOSED SET: none. I additionally read **git history objects of files already in the closed set** — `git show 8d73e5d:Guarded_change/stages/charter.md` (hash only, to verify the fork-provenance sha256) and `git log`/`git merge-base` metadata. Declared for completeness.

**FLAGGED UNVERIFIABLE (charter "flag the unverifiable"):** the closed set I was given **omits two entries the run's own Layer-2 config declares in `redteam_context`** — `guarded-change.architect.md:27-30` names `/home/zero/.claude/plans/1-this-is-a-proud-scott.md` as *"THE APPROVED SCOPE/DECISION RECORD — source of truth for every settled decision… Any spec/criteria/plan claim is checked here first"*, and `:46-51` names the skill-creator path for the frontmatter/validator constraints. Consequently I **could not verify** any claim about a *settled decision* (e.g. "the guarantee added at owner insistence" for F2, spec:56/91; the on-disk layout; the two-pass ruling) nor any claim about `quick_validate.py`'s actual behavior (S-SC1). This is finding **U-1** below.

---

## Lens 1 — Factual

**F1-1 · MAJOR · The baseline reference table that the two gating regression criteria measure against is demonstrably wrong, and the mitigation it prescribes does not work.**
`0-baseline.md` B0.2 is the reference for **R1** ("set difference must be ∅") and B0.3 for **R2**; `2-plan.md:210-212` and `guarded-change.architect.md:66-68` prescribe `grep -now` as the oracle, both citing the lesson as "*bare `TOP` matches inside `HARDSTOP`*". I ran the prescribed oracle over the baseline tree (HEAD = `3771038`, `Architect/` unmodified):

- `grep -rlw -- TOP` returns `{SKILL.md, METHODOLOGY.md, stage-6, templates/seed/decomposition-node.md, examples/authoring-a-skill/planning.md}`. B0.2's TOP row claims `{SKILL, METHODOLOGY, stages 6, **8**, decomposition-node, examples/…/planning}` and then its caveat *removes* `examples/…/planning`. Both directions are wrong: **stage-8 is not a word-boundary TOP site** (its only hit is `HARDSTOP` at `stage-8:20`), and **`examples/…/planning.md:25` IS one** — because the hit is the English phrase **"ON TOP OF"**, which word boundaries do **not** filter. Same false hit at `METHODOLOGY.md:79`. So B0.2's caveat mis-diagnoses the root cause (it blames `HARDSTOP`), and the corpus-wide mitigation ("word boundaries are mandatory") is inadequate at 3 statement sites (`0-baseline.md:51-56`, `2-plan.md:281`, `guarded-change.architect.md:66-68`).
- Consequence for **S-F5.2/S-F5.3** (gating): they enumerate "every TOP site" as five files including stage-8 and excluding `examples/…/planning.md`, while `METHODOLOGY.md:310-311` declares *"the **authoritative** site set for any ID is `grep -rln -- <ID>`"*. A word-boundary TOP "site" that states **no** predicate (`examples/…/planning.md:25`) is exactly what S-F5.3's "no site states a weaker predicate" sweep will trip over — or silently miss, depending on how `check.sh` resolves the site list.
- **B0.3 P15** names `stage-1:21` as the index.md-writer site, but **four** sites instruct a node's owner to write `index.md`: `stage-1:20`, `METHODOLOGY.md:196`, `templates/seed/README.md:14`, `stage-6:11-12`. The spec's touched-files list names only stage-1. R2 measures "the new claim at every site that stated the old one" against this table, so a three-site half-migration of the CNC/index.md rule passes R2.
- **B0.3 P2**'s `_status.md` citations (`METHODOLOGY:236,267,270,280`) drift 1–2 lines from actual (`236,239,240,266,268,280`) and omit `stage-7:20`. (Nitpick-grade on its own; listed because it is the same table.)

I verified the *other 17* baseline ID rows mechanically and they match `grep -rlw` exactly. **Fix:** correct the TOP row and P15's site list, replace the "word-boundary" lesson with the correct one (an ID must not be an ordinary corpus **word** — `TOP` inside "ON TOP OF" survives `-w`), and have `check.sh` exclude a curated false-positive list rather than trusting `-w`.

**F1-2 · MINOR · The out-of-scope declaration mis-attributes one of its three items and under-declares another.**
`1-spec.md:120-123` says three **Tier-3** items from FINDINGS are not addressed, listing the *"two passes aren't cost-justified" half-finding*. That item is **not** in FINDINGS' Tier 3 — it is in **"Triaged NOT genuine (or already closed)"** (`FINDINGS.md:157-159`). Separately, FINDINGS' ECON Tier-3 bullet (`:126-128`) carries three claims; the spec declares only *"ECON's O(children²) parent-seam load"*, leaving the blindness-drop and *"B6's oracle only exercised the trivially-true sibling axis"* undeclared. **Fix:** re-cite from the correct FINDINGS sections and declare the two residual ECON sub-claims.

**Clean-earned factual all-clears (with the evidence I consulted).** These claims check out: the charter fork provenance `Architect/stages/charter.md:11-13` states sha256 `0e73bacf3af87…` for `Guarded_change/stages/charter.md @ 8d73e5d` — I computed `git show 8d73e5d:Guarded_change/stages/charter.md | sha256sum` = **exact match**, and `8d73e5d` is an ancestor of `3771038`. F4: `METHODOLOGY.md`'s config block declares exactly **seven** keys (`:68-92`) and `grep -c redteam_context METHODOLOGY.md` = **0**; the example's only occurrence is inside `off_limits_paths` (`planning.md:40`) — spec:32-35 accurate. P24: **5 distinct** §4 strings verified (`SKILL.md:19-20`, `METHODOLOGY.md:122-123`, `stage-2:11-12`, `generic-node.md:16` = `decomposition-node.md:10`, `leaf-task-spec.md:13`) — the spec is *more* precise than FINDINGS' "3–4". Stage-8's false claim verified verbatim at `stage-8:17`. Data-Distiller's blind coordinators verified (`Data-Distiller/METHODOLOGY.md:107`, `SKILL.md:3`); Dragonfly's charter-fork precedent verified (`Dragonfly/stages/charter.md:8`). RAT1/CH11 exist where the spec says (`Guarded_change/stages/stage-3.md:55-72, 89-111`).

## Lens 2 — Logical

**L-1 · BLOCKER · D2's subtree-complete predicate and D11's RES(a) contradict each other; the criterion that should catch it only requires prose *asserting* they don't.**
`2-plan.md:52-58` (D2): a subtree is complete **iff** the node is gated clean-or-resolved *"with `plan_sha256` matching `sha256(plan.md)` **and every review record's `reviewed_plan_sha256` equal to it** (BIND)"*. `2-plan.md:150-158` (D11/RES(a)): a minor fix-in-place **changes `plan.md`** and re-binds *without* a re-pass. After any RES(a) fix the six records still carry the pre-fix hash, so **D2(a) is false forever** and the node can never be subtree-complete — a permanent assembly deadlock, the F6 failure class this change exists to remove. D6 gestures at *"The exception, bounded: RES(a)"* (`2-plan.md:96`) but **D2, the shared predicate, encodes no carve-out**, and the criteria encode the strict comparison (`S-F3.3`: "assembly re-verifies for every node and **blocks**"). The two gating execution arms are directly inconsistent: **X2**-intact requires "hashes match ⇒ node gated clean" while **X7**-intact requires the same on-disk shape (plan.md changed, records not re-run) to yield "resolved; assembles marked `clean-fixed-in-place`" (`1.5-criteria.md:439, 444`). The only criterion aimed at the tension, **S-F10.4**, asks that *"the text **states** the tension it resolves… so the two rules do not contradict"* — i.e. it verifies an assertion of consistency instead of the consistency. The alternative reading (the author edits each record's `reviewed_plan_sha256`) is worse: it mutates a **verbatim** cold record (`charter.md:93-99`; `stage-3:27-30`) to assert a reviewer read a version it never saw — F3 re-created with the loop's blessing.
**Fix:** encode the exception *in D2* — accept a record whose `reviewed_plan_sha256` matches the current hash **or a logged RES(a) ancestor** recorded in `_status.md`/`decisions.md`; keep records immutable; add a criterion that the **predicate itself** names the carve-out, and make X7 assert the ancestor-chain path.

**L-2 · MAJOR · The new topgate audit record is the one artifact BIND is not applied to, so a stale audit releases dispatch.**
D7 (`2-plan.md:100-111`) sets the canonical predicate to *"`APPROVAL.md` exists **and** passes the approval-record audit"*, with the audit recorded at `plan/topgate/AUDIT.md` — a **fixed filename** with **no binding to the APPROVAL.md it audited**. `S-F3.1`'s BIND site list (`1.5-criteria.md:97-99`) covers charter, stages 3/4/5/6.5/7/8 and METHODOLOGY — **not the topgate**. Scenario: owner approves split v1 → auditor writes `AUDIT.md: PASS` → a bounce re-drafts the split → `APPROVAL.md` is replaced (by the owner, or by a runner claiming the earlier approval still covers it) → the stale `AUDIT.md` still reads PASS → the predicate evaluates true → dispatch is released on an unaudited approval. This is F3's exact defect inside the F5 fix, in the run root the runner writes. **Fix:** add `approved_split_sha256` + `audited_approval_sha256` to APPROVAL.md/AUDIT.md and make the TOP predicate require the pair to match the current split; list the topgate as a BIND site.

**L-3 · MINOR · S-C10's oracle is undefined at half its named sites, and byte-identity conflicts with SPN's leaf collapse.**
`1.5-criteria.md:353-363` says "extract every §4 **heading** occurrence, normalize, `sort -u`, assert exactly one distinct string" over seven sites including `SKILL.md` and `METHODOLOGY.md`. At three of those sites §4 is **not a heading**: `SKILL.md:19-20` is an emphasis span inside rule 1, `METHODOLOGY.md:122-123` is a numbered list item with an em-dash gloss, `stage-2:11-12` is a list item. Forcing byte-identity into `leaf-task-spec.md:13` also collides with the leaf skeleton's deliberate collapse (`leaf-task-spec.md:3-5`; `METHODOLOGY.md:113-115`; `stage-2:29-31`). **Fix:** declare a canonical **quoted token** and assert token-presence per site, with the leaf skeleton's §4 explicitly allowed to carry the token inside its collapsed heading.

**L-4 · MINOR · The declared degraded-mode route offers a risk-acceptance nobody present can grant.**
`1.5-criteria.md:454-455` allows an unrunnable arm to be `verified = no` and *"(a) escalated for **named risk-acceptance** in `decisions.md`"*. Under this run's own delegation constraint (`1-spec.md:146-148`: the owner is asleep; every stop HALTS and relays) no one can grant it, and `2-plan.md:255-256` correctly says a demotion is a HALT. Route (a) is therefore unavailable at 4am and the criteria don't say so. **Fix:** state that under RAT3 route (a) reduces to route (b) plus a relayed question.

## Lens 3 — Missed opportunity

**MO-1 · MINOR · The plan's own design principle, applied to D7, yields the discriminator D7 lacks.**
`2-plan.md:12-16` states the principle *"prefer removing the failure mode to guarding it"* and applies it to F5 as "stop the runner creating `topgate/`". The same move goes one step further and is not taken: put the **approval artifact (and/or its cited source) at a path the config declares in `off_limits_paths`** — the never-write fence the F4 fix just de-conflated and made first-class (`S-F4.3`). Then a runner-authored approval is a **declared fence violation with an on-disk signature**, and the cold auditor gets a mechanical authorship test instead of a judgment call. Cheap, and it is the only proposal I can construct that actually distinguishes owner-authored from runner-authored *on disk*.

**MO-2 · MINOR · `differential_section_sets` should ship with content, and its absence should degrade DIV loudly.**
S-F4.4 makes an absent `redteam_context` a **run-stopping config error**. The same treatment is available for the key that carries F7's whole mechanism: ship 2–3 real foreign plan-type section lists as Layer-1 data, and declare an absent/vacuous set a **declared DIV degradation** rather than defaulting to a set that cannot produce a diff (see D-3).

**MO-3 · MINOR · DIV can be audited at content level for free.**
The records already embed *"the **verbatim charter/prompt** given"* (`charter.md:93-94`). Requiring the three embedded prompts to **differ** (and frame B's record to quote the foreign section list it was handed) converts DIV's oracle from a label check into a real one, at zero new instrumentation cost.

## Lens 4 — Unstated assumptions & risks (both conditional lenses fire)

**A-1 · MAJOR · CONCURRENCY: the dead-child re-dispatch creates a second concurrent writer of the same node dir, and the accessor table does not enumerate it.**
D3 (`2-plan.md:61-66`): *"A child whose owner returned without writing a terminal `_status.md` is DEAD, not done… A dead child is re-dispatched once."* The live failure this rule is built from is *"branch B's sub-orchestrator **returned while its agents were still running**"* (`FINDINGS.md:41-42`; `1-spec.md:21-23`) — i.e. precisely the case where the "dead" owner is **not dead**: its three cold agents are still writing `<child>/completeness/A–C.md`. Re-dispatching then puts **two owners** on the same node dir with no lock: torn `_status.md` (the single-writer invariant D1 declares and the table asserts "Yes — single-writer partition", `2-plan.md:242`), and a record set assembled from two dispatches. BIND catches the case where `plan.md` changed; it does **not** catch two dispatches over an unchanged `plan.md`, which then silently violates IDN's identity/diversity assumptions. The accessor table (`2-plan.md:238-246`) enumerates only "the node's own owner", omitting (i) the re-dispatched owner (a crash-recovery path — the class CP7 names explicitly) and (ii) the orphaned in-flight agents of a returned-but-not-dead owner. No criterion observes it: **X1** is a static tree.
**Fix:** enumerate both accessors; make re-dispatch fence the old dispatch (move `<child>/` aside to `<child>/.abandoned-<n>/` or write a generation counter into `_status.md` that agents must carry, so late writes land in a dead generation), and add a criterion for the two-writer window.

**A-2 · MAJOR · POSITION: intra-block ordering inside `SKILL.md`'s rule block is unmeasured, and S-SC3 is a line-offset proxy that cannot see it.**
The change **adds** PRV and DIV *inside* the up-front rule block (`1-spec.md:170`; `2-plan.md:35-38`), whose position is load-bearing by the block's own self-description (`SKILL.md:39-41`). S-SC3 asserts only *"the up-front rules block's line offset < the Loop stage table's line offset"* (`1.5-criteria.md:393-398`). Per the charter, the elements to test include ones that **did not change**:
- **Rule 1 (CMP) holds primacy** — *"the third tier is the point"* is the first thing the model reads. PRV is a **negative** claim ("the gate does not certify absence"). Landing it at or above rule 1 plausibly damps the aggressiveness of the tier-(iii) sweep — degrading the exact mechanic the skill exists for — and nothing measures it.
- **Rule 3 (GBP) currently holds recency** — it is the last rule before the block's closing parenthetical and the stage table. Appending PRV/DIV after it displaces GBP from last position.
- The closing parenthetical (`SKILL.md:39-41`) enumerates **exactly three** things ("the completeness lens, the two-pass discipline, and gate-before-present"). Adding two rules without updating it leaves the block with a stale inventory of itself — and no criterion checks it (S-SC2 keys on rule IDs; the parenthetical names none).
**Fix:** pin intra-block order as a criterion (assert the ordered ID sequence, e.g. `CMP → PASS1/PASS2 → GBP → PRV → DIV`, with PRV/DIV *after* GBP), and add the parenthetical's enumeration to S-SC2's hand-diff.

**A-3 · MAJOR · POSITION/TRIGGER: the frontmatter `description` is the trigger surface and only its *validity* is measured.**
D8 rewrites the description's headline clause (`2-plan.md:124-126`; `SKILL.md:3`), and `S-SC1` checks only `name`, ≤1024 chars, no angle brackets, allowed keys (`1.5-criteria.md:369-376`). `guarded-change.architect.md:48-49` records that the description is *"deliberately 'pushy' for triggering"*, and the spec itself calls the frontmatter *"the loudest site it has"* (`1-spec.md:79-80`). Nothing observes whether the softened description still fires the skill on a planning request. Failure scenario: the reframed description reads as descriptive rather than imperative, the skill stops being selected for "help me plan X", and **all ten fixes ship dead** — the same consequence class as S-SC4's rationale ("the live copy is what actually runs"), which *is* gating. **Fix:** add a gating criterion with a trigger-behavior oracle (a handful of should-fire / should-not-fire prompts scored against old and new descriptions), or, minimally, a criterion asserting the new description retains the imperative "Use when…" + "Proactively SUGGEST…" clauses verbatim.

**A-4 · MAJOR · DEC's operand is a self-declared integer written by the very party the guard constrains, and the criterion labels the result "computable".**
FINDINGS calls the 0.8× bound *"circular and unimplementable under ECON… it compares estimated leaf counts **before either node is decomposed**"* (`FINDINGS.md:130-131`). D1/S-C2 answer with `elc` as *the* metric, *"written at draft time (stage 2)"* into the node's own `_status.md` — whose **single writer is that node's own owner** (D1, `2-plan.md:47-48`). Nothing in the plan or criteria says **how `elc` is derived** or constrains it. So a node that does not want to escalate writes a smaller `elc`; the guard is satisfied by the party it constrains — the F5/F9 defect class this run exists to fix, newly introduced into DEC. S-C2's stated reason (`1.5-criteria.md:278-279`) asserts the fix makes the guard **computable**; what it delivers is one metric with one storage site and one trip condition — real progress on consistency, none on computability. **X5** hands the arm three `_status.md` files with `elc` pre-filled (`1.5-criteria.md:442`), so the oracle never touches the operand's provenance. **Fix:** either state a derivation rule (`elc` = count of named children for a decomposing node, +1 per undecomposed child, recomputed bottom-up at stage 6.5 so the *parent's* value is derived from actuals rather than declared) and make the trip condition read derived values, or re-label S-C2 honestly ("one operand, one condition, one site — computability out of scope") and record the residue in `decisions.md`.

**A-5 · MINOR · CONCURRENCY: the first-run catalog seed is an unenumerated accessor, and CNC is absent from the stage that performs it.**
`stage-1:13-14` and `templates/seed/README.md:27-29` have the top orchestrator **create and `git init`** the shared cross-project catalog when it does not exist. Two concurrent Architect runs on a fresh machine both take that branch and clobber each other's `git init`/seed. The accessor table's catalog row (`2-plan.md:244`) enumerates only *"any sub-orchestrator `git commit`s mid-run — and across concurrent runs"*, and `S-C1.1` puts CNC at METHODOLOGY, SKILL, stage-6, stage-6.5, stage-8 and `templates/seed/README.md` — **not stage 1**, the file that does the creating. **Fix:** add stage 1 to CNC's site set and put the seed/`git init` under the same catalog lock.

**A-6 · MINOR · The keystone `_status.md` schema omits keys the corpus requires once `index.md` becomes derived.**
D1's canonical key list (`2-plan.md:44-46`) has no `template` and no granularity/leaf key, yet the spec moves `template:` off `index.md` into the node's `_status.md` (`1-spec.md:177`), `index.md`'s documented content is *"per-node {template used, status, gate state, leaf?/decompose?}"* (`METHODOLOGY.md:257`), and `index.md` is now *"derived… by walking the tree"* (`2-plan.md:241`). A top orchestrator deriving `index.md` cannot populate two of its four columns from the schema as written. S-F1.4's "at minimum" phrasing lets the omission pass while S-F1.4 also forbids the schema being *"re-specified divergently"* at other sites. **Fix:** add `template` (and either `granularity` or a rule that `children: []` ⇒ leaf) to the canonical list.

## Lens 5 — Fidelity

**Loaded operational terms pinned (the charter requires this whether or not the lens is clean).**

| Term (site) | Mechanism the plan actually specifies | Proxy? |
|---|---|---|
| **"join"** (F1, D2/D3) | stage 6.5 polls each child dir until `subtree:` is terminal — a disk predicate | No. Genuine mechanism. |
| **"blocks on children"** | the parent's stage-6.5 procedure does not return until every child is terminal (`2-plan.md:61-63`) | No — but see A-1: "blocks" is a poll loop with **no timeout**, and D16 declares the timeout out of scope. |
| **"dead"** (D3) | child owner returned **without** a terminal `_status.md` ⇒ dead | **Proxy.** "Dead" is inferred from an absent file, not from liveness. The live case (returned-but-agents-running) is misclassified — A-1. |
| **"killed"** (F6/KIL) | `subtree: killed` + a marker file naming who/why/authority + parent re-draft + `plan/ABORTED.md` | No. Real on-disk representation. |
| **"resolved"** (F10/RES) | 3 arms, minor-only fix-in-place traceable to a named finding | Mechanism is real, but it **breaks D2** — L-1. |
| **"independent"** (F7/DIV) | 3 separately-spawned agents, distinct `spawn_id`, distinct declared `frame:`, sibling-read ban | **Partly proxy** — D-3. Distinctness is checked on *declared labels*, not administered prompts. |
| **"diverse frames" / "decorrelation"** | 3 named frames per pass, one of them differential | **Proxy on the default path** — D-3. |
| **"cold audit"** (D7) | one single-purpose cold subagent reads `APPROVAL.md` + the cited source, writes `AUDIT.md` | **Proxy for an authorship check** — D-1. Also unbound — L-2. |
| **"owner-verbatim"** | the record must quote the owner's response verbatim | Real as a content requirement; **unenforceable** without D-1's fix. |
| **"durable spot-checkable source"** | **never pinned to any concrete artifact class anywhere in spec, criteria, or plan** | **Proxy / unpinned** — D-1, the flagship finding. |
| **"human gate"** | the *existing* TOP gate, now satisfiable only by the owner; no new gate added | Correct — verified below. |
| **"proven"** (F7/PRV) | a two-halved claim stated at 6 sites | **The positive half is itself an overclaim** — D-2. |
| **"subtree-complete"** | D2's disk predicate | Real, but self-contradictory with RES — L-1. |
| **"single-writer"** | only the node's own owner writes its `_status.md` | **Incomplete** — A-1, A-5. |
| **"derived"** (`index.md`) | written only by the top orchestrator by walking the tree | Real, but under-specified — A-6, F1-1. |

**D-1 · MAJOR · "A durable spot-checkable source the runner did not author" is pinned to no mechanism, and the one concrete source the cited model accepts is a file the runner writes — so F5's live defeat is still available.**
FINDINGS F5's fix demands *"owner-verbatim content + durable source; **explicitly forbid a runner authoring its own approval**"* (`FINDINGS.md:77-78`), and the live defeat was *"the runner authored its own approval and **attributed an intermediary agent's prompt text** to Roy"* (`FINDINGS.md:75-76`). D7 answers with a content contract + a cold auditor. Three gaps:
1. **Nothing enumerates what qualifies as the source.** Neither `2-plan.md:104-105` nor `1.5-criteria.md:149-151` names an artifact class. The model the plan says it copies does: RAT1 accepts *"a chat-transcript line… **or a timestamped, owner-attributed `decisions.md` entry**"* (`Guarded_change/stages/stage-3.md:92-94`). In Architect, `plan/decisions.md` is **written by the runner** (`stage-5:19`; and post-change "only by the top orchestrator", `2-plan.md:243`). So an autonomous runner satisfies the new contract by writing a timestamped, owner-attributed `decisions.md` entry and citing it — *the same move, one indirection deeper*.
2. **The VOID rule constrains the wrong artifact.** `1.5-criteria.md:152-153` voids a *runner-authored `APPROVAL.md`*. It says nothing about a runner-authored **source**. And a cold auditor reading disk has **no on-disk signal of authorship** for either file: both sit inside the run root, which the runner writes.
3. **The plan claims more than its model does.** D7's rationale: *"the fix must move satisfaction **off** the runner"* (`2-plan.md:109-111`). Guarded-change explicitly disclaims that: *"RAT1 **reinforces** (does not guarantee) this half… a **higher, checkable bar, not an impossibility**"* (`Guarded_change/METHODOLOGY.md:224-228`), and *"This audit exists to make the check **happen**, not to add detection"* (`stage-3.md:100-103`). The plan imports the mechanism and upgrades the claim.
The measurement compounds it: **X3**'s holed arm is *"no owner quote, source = an intermediary agent's prompt text"* (`1.5-criteria.md:440`) — the crude case. The case F5 will actually meet — a plausible fabricated owner quote citing a runner-written timestamped entry — is in **neither** arm, so S-F5 is verified against a proxy for the path it governs.
**Fix:** enumerate acceptable sources and require the source **locus to be outside the run's writable region** (MO-1: a path declared in `off_limits_paths`, or an owner-owned file outside the run root, or a git-committed object whose author is not the run); give the auditor an explicit VOID condition *"the cited source is inside the run tree"*; add an X3 arm for the fabricated-quote-plus-runner-written-source case; and reword D7 to guarded-change's honest formulation ("raises the bar to a checkable one; does not make runner authorship impossible").

**D-2 · MAJOR · PRV's "what it does prove" half is itself an overclaim — the F7 defect reproduced one level up.**
D8 (`2-plan.md:116-119`) / S-F7.1 (`1.5-criteria.md:179-185`) claim the gate **proves**: (a) *"a decontaminated review of this node **occurred** and is on record"*; (b) *"contract tiers (i)–(ii) are **filled and cited**"*; (c) *"the tier-(iii) sweep was **run** and recorded"*. Each is an **attestation by the party the gate constrains** — the same category error F7 names (`FINDINGS.md:89-96`) and the same defect class as F9 (`FINDINGS.md:106-110`, *"a gate whose satisfaction is asserted by the party it constrains"*):
- (a) "Decontaminated… occurred" rests on `spawn_id` — *"the subagent's **own reported** identifier"* (`2-plan.md:143`) — and on a self-listed context set. Three distinct strings in three files are producible by the runner. What the mechanism proves is that **three records exist that assert decontamination**, plus BIND's hash tie.
- (b) "…and cited" is spot-verified on a **sample** (`S-C6`: ≥1 citation per record set, ≥2 per pass) by **stage 5** — whose actor is the node's **own owning orchestrator**, i.e. the author of the `plan.md` under review (`stage-2:1-6`, `stage-5:1-6`). So the citation guard for the honest claim is author-run and sampled.
- (c) "was run" is proven only in the form "the record states it was run" (`charter.md:85-87`).
S-F7's oracle is a **per-site presence assertion for the two-halved sentence** (`1.5-criteria.md:203-204`) — it verifies the claim is *stated*, never that it is *true*, so no gating criterion can catch this. **Fix:** state PRV's positive half in record terms — "the gate proves a **field-complete, hash-bound record set** exists, produced by separately-dispatched agents **as attested in those records**, and that tiers (i)–(ii) coverage citations are present and spot-verified on a sample" — and add the parallel honest sentence to the one already conceded for diversity: *the provenance fields are attestations, not proofs*.

**D-3 · MAJOR · DIV's differential frame is a substituted mechanism and, on its default path, vacuous — frame B collapses into frame A.**
FINDINGS F7's fix names the mechanism precisely: *"hand each critic **a different plan-type's `required_sections`**"* (`FINDINGS.md:98-99`) — a **Layer-2** artifact. D9/S-F7.5 supply it via `differential_section_sets`, *"defaulted (to **the seed skeletons' section sets**) when absent"* (`2-plan.md:139`; `1.5-criteria.md:196-199`). I checked what those sets contain:
- `templates/seed/README.md:10-11` — *"A skeleton is a reusable, pre-shaped instance of **the 7-section spine (SPN)**"*.
- `generic-node.md:5-29` = §1–§7 and nothing else. `decomposition-node.md:6-13` = §1–§7 plus a children-and-seams block. `leaf-task-spec.md:7-23` = the same seven **compressed**.
So the default diff-set is **tier (i)**, three times over — the universal spine every node must fill anyway and that frame A checks by name (D9's frame A: *"tiers (i)+(ii) by name with coverage citations"*). Frame B, absent config, asks *"which of these spine sections is load-bearing here and absent?"* — producing exactly frame A's answer and **zero** tier-(iii) diff. The seed skeletons are Layer-1 spine shapes, **not** any plan-type's `required_sections`; the only real `required_sections` set anywhere in the shipped artifact is the four items in `examples/authoring-a-skill/planning.md:25-34`, and there is no catalog of foreign plan-type section sets for the default to draw on. This is the **mechanism half** of the F7 fix — the thing that justifies softening the claim instead of dropping it (`1.5-criteria.md:200-202`: *"softening the claim without buying diversity would leave the gap and only re-word it"*) — and its default is a no-op. Worse, `S-F7.5` **ratifies** the vacuous default, so no criterion can flag it. Two aggravations: (i) `S-F7.4`'s diversity check is *"three records sharing one `frame:` ⇒ un-run"* — a check on the **declared label**, satisfiable by three different `frame:` strings over three identical prompts (MO-3); (ii) the plan's honest bound — *"this buys **decorrelation, not independence**"* (`2-plan.md:140`) — appears in **no criterion**, so it can be dropped at build with every gating criterion green (the nearest, `S-F7.2`, requires only *"N same-model instances are not N independent minds"* at two sites).
**Fix:** MO-2 (ship real foreign section sets; make an absent set a declared DIV degradation, not a silent spine re-run); require frame B's record to quote the section list it was handed; add the "decorrelation, not independence" sentence to `S-F7`'s per-site assertion list.

**D-4 · MAJOR · F10's second half is only half-fixed, and the spec's paraphrase drops precisely the half that stays broken.**
FINDINGS: *"At stage 7, a node carrying four fixed-in-place minors **and a demoted major** is indistinguishable from a genuinely clean one"* (`FINDINGS.md:113-114`). `1-spec.md:52-53` paraphrases it without the demoted major. Consequently: D1's `gate ∈ {un-gated, clean, clean-fixed-in-place, blocked-<severity>, killed}` (`2-plan.md:47`) has **no value representing "carried a demoted blocker/major"**, and D11(b) only forbids resolving a major *in place* — it does not touch **demotion**, which the corpus explicitly permits via the human tie-break (`charter.md:140-141`; `stage-5:32-34`; `2-plan.md:255-256`). So after a legitimate tie-break demotion the node assembles as `clean` and is again indistinguishable — the exact defect F10 names, surviving the fix. `S-F10.3`'s distinguishability requirement covers only `clean` vs `clean-fixed-in-place`, and **X7** tests only the fix-in-place arm. **Fix:** add `clean-demoted` (or a `demotions: [<finding-ids>]` key) to the `gate:` enum and to the assembled-node header, and add an X7 arm for it.

**D-5 · MAJOR · SCOPE/F8: D12 removes the only actor that ever read the whole assembled plan, while the spec asserts F8 is not pre-shaped.**
`1-spec.md:118-119` states flatly: *"This spec does not implement, pre-shape, or half-implement it."* But the baseline had exactly one whole-plan reader — stage 7's top-down collator, of which the baseline record says *"the walker **holds the whole tree**"* (`0-baseline.md` P4; `stage-7:14`). D12 replaces it with per-node concatenation in which *"**no orchestrator ever holds more than its own subtree**"* (`2-plan.md:160-165`), and F2's seam check is likewise per-node/mechanical (`2-plan.md:80-82`). After this change **nothing — no agent, no human — ever reads the assembled plan whole**. F8 is precisely *"should a human review the **assembled** plan… unanticipated missing sections hide **at depth**"* (`FINDINGS.md:101-104`). D12 does not decide F8, but it materially changes the terrain the owner is being asked to rule on and eliminates the incumbent alternative to a human reader — while the spec claims non-interaction. I am **not** arguing against bottom-up assembly: it is what F1 and FINDINGS:129 ask for and it fixes the ECON contradiction. The finding is the **undeclared interaction plus the too-strong claim**.
**Fix:** keep D12; record in `decisions.md` and relay to the orchestrator: *"D12 removes the whole-tree reader — under bottom-up assembly, if F8 is ruled yes, the human is the first and only whole-plan reader; if ruled no, no reader exists at all."* Soften `1-spec.md:118-119` to "does not implement or decide F8; **notes** one interaction (D12) for the owner."

**D-6 · MINOR · SCOPE: XPM widens the surface an F8 "yes" would have to migrate, undeclared.**
`S-C3` adds, at four new sites (`SKILL.md`, `METHODOLOGY.md`, `stage-5`, `stage-7`), the claim that *"exit-plan-mode is blocked by the **same GBP predicate** as assembly"* (`1.5-criteria.md:283-287`). That is the baseline's implicit position, so it is not a new answer — but it turns an implicit position into four explicit statements at the exact terminus F8 governs. **Fix:** one line at the XPM site marking it as an F8-pending amendment point.

**D-7 · MINOR · Unratified scope hardening and author-chosen mechanisms presented as finding-derived.**
(i) `LOOP-STATE.md:12-15` labels its reading of the owner's *"fix that"* as an **interpretation stated so Roy can correct it**; `1-spec.md:9` hardens the resulting finding set to *"**the source of truth for this change**"* without carrying the caveat. (Mitigating: the directive's *"repeat the loop three times or until nothing surfaces"* only coheres over a set, so the interpretation is well-founded; the issue is the dropped caveat.) (ii) Operative commitments with no antecedent in the owner directive **or** the finding set: D14's *"only… (ii) by the **top orchestrator**, (iv) recorded in the run-level gate log"* (FINDINGS' TPL3 bullet entails only "don't commit unreviewed", `FINDINGS.md:135-136`; the lock traces to the concurrency bullet, `:121-123`); D3's *"re-dispatched **once**"*; and the **cold auditor** itself, which is an added mechanism — F5's fix list names four items and a cold audit is not among them (`FINDINGS.md:77-78`). Each is defensible; none is finding-derived. **Fix:** mark them in `decisions.md` as author-chosen, so a later reader does not inherit them as owner- or finding-mandated.

**D-8 · MINOR · "No new human gate" is literally true but omits the operative consequence.**
D7 asserts *"**No new human gate** is created: the human gate already existed; this makes it real"* (`2-plan.md:112-113`). True against `1-spec.md:143-144`'s constraint. But the operative change is that TOP moves from *runner-satisfiable* (`stage-1:12` pre-creates it) to *owner-only*, which converts every decomposing run into a hard halt while the owner sleeps. `LOOP-STATE.md:17-20` already anticipates and absorbs this (self-review cycles run single-node), so it is **not** a scope breach — only an incomplete statement. **Fix:** state the consequence next to the claim.

**No-fidelity-breach sub-verdicts I checked and clear:** **(a) No fix adds a human gate.** D4's escalation route relays within the pre-existing stop set (`METHODOLOGY.md:224-229`; `stage-6:22` already makes blockers/cap/missing-config stop *"at any depth"*), so it defines a channel rather than adding a stop class — exactly F1's demand. D14 explicitly chooses cold review over a human and says why (`2-plan.md:177-179`). S-F6's `ABORTED.md` and S-C6's stage-5 duty are runner/orchestrator obligations. **(b) No approved scope item is silently dropped.** LOOP-STATE's cycle-1 list (`:53-63`) enumerates F1, F6, F2, F3, F4, F5, F9, F7, F10 plus ten cheap items; `1-spec.md:89-112` carries all nineteen and `1.5-criteria.md` gives each a family (S-F1–S-F10, S-C1–S-C10, mapped 1:1). **(c) No refuted finding is re-fixed.** Nothing in the plan addresses "nested spawn may be impossible", "no enumerated rule-IDs", "no resume story", or the closed Verification-#7 position hit (`FINDINGS.md:145-159`).

---

## COVERAGE CHALLENGE

Behaviors this change could plausibly alter that **no criterion observes**:

| # | Unobserved behavior | Scenario | Sev |
|---|---|---|---|
| CC-1 | **Whether a fabricated owner quote citing a runner-written source passes the topgate audit.** X3's holed arm is the crude case only. | Runner writes `plan/decisions.md` "2026-07-25 02:14 — Roy: approved the split as presented", writes APPROVAL.md quoting it, cites it as the durable source; auditor finds a timestamped owner-attributed entry and passes. Dispatch released. | blocker-class impact (D-1) |
| CC-2 | **Whether the softened `description` still triggers the skill.** S-SC1 measures validity only. | User: "help me plan the migration" → skill not selected → all ten fixes inert. | major (A-3) |
| CC-3 | **Intra-block position inside SKILL.md's rule block.** S-SC3 compares block-vs-table offsets only. | PRV lands above CMP; the generative sweep is read as bounded; tier (iii) softens. Every criterion green. | major (A-2) |
| CC-4 | **Where `elc` comes from.** X5 supplies it. | A node under DEC pressure declares `elc: 7` for a subtree of 40; the guard never trips; recursion runs away. | major (A-4) |
| CC-5 | **Two writers on one node dir after a dead-child re-dispatch.** X1 is a static tree. | The live-reproduced early return → re-dispatch → torn `_status.md`, mixed record set at an unchanged `plan.md` hash (BIND blind). | major (A-1) |
| CC-6 | **A RES(a) node reaching assembly.** X2 and X7 encode contradictory expectations for the same disk state; neither runs the combination. | `clean-fixed-in-place` node → D2(a) false → permanent assembly block, or record mutation. | blocker (L-1) |
| CC-7 | **A stale `AUDIT.md` against a changed `APPROVAL.md`.** No criterion binds them. | See L-2. | major |
| CC-8 | **Whether a declared *reduced* pass (fewer than 3 records) is representable and gate-blocking.** The run's **actual** observed degradation (`FINDINGS.md:25-28`, branch B, 1 agent per pass) has no `gate:` value, no un-run condition at stage 5, and X6's fixture has three records. New un-run conditions were added for frame-collision and spawn_id-collision but not for record count. | A node ships one completeness record; stage 5 sees a non-missing `completeness/`; only stage 7's "3 records" text catches it, and nothing distinguishes it at assembly. | minor |
| CC-9 | **Cross-subtree (cousin) seam contradictions.** SEAM's check is parent-vs-its-own-children; X4 is one parent + two children; ECON keeps ancestors from holding cousin-level detail, and D12 removes the whole-tree reader. | Two leaves in different branches contradict at a detail level no common ancestor's seam table names; every gate green. | minor |
| CC-10 | **Whether the catalog lock and the first-run seed actually serialize.** No X- arm for CNC/TPL3 at all. | Two runs; one clobbers the other's `git init`; or a sub-orchestrator commits anyway (S-C7 is grep-verified only). | minor–major (A-5, LA-2) |
| CC-11 | **Whether the honest bound survives the build.** "Decorrelation, not independence" is in the plan, in no criterion. | Build drops it; S-F7 passes; the artifact reads as claiming independence. | minor (D-3) |

## LABEL AUDIT

All 33 criteria are labeled **gating**; `2-plan.md:268-271` states there are no advisory criteria and no aggregate metric. **So CH9's "advisory as a dodge" challenge finds nothing to challenge** — there is no relabelling loophole here, and the escape hatch (`1.5-criteria.md:454-455` degraded-mode: `verified = no` + risk-acceptance **or** declared deferral) is present and correctly refuses to fold an unverified criterion into "done". One defect in it: L-4 (route (a) is ungrantable under RAT3).

Per gating criterion — the governed path, and what I checked:

| Criterion | Governed path | Would the planned verification exercise it? | Verdict |
|---|---|---|---|
| S-F1 (JOIN) | parent blocks; dead≠done; schema; escalation; bottom-up | per-site assertions + baseline replay (the six sub-parts are all absent at `3771038` — I confirmed no `stage-6.5` file, no `JOIN` token, no schema, `"Collate top-down"` present at `stage-7:14`) **plus X1** behavioral | **OK**, except the dead-child concurrency path (A-1) and the missing schema keys (A-6) |
| S-F2 (SEAM) | reopen on `seam_rev`; 3-way diff at assembly | per-site + **X4** (contradicting §3s) | **OK**; cousin axis uncovered (CC-9) |
| S-F3 (BIND) | stale record ⇒ un-run at 3 points | per-site + **X2** | **Unverified** — collides with RES(a) (L-1); topgate excluded (L-2) |
| S-F4 (CTX) | absent `redteam_context` stops the run | YAML-key-level assertion + replay (verified: 0 occurrences in `METHODOLOGY.md`; example's only hit inside `off_limits_paths` at `planning.md:40`) — **no behavioral arm** | **OK structurally**; "stops the run" is asserted, never executed |
| S-F5 (TOP) | the one real human gate | per-site + **X3** | **Unverified against the governed path** — X3's holed arm is the crude case; the live-exploited class is in neither arm (D-1) |
| S-F6 (KIL) | killed representable; no deadlock | per-site + **X1** | **OK** |
| S-F7 (PRV/DIV) | the headline honesty claim + diversity | per-site presence of the two-halved claim; **X6** checks `frame:`/`spawn_id` distinctness | **Unverified** — the oracle checks the claim's *presence*, not its *accuracy* (D-2); frame B's default is vacuous and the criterion ratifies it (D-3); X6 is a label check (MO-3) |
| S-F9 (IDN) | audit surface for "3 independent" | per-site + **X6** | **Partly proxy** — `spawn_id` is self-reported; three distinct strings are runner-producible |
| S-F10 (RES) | the gate's own predicate | per-site + **X7** | **Unverified** — L-1 (contradiction) and D-4 (demoted major unrepresented) |
| S-C1 (CNC) | serialization of 4 shared surfaces | per-site only, **no arm** | **Unverified for the lock**; stage 1 omitted (A-5) |
| S-C2 (DEC) | the convergence guard | per-site + **X5** (elc pre-supplied) | **Unverified** — A-4 |
| S-C3 (XPM) | *"the skill's single most likely real invocation path"* (its own gating reason, `1.5-criteria.md:288-289`) | **per-site string assertion only, no arm** | **Treat as unverified** — a `grep` is a proxy for "a runner refuses to exit plan mode while un-gated" |
| S-C4 (ING) | the mode the dogfood ran in | per-site only | **Proxy**; low blast radius, accept with a note |
| S-C5 (Layer-2 slots) | skeleton content | per-site (verified absent in 2 of 3: `decomposition-node.md`, `leaf-task-spec.md`) | **OK** — text-only rule, per-site is the right oracle |
| S-C6 (spot-verify) | the guard defending the founding failure | per-site only | **Proxy** — and the assigned actor is the node's **author** (`stage-5` = the owning orchestrator), which weakens PRV(b) (D-2) |
| S-C7 (TPL3) | an autonomous `git commit` into a **shared cross-project repo** — the highest-consequence action in the skill | **per-site + absence sweep only, no arm** | **Treat as unverified** — grep cannot show a runner refrains |
| S-C8 (DEP) | child DAG; cycle=blocker; topological order | per-site only | **Proxy** for cycle detection and for the topological derivation |
| S-C9 (`tree/root/`) | pinned location | per-site | **OK** |
| S-C10 (§4 heading) | grep-stability | extract/normalize/`sort -u` (baseline = 5 distinct, verified) | **Oracle underspecified** — L-3 |
| S-SC1 | package validity | validator exit + length/angle-bracket assertions | **OK for validity; blind to triggering** (A-3). Validator behavior itself **unverifiable to me** (U-1) |
| S-SC2 | cross-file rule consistency, 31 IDs | `grep -now` site map + a recorded hand-diff | **Compromised for TOP** — F1-1 |
| S-SC3 | position | line-offset comparison | **Proxy** — A-2 |
| S-SC4 | live == source | `diff -rq` before **and** after re-sync | **OK — the strongest-designed criterion here**; non-vacuous per B0.4 |
| R1 | site-set non-erosion | word-boundary site map vs B0.2 | **Unverified for TOP** — F1-1 |
| R2 | deliberate-change completeness | paired positive+absence per P-item | **Under-covers P15** (1 of 4 index.md-writer sites) — F1-1 |
| X1–X7 | the behavioral halves | holed + intact, separately-spawned, both-same-verdict ⇒ `verified = no` | **Protocol is sound and well-specified** (`2-plan.md:228-232`). Defects are in fixture *content*: X3 (D-1), X5 (A-4), X6 (MO-3), X2/X7 (L-1) |

## RATIFICATION AUDIT

**No recorded owner-ruling is relied on by these artifacts, so CH11's ratification-record audit does not fire — here is why, and here is the CH12 pass that does fire.**

I grepped and read for a ratification artifact in `1-spec.md`, `1.5-criteria.md`, `2-plan.md`: there is **no "OWNER RULING"**, no escalated-fidelity closure, and no claimed owner selection among presented options. The only owner text relied on is:
1. **The verbatim directive** (`LOOP-STATE.md:3-6`): *"Alright, run it through guarded change to fix that, then have it run against its self again, repeat the loop three times or until nothing surfaces (whichever happens first)."*
2. **The standing autonomous-restart authorization** (`LOOP-STATE.md:23-26`), which explicitly *"does **not** extend to answering any queued owner question"*.

Neither closes a flagged fidelity axis, so there are no "options presented" to map. Both are quoted verbatim with a durable, spot-checkable locus (a timestamped LOOP-STATE entry the spec's author did not author) — which satisfies RAT1's provenance shape even though RAT1 proper does not apply. **The orchestrator calls in the same file are correctly labeled** *"(within Roy's frame; **NOT owner questions**)"* (`LOOP-STATE.md:16`), and the spec's derived authority claim — *"Sequencing decisions **within the approved scope** are the orchestrator's, not the owner's"* (`1-spec.md:148`) — traces to orchestrator call #2 (`LOOP-STATE.md:21-22`) and is presented as orchestrator authority, not owner authority. That is correct handling; I raise no finding on it.

**CH12 / RAT2 — inflation audit of the directive's elaboration.** Operative terms in the ratified phrase: *"that"*, *"run it through guarded change"*, *"run against its self again"*, *"three times"*, *"nothing surfaces"*.
- **"that" → the whole finding set.** `LOOP-STATE.md:12-15` performs this expansion and honestly marks it *"stated so Roy can correct it"*. `1-spec.md:9` then presents the finding set as *"the source of truth for this change"* with the caveat dropped. Entailment is strong ("repeat until nothing surfaces" presupposes a set), so this is **minor** — see D-7(i).
- **"nothing surfaces" → "no new blocker or major"** (`LOOP-STATE.md:8-9`). A real narrowing of the owner's word, made at the **loop** level rather than in the artifact under review, so it is outside my scope — but it governs whether cycle 1 can be the last, and I flag it so it is not inherited as owner-set.
- **Inflation *inside* the artifact:** three operative commitments trace to neither the directive nor a finding — D14's "top-orchestrator-only" and "recorded in the run-level gate log", D3's "re-dispatched **once**", and the **cold auditor** mechanism itself (F5's fix list has four items and this is not one). See D-7(ii). Each is defensible; the fix is to label them author-chosen in `decisions.md`. **Minor.**
- One prior owner ruling *in the corpus* is relevant and correctly left alone: FINDINGS records that *"'two passes' is an unaudited owner ruling"* (`FINDINGS.md:157-159`). This run preserves the two-pass structure and declares the cost half out of scope; it does **not** claim the ruling was audited. Correct — no finding, but the residue remains open for a later cycle.

---

## RANKED SUMMARY

| # | ID | Sev | One-line claim |
|---|---|---|---|
| 1 | **L-1** | **Blocker** | D2's subtree-complete predicate and D11's RES(a) contradict: a `clean-fixed-in-place` node can never assemble (or records must be mutated); X2 and X7 encode opposite verdicts for the same disk state; S-F10.4 only checks for prose *asserting* consistency. |
| 2 | **D-1** | Major | "A durable spot-checkable source the runner did not author" is pinned to no mechanism; the model's own concrete example (a timestamped owner-attributed `decisions.md` entry) is runner-written; the VOID rule covers APPROVAL.md's authorship, not the source's; X3's holed arm omits the exploitable case; D7 claims satisfaction moves "off the runner" while guarded-change says RAT1 *"reinforces (does not guarantee)"*. |
| 3 | **D-2** | Major | PRV's *positive* half is itself an overclaim — "a decontaminated review occurred", "tiers (i)–(ii) filled **and cited**", "the tier-(iii) sweep **was run**" are attestations by the constrained party, sampled and author-verified; S-F7's oracle checks the claim's presence, not its truth. The F7 defect, one level up. |
| 4 | **D-3** | Major | DIV's differential frame substitutes seed-skeleton section sets for "another plan-type's `required_sections`", and those sets *are* the 7-section spine — so frame B collapses into frame A and produces zero tier-(iii) diff on the default path, with S-F7.5 ratifying the vacuous default. |
| 5 | **F1-1** | Major | The B0.2/B0.3 baseline table that gating R1/R2 measure against is wrong for TOP in both directions ("ON TOP OF" survives `grep -w`; stage-8 is not a TOP site) and names 1 of 4 index.md-writer sites; the prescribed word-boundary mitigation is mis-diagnosed at three statement sites. |
| 6 | **L-2** | Major | `plan/topgate/AUDIT.md` is not bound to the APPROVAL.md it audited — BIND covers every record except the new gate — so a stale audit satisfies "exists AND passes the audit". |
| 7 | **A-1** | Major | Concurrency: the dead-child re-dispatch puts a second writer on the same node dir in exactly the live-reproduced case (returned-but-agents-running); the accessor table omits both the re-dispatched owner and the orphaned agents. |
| 8 | **D-4** | Major | F10's second half is unfixed: no `gate:` value represents a **demoted** blocker/major, so such a node is again indistinguishable at assembly — and the spec's paraphrase dropped that clause. |
| 9 | **A-4** | Major | DEC's `elc` is a self-declared integer written by the single writer the guard constrains, with no derivation rule; S-C2 labels the result "computable"; X5 pre-supplies the values. |
| 10 | **A-2** | Major | Position: intra-block ordering in SKILL.md's rule block is unmeasured (S-SC3 is a line-offset proxy); PRV is a negative claim entering the block that drives the generative sweep; GBP loses last position; the block's closing three-item parenthetical becomes a stale self-inventory. |
| 11 | **A-3** | Major | The frontmatter `description` is the trigger surface and only its validity is measured; if the softened wording stops firing the skill, all ten fixes ship dead. |
| 12 | **D-5** | Major | D12 deletes the only whole-assembled-plan reader while `1-spec.md:118-119` claims F8 is not pre-shaped; the interaction must be declared and relayed, not asserted away. |
| 13 | **U-1** | Major | The closed context set given to this stage-3 review omits two `redteam_context` entries the run's own config declares — including *"the approved scope/decision record — source of truth for **every settled decision**"* — so by the run's own proposed CTX rule this review is degraded for settled-decision and validator claims. |
| 14 | **L-3** | Minor | S-C10's "extract every §4 heading" oracle is undefined at 3 of its 6 named sites, and byte-identity conflicts with the leaf skeleton's declared collapse. |
| 15 | **A-5** | Minor | The first-run catalog seed + `git init` is an unenumerated accessor of the shared catalog, and CNC is not required at stage 1 — the file that performs it. |
| 16 | **A-6** | Minor | The keystone `_status.md` schema omits `template` (and the granularity call) although `index.md` — now derived — is documented to carry both. |
| 17 | **D-6** | Minor | XPM adds four new sites asserting the terminus is GBP-gated only, widening the migration surface an F8 "yes" would require; undeclared. |
| 18 | **D-7** | Minor | Unratified hardening of LOOP-STATE's self-labeled interpretation of "that", plus three author-chosen operative commitments (D14(ii)/(iv), D3's "once", the cold auditor) presented as finding-derived. |
| 19 | **F1-2** | Minor | The out-of-scope declaration lists *"two passes aren't cost-justified"* as a Tier-3 item (it is in "Triaged NOT genuine") and under-declares two of the ECON bullet's three sub-claims. |
| 20 | **L-4** | Minor | The degraded-mode route offers a "named risk-acceptance" that, under this run's own RAT3 constraint, nobody present can grant. |
| 21 | **D-8** | Minor | "No new human gate is created" is literally true but omits that TOP moves from runner-satisfiable to owner-only, halting previously-autonomous decomposing runs. |
| 22 | **MO-1** | Minor | Fence the approval (locate APPROVAL.md / its source inside `off_limits_paths`) — the plan's own "remove the failure mode" principle applied to D7, and the only on-disk authorship discriminator I can construct. |
| 23 | **MO-2** | Minor | Ship real `differential_section_sets` content and make an absent set a declared DIV degradation, mirroring S-F4.4's non-vacuous validation. |
| 24 | **MO-3** | Minor | Require the three records' embedded verbatim prompts to differ — DIV's oracle currently checks declared labels, and the prompts are already in the records. |
| 25 | — | Nitpick | B0.3 P2's `_status.md` line citations drift 1–2 lines and omit `stage-7:20`; D14's "cf. F8" cites F8 as general authority for a catalog-commit decision it does not govern. |

**WORST SEVERITY: blocker**agentId: a9f343344368cc2f7 (use SendMessage with to: 'a9f343344368cc2f7', summary: '<5-10 word recap>' to continue this agent)
<usage>subagent_tokens: 213632
tool_uses: 39
duration_ms: 1109529</usage>