AGENT TYPE / MODEL: `general-purpose` subagent (Claude Code Agent tool) / `claude-opus-5`
SPAWN IDENTITY: unavailable (I cannot observe a session/agent id for myself; the scratchpad segment I was given is `45cb99a2-543d-4447-a3e3-2a38963b0775`)
FRAME: **Failure injection / mechanism-breaking — "name the run that dies."** Simulate the pass-2 machinery (`PRD` table, D1–D15) on concrete tree shapes and report the exact step that deadlocks, livelocks, silently mis-decides, or fails to terminate. Tasked specifically with the 9 traces in my prompt.

CONTEXT FILES READ (sha256):
```
<CF>/0-baseline.md                                  ef8f14ad864972aa29da93ae70267565107b90651aa8e7f76e0acb3bc8cadfc7
<CF>/1-spec.md                                      14110a9be683212f1ad36a013826d34f2ae5b74618a4fa0a45e0dee5b82c3933
<CF>/1.5-criteria.md                                62227eb1d9c9c107cacfe1517ba16c51982da95e20b583bbaa9af2467ba00a66
<CF>/2-plan.md                                      0afe0acd19ed145b8061d61d2357528ed601ae6ac7e554a5250331ee5982cabf
<CF>/3-charter-given.md                             1b3c1da3be65cd07fdcc204e6b7437c56a78a4dbda29e2cdea145055477a39a4
<CF>/3-redteam-plan.md                              6e4ba56ca015dd798772a53c91136b364014977c55ae8e2c111c9ee1f3fbd160
<CF>/3-redteam-plan.B.verbatim.md                   93587c9c8509a193b2562fd59d5450b612cd6cfd43fe6c1c201a277e145acd7f
Architect/SKILL.md                                  7584924a2876ef669115f3e47b1721d98b14e4ff3a583906381ea1f8b521c450
Architect/METHODOLOGY.md                            f64f8ee4c80e8b1d38624088c3f0c49cb4355c28782e09009509816a237241fa
Architect/stages/charter.md                         6134cad8d5edceaa4be6a39bd7eaec430c9236360ddb41ff38511d6a983c7819
Architect/stages/stage-1-frame-template-match.md    ef83617b8bdbba0bd1a3152f03cfdcf899da9ab95ba428e11230acf36e2deec5
Architect/stages/stage-2-draft-node.md              2e76963ce446190ff4bb4d8100a097d8a62e684d5936d38a74e227aea3ad1036
Architect/stages/stage-5-gate.md                    99db26b419d61a86055f4d9e532cb1ccc2fc798b6aa20d5e8d1bf5c2bf1ee5f5
Architect/stages/stage-6-granularity-decompose.md   b202101b7b4b16314d4742851138b53efe40b33f3025886149f02ba4aeac1993
Architect/stages/stage-7-assemble.md                864b74dcfcf43e18b576145327beeb011b1e44bb672f7a10e8d8b0f9ad9cb607
Architect/stages/stage-8-restart-resume.md          97431f52e7487ab34c9e9278496b687ca2b4ca2bf178203de3d76151c35762c1
Architect/examples/authoring-a-skill/planning.md    b52a22d2012e7a640e68300a2a8f0a985e811e024c0259b26d7d0aabc6ec37ea
/home/zero/architect-dogfood-2026-07-24/FINDINGS.md 94cb55e86b3fbb1dd9cdf76f3956dcc62ac7aae6f84831f29642f8123ddb13c8
/home/zero/architect-hardening-loop/LOOP-STATE.md   0d1dfab5f774747807194a4e7e390d68a186ea3664e1532c18108dff8e79cba8
/home/zero/.claude/plans/1-this-is-a-proud-scott.md aa6c2e12bd274388868570a3cb7b83542eced6eef224e4812f8fd2c044012249
Architect/guarded-change.architect.md               (read via `git diff` + grep only; working-tree copy is DIRTY — see F1-6)
```
(`<CF>` = `<WT>/Architect/changes/hardening-cycle-1`; `<WT>` = `/home/zero/Desktop/claude-code-skills/.claude/worktrees/recursing-visvesvaraya-b40a0c`.)

FILES I WAS GIVEN BUT DID NOT READ (in full):
- `3-redteam-plan.A.verbatim.md`, `3-redteam-plan.C.verbatim.md` — I read each one's **RANKED SUMMARY in full** (all 23 / 25 rows) plus the consolidated index's convergence table and blocker list, and used those to check carried-forward disposition. I did **not** read their bodies, so any A- or C-finding whose substance is not captured in its own summary row is outside my carry-forward check. **Declared as a gap in my own coverage.**
- `Architect/stages/stage-3-completeness-critic.md`, `stage-4-adversarial-redteam.md`, `README.md`, `templates/seed/*`, `examples/authoring-a-skill/README.md` — not opened (I make no claim about their text beyond what B0.6/B0.8 and pass-1's spot-verified citations report).
- `<WT>/Dragonfly/`, `/home/zero/Desktop/claude-code-skills/Data-Distiller/`, `<WT>/Guarded_change/` — not opened this pass. I therefore **re-verify no claim about prior art**; pass-1 B verified `Guarded_change/stages/stage-3.md:55-72,89-112` (RAT1/CH11) supports the plan's claim, and I inherit that as *checked-by-another-reviewer*, not as my own citation.
- `1-spec.v1-superseded.md`, `1.5-criteria.v1-superseded.md`, `2-plan.v1-superseded.md`, `<CF>/decisions.md` — not read (superseded / author's interpretation layer).

ANYTHING I READ OUTSIDE THE CLOSED SET: **yes, two things, both mechanical checks of closed-set claims.** (1) `git status --porcelain` and `git diff Architect/guarded-change.architect.md` in the worktree, to test `0-baseline.md:6-7`'s "worktree clean at run start" (result: **false**, see F1-6). (2) `ls` of `<CF>/oracles/`, `<CF>/fixtures/`, `Architect/stages/`, `Architect/templates/seed/` to confirm which planned artifacts exist (result: `oracles/` and `fixtures/` are **empty**; `stage-6.5-join.md` and `templates/seed/section-sets/` do **not** exist — so every claim about oracle behaviour is a plan, not an artifact).

---

# LENS 1 — FACTUAL (claims vs. source)

**F1-1 · BLOCKER · The plan's central new mechanism — "the runner *cannot* write `APPROVAL.md` because the path is fenced" — is contradicted by the source it fences with. `off_limits_paths` is, in the corpus's own words, enforced by nothing.**
`2-plan.md:14-16` states design principle 1: *"Remove the failure mode, don't guard it. The runner cannot write `APPROVAL.md` because the path is **fenced**"*; `:188-189` repeats it; `:367` records it in the concurrency table as *"runner **fenced out** (`off_limits_paths`) — **yes**"*. Against the source:
- `METHODOLOGY.md:99-101`: *"Off-limits paths are named to be fenced, not visited… **Naming is the fence — no guard catches a stray write the config never declared.**"* The "fence" is a **prompt instruction to the same agent** — the identical modality as pass 1's *"a runner may not author its own approval"*, which is the ambiguity F5 was defeated through (`FINDINGS.md:70-79`).
- The fenced list is **authored by the party it constrains**: `METHODOLOGY.md:105-107` and `SKILL.md:49-50` — *"If none exists, **help author one** against the config contract"*. A runner that authors the config authors its own fence.
- Nothing validates the fence's contents. `CTX`'s non-vacuous validation (`2-plan.md:320-321`) covers **`redteam_context` only** (*"absent or empty ⇒ a config error that stops the run"*). No rule requires `off_limits_paths ⊇ {plan/topgate/}`; `1.5-criteria.md:132-135` verifies only that the **guidance** and the **example config** mention it.

*Failure scenario:* any run whose `config/planning.md` was authored fresh (the common case — this key's guidance is new) omits `plan/topgate/` from `off_limits_paths`. The TOP predicate is now exactly pass-1's state: a prohibition with no producer of enforcement. The runner writes `plan/topgate/APPROVAL.md`, cites a locus, dispatches its own auditor, and `decisions.md` records nothing anomalous — **the live F5 failure, byte for byte** (`FINDINGS.md:75-76`).
*Note on provenance of the idea:* this fence was reviewer C's suggestion, offered as a **minor** and explicitly hedged (`3-redteam-plan.C.verbatim.md` MO-1: *"the only on-disk authorship discriminator I can construct"*). The plan promoted a hedged minor into **principle 1** and into a "cannot". Three of principle 1's four instances are genuine removals (`index.md` derived; the seam a hash; `killed_handled_at` a string compare). This one is not a removal.
*Instead:* say **"must not"**, not "cannot"; make the fence a *validated* config invariant (`plan/topgate/` in `off_limits_paths` is a startup assertion, like `CTX`'s) **and** put the discriminator where it cannot be self-served: the approval's admissibility must rest **entirely** on the auditor's independent fetch of the locus (D9's third bullet), with the fence labelled as defence-in-depth, not as the mechanism.

**F1-2 · BLOCKER · The fence and the cold audit are mutually exclusive: `AUDIT.md` is written *into the fenced directory* by a runner-dispatched agent. Either the fence admits run-dispatched writers (⇒ the runner can forge the approval the same way) or `AUDIT.md` can never be written (⇒ TOP is unsatisfiable and every decomposing run deadlocks).**
`2-plan.md:74` assigns `plan/topgate/AUDIT.md` to *"a **single-purpose cold auditor**"* — an agent the runner dispatches (`:206-208`, *"Cost: one agent, once, at the top split only"*). `:188-189` fences the same directory against *"the run"*. `METHODOLOGY.md:87-88` defines the key as *"paths the RUN must never write into"*.
*Failure scenario (root + 2 children):* root reaches stage 6, TOP blocks. The owner supplies `APPROVAL.md`. The runner dispatches the auditor, which must write `plan/topgate/AUDIT.md`. Horn (a) — the fence covers run-dispatched agents: the write is refused, `AUDIT.md` never exists, and the canonical predicate (`:191-193`, dispatch blocked until the approval *"passes the approval-record audit"*, with `AUDIT.md` read by the TOP predicate per `:74`) can **never** be satisfied. Dispatch is blocked forever on every decomposing tree — the F6 deadlock class, installed at the top gate. Horn (b) — the fence does not cover run-dispatched agents: the runner dispatches an agent to write `APPROVAL.md` instead, and F5 is reproduced with one extra hop, which is *precisely* the live shape (*"attributed an intermediary agent's prompt text to Roy"*).
*Instead:* move the audit **out** of the fenced path (`plan/topgate-audit/AUDIT.md`, or `plan/decisions.md`), and state the fence's scope explicitly (does it bind agents the run dispatches? — it must, or it binds nothing).

**F1-3 · MAJOR · No *admissible* owner locus is demonstrated to exist, and the only candidate in the closed set is agent-authored — so D9's positive admissible-author clause is satisfiable only in the shape F5 names.**
D9 (`2-plan.md:197-201`) requires *"a **durable locus in the owner's own exchange** that the runner did not author and that the auditor can **independently fetch** and confirm the quote in."* In the only environment for which we have evidence, no such artifact exists: the owner's words live in chat; the durable copies are `LOOP-STATE.md:3-5` and `:23-25`, which are authored by the **orchestrator** (an agent), in a file the orchestrator maintains (`LOOP-STATE.md:50` — *"Cycle log (append-only…)"*, and `:52-67` is the orchestrator writing corrections to itself). Pass 1's B recorded the same limit from the other side (*"I could not independently verify the quotes against a chat transcript — that source is not in the closed set"*). The plan names **zero** concrete admissible loci.
*Failure scenario:* delegated run, owner asleep, question relayed and answered in chat. The orchestrator writes `APPROVAL.md` citing `LOOP-STATE.md:NN`. The auditor fetches it: the quote is there — in a file **an agent wrote**. Per D9 the record is admissible; per `FINDINGS.md:75-76` this is the defeat. Per the alternative (no agent-authored locus counts) nothing is ever admissible.
*Instead:* name at least one concrete admissible locus class and say what makes it non-runner-authored (e.g. a file under a path the run has **no write access to at all**, owner-owned, whose mtime/authorship the auditor reports) — and if none exists in the harness, declare TOP **degraded under delegation** rather than shipping a contract nothing can satisfy.

**F1-4 · MAJOR · `off_limits_paths` is repurposed against its documented semantics, inside the very fix that claims to *de-conflate* that key.**
`METHODOLOGY.md:87-88` + `:99-101` define the key as **external read-only context** (*"e.g. the target repo being planned FOR"*), and `run_root` (`:90-92`) is a path the run **owns and writes wholesale**. `plan/topgate/` lives inside `run_root`. So D9 adds a run-internal path to a list documented as "the plan's read-only outside world", in the same pass where `CTX` (`2-plan.md:320`) claims to de-conflate that key's two meanings (*"citable source vs. never-write fence"*). It in fact adds a **third** meaning: an intra-run-root self-restriction.
*Instead:* introduce a distinct key (`run_internal_fenced_paths:`) with its own validation, or state the third semantic explicitly in the contract; either way the criterion must assert the semantic, not just the string's presence.

**F1-5 · MINOR · The plan's own required `PRD` table omits the run's single deliverable and one write D10 mandates — while §1 closes with "Nothing in §2–§3 reads a fact absent from this table."**
`2-plan.md:79` asserts that invariant. But: (a) **`assembled-plan.md` has no row** anywhere in §1a/§1b (`grep` — the string occurs in `2-plan.md` only at `:243` and `:308`), while §2 D12/D15 write it and GBP reads its existence (`stage-7:39-41`, *"its existence is the 'run complete' marker"*). The founding failure was a missing **output location**; the assignment table that exists to prevent unproduced facts omits the output. (b) D10 (`:211-212`) has the orchestrator record `spawn_id` *"into the node's own `decisions.md`"* at **stage 3/4 dispatch**, but §1b's only assignment for `<node>/decisions.md` is *"**stage 5** (that node's owner) | every gate"* (`:64`) — a second writer and a second trigger, absent from the table.
*Instead:* add both rows; make `S-PRD` assert the table is closed over §2–§3 (it currently asserts the reverse direction only — every schema key has a stage, not every written fact has a row).

**F1-6 · MINOR · `0-baseline.md`'s "worktree clean at run start" is still false, and this now bites two gating criteria.** `0-baseline.md:6-7` claims *"worktree clean at run start"*; `git status --porcelain` → ` M Architect/guarded-change.architect.md` (the `redteam_context` additions are already applied, per `git diff`). Pass-1 B raised this as a nitpick (F-6) and it is unaddressed. It is no longer cosmetic: `S-SC4` asserts the live-vs-source diff **before and after** the sync in a stated direction, and the shared `S-` baseline replay asserts *"every preserved-rule assertion must PASS"* against `3771038` — for this file the pre-build working tree ≠ `3771038`, so one file's baseline reference is wrong in the same way B0.2 was.

**F1-7 · NITPICK · Broken internal cross-reference.** `2-plan.md:73` sends the reader to *"§3 D7"* for `APPROVAL.md`'s authorship; the topgate is **D9** (`:186`). D7 is SEAM.

**Factual claims I checked and found SOUND (earning this lens):**
- `METHODOLOGY.md:67-93` declares exactly seven config keys and **zero** `redteam_context` — F4 verified; the example's only mention is inside `off_limits_paths` (`planning.md:38-40`) — the conflation is real.
- `stage-1:10-14` **does** create `plan/topgate/` (empty) as run-level setup — F5's pre-creation verified. TOP is stated with bare-directory existence at `SKILL.md:77` and `METHODOLOGY.md:214` and more strictly at `stage-6:15-19` — three-ways verified.
- `charter.md:93-99` requires the five provenance fields **incl. "(v) the reviewer-reported sha256 of each context file it read"**, and its closed input set explicitly includes *"the parent node's plan"* — so D6's claim that the charter already collects both hashes is **accurate** (and is what makes B-4 below a root-only defect, not a general one).
- `stage-5:42-46` + `charter.md:140-143` define CAP's finding class as *"same gate + same **targeted node section**, regardless of wording"* — the citation B-3 turns on.
- `stage-7:10-14` (*"Collate top-down"*, whole-`tree/` GBP walk), `stage-8:15` (stage-done = output-**exists** only), `stage-6:23-26` (fan-out, procedure ends) — P4/P7/P1 verified.
- `METHODOLOGY.md:279-280` bounds a node's readable surface to *"its own subtree's `plan.md` + child seams + child `_status.md`"* — the citation M-12 turns on.
- **Scope fidelity, item by item.** (a) Nothing in the plan implements or pre-shapes **F8**; `1-spec.md:160-164` declares the two places the fix *widens* F8's migration surface, and `LOOP-STATE.md:69-75` now carries both as queued-not-answered. ✓ (b) No fix adds a human gate: D6's post-approval re-fire is explicitly *"the existing human gate (not a new one)"* (`:148-149`); D7's reopen routes to the **existing** CAP tie-break. ✓ (c) Every claim the spec makes about a finding matches `FINDINGS.md` (F1 `:35-44`, F2 `:46-51`, F3 `:53-58`, F4 `:60-68`, F5 `:70-79`, F6 `:80-83`, F7 `:89-99`, F9 `:106-110`, F10 `:112-114`); the mis-filing of *"two passes aren't cost-justified"* as Tier-3 is **corrected** at `1-spec.md:165-168` and the item is now queued in `LOOP-STATE.md:70-71` (pass-1 B/RAT-4 **closed**). ✓ (d) **No Tier-3 item is silently dropped**: all fourteen `FINDINGS.md:118-141` bullets are either scoped (`CNC`, `DEC`, `XPM`, `IGM`, seed slots, spot-verify, `TPL3`, `DEP`, `RST` root pin, §4 heading, stage-7 assembler named) or **declared** out of scope (`1-spec.md:165-168`: cost/fan-out envelope, ECON's O(children²), the two-pass half). ✓ (e) No refuted finding is re-fixed (nothing addresses "nested spawn may be impossible", "no resume story", or the closed position hit — `FINDINGS.md:145-159`). ✓

---

# LENS 2 — LOGICAL (the runs that die)

## Trace 1 — the smallest trees

**(a) Root only, LEAF — the scale-down case the skill markets.** 1→2→3→4→5 (clean) → 6 LEAF → 7 writes `tree/root/_assembled.md` → the terminal status. `XPM` (`:308-310`) says the presented artifact is *"the root's `plan.md` (single-leaf run)"*, and D12's final rule (`:243`) writes `assembled-plan.md` from *"its own children's `_assembled.md`"* — **the root has no children, so `assembled-plan.md` is empty or unwritten.** Consequence: `stage-7:39-41`'s *"`assembled-plan.md` … its existence is the 'run complete' marker"* becomes false for this shape, and `stage-8`'s resume (*"a restart before it exists resumes the first un-gated node"*) has no completion marker to stop at. **No criterion observes this** (`S-C3` asserts only which artifact is named per shape; `R2`'s P-set has no row for the run-complete marker).
**(b) Root + one leaf child A.** Root 1→5 clean; 6 DECOMPOSE + TOP ⇒ HALT (see B-5/F1-2); assume approval. Root writes `children.A.dispatched_at`/`.dispatch_seq` (`:47`) and dispatches. A: 1→5 clean, 6 LEAF, 7 writes `_assembled.md`, then the terminal write. Root's 6.5 then needs D2(c): `A.subtree == complete` **and** `A.seam_sha256 == root.children.A.declared_seam_sha256` — which is **B-1**.
**(c) Root + two leaves.** Same, twice — which is **B-3** (CAP).

**B-1 · BLOCKER · `children.<c>.declared_seam_sha256` has no derivable initial value, so the SEAM reopen either fires on *every* parent–child pair at the *first* join (mandatory O(depth) cascade to the root on every run), or is initialised from the child's own hash — in which case the parent never reviews the child's actual seam and **F2 is unfixed**.**
`:40` defines `seam_sha256 = sha256(normalize(§3 + the decomposition seam table))` of **that node's own `plan.md`**, written by **stage 2**. `:48` assigns `children.<c>.declared_seam_sha256` to *"**stage 2** (initial) / **stage 6.5** (on accepting a re-plan)"*, trigger *"own plan written"*. The reopen test (`:153-155`) is an **equality** between the child's hash and that recorded value.
At the parent's stage 2 the child's `plan.md` **does not exist** (the child is dispatched at stage 6, `:47`). So the initial value can only be (i) absent/placeholder, (ii) a hash of the parent's *own* seam-table text, or (iii) adopted later from the child.
- (i) or (ii) ⇒ the values are **unequal by construction** on the first comparison. D7's reopen fires: the parent *"re-drafts its seam slice, **re-runs its own two passes**, takes a new `plan_sha256`"* (`:156-157`). That new `plan_sha256` invalidates **every** child's records via D6's parent clause (`:136-139`) ⇒ all N children re-run both passes (6 agents each). And the re-draft recomputes the parent's own `seam_sha256` ⇒ **its** parent reopens (`:158-159`) ⇒ up to the root ⇒ the root's re-gate changes `plan_sha256`/`seam_sha256` ⇒ `APPROVAL.md` *"no longer binds"* ⇒ D6's *"re-fires the existing human gate — which under RAT3 is a HALT + relay"* (`:148-149`). **Every decomposing run halts at the human gate on its first join, with nothing wrong.**
- (iii) ⇒ the parent silently adopts whatever the child wrote. Then the parent's seam review is **never reopened for the child's first real plan** — which is F2 verbatim (`FINDINGS.md:46-51`: *"the parent's seam review runs at stage 2, on **proposed** children, and is **never reopened** once those children are actually planned"*). The Tier-1 fix would be a no-op on the case the finding describes.
*Why the oracles can't see it:* `X4`'s holed fixture (`1.5-criteria.md:405`) **pre-supplies** *"child's `seam_sha256` ≠ parent's `declared_seam_sha256`"* and its intact twin pre-supplies *"hashes equal"* — the fixture hands the agent the artifact whose **production** is in question. This is the exact defect pass-1 B named for `X1` and it has been reproduced one mechanism over.
*Instead:* make the operand producible. The cheapest correct form: the parent's seam-table row for `<c>` is **copied verbatim** into the child's `plan.md` §3 as a quoted "seam I was handed" block, and `declared_seam_sha256` hashes *that block* — then equality is achievable, the child cannot self-certify (it may not edit a quoted block without the diff showing), and the reopen fires only on a real change. Add a positive assertion that the initial value has a stated derivation.

**B-2 · BLOCKER · The terminal-status write is assigned to stages that run *before* the stage producing its own precondition, and it is the one write with no stage-done predicate — so the smallest decomposing tree either livelocks or escalates a clean leaf, depending on which runner you get.**
D2(b) makes *"its own `_assembled.md` exists"* part of subtree-complete (`:94`). D3 (`:106-107`) assigns the write to **stage 6's LEAF branch** *"**after its own stage 7** has written `_assembled.md`"*, and to **stage 6.5** for a decomposing node *"once (a)–(c) hold"* — (c) including (b). `_assembled.md` is written by **stage 7** (`:68`). Stage 7 runs **after** 6 and 6.5. So both producers are scheduled before their own precondition, and `1.5-criteria.md:63-65` (S-F1.4) freezes the mis-assignment (*"stage 6's LEAF branch writes `subtree: complete`; stage 6.5 writes it for a decomposing node"*), while `X1`'s holed verdict (`:402`) **teaches** it: *"leaf ⇒ **stage 6 should have written `complete`**"*.
The write is also the only new fact that is **not a deterministic output**: `RST`'s stage-done is *"the deterministic output exists"* (`stage-8:15`), amended by D6 only for review records (`:144-145`). `_status.md` exists from stage 1 (`:34`), so its *contents* are outside every stage-done predicate.
*Trace (root + one leaf A), HARDSTOP between A's stage-7 write and the late status write:*
1. `tree/A/_assembled.md` exists; `A/_status.md` has `stage: 7`, `subtree: incomplete`, `updated: T1`.
2. Resume. The restart walk (`stage-8:32-34`, *"walk `tree/` for the first node whose expected output is missing"*) finds **nothing missing** for A. A is never re-entered to write the status.
3. Root resumes its 6.5 and, per D4, *"re-dispatches every non-terminal child idempotently"* with `dispatch_seq` 2, `dispatched_at: T2 > T1`.
4. **Runner α:** the resumed A finds every stage-done predicate satisfied and returns without writing. `updated: T1` predates `dispatched_at: T2` ⇒ **dead** ⇒ one more re-dispatch ⇒ *"then escalated"* (`:116-117`) ⇒ HALT + relay to a sleeping owner. **A perfectly clean two-node tree stops the run** — pass-1 blocker B/L-1's outcome, reached by a different road.
5. **Runner β:** the resumed A writes `stage`/`updated` on entry (`:36`, *"every stage | on entry"*) but still never reaches the post-7 write. Now `updated > dispatched_at` ⇒ **not dead**, non-terminal ⇒ the parent's poll has **no rule** ⇒ **poll forever**.
Two conforming runners, opposite failures — the `S-SC2` defect class, on the plan's headline mechanism.
*Instead:* assign the terminal write to **stage 7** (it is the stage whose output is its precondition), make it part of stage 7's deterministic output (write `_status.md`'s terminal status **before** returning, or make the terminal marker a file: `<node>/_complete.md`), and re-state the per-node order explicitly as **6 → (dispatch) → 6.5 → 7 → terminal status**, because §1a's producer column currently contradicts D12's stated ordering argument.

**B-3 · BLOCKER · D7's termination proof rests on a CAP scoping that contradicts CAP's own definition. Under CAP as written, every 2-child node's first join trips the human tie-break; under D7's narrowing, an A↔B seam ping-pong is uncapped and never terminates.**
D7(ii) (`:161-163`): *"a reopen at the same node on **the same child pair** counts as a **bounce at gate 5** under the existing **CAP**, so 2 → human tie-break."* CAP as it exists: *"2 bounces at the same gate on the same finding class (**same gate + same targeted node section**, regardless of wording — a rephrased objection **or the same defect in a nearby spot** still counts)"* (`stage-5:42-46`, `charter.md:140-143`). A SEAM reopen always targets the **same node section** — the parent's §3 + seam table. So:
- **Broad reading (CAP's actual text):** two reopens at one parent — *for different children* — are the same finding class. Combined with B-1, every parent with ≥2 children trips CAP on its **first join** ⇒ human tie-break ⇒ RAT3 HALT. `plan.md`'s claim *"No new envelope is invented"* is true and fatal.
- **Narrow reading (D7's "same child pair"):** *Trace, parent P with children A and B.* A's §3 requires interface I async; P's seam row says sync. Reopen(A) fires. P re-drafts §3 — which also changes B's row — re-runs its passes, new `plan_sha256`. D6's parent clause now makes **B's six records stale ⇒ un-run ⇒ B un-gated** (`:140-141`). B re-runs both passes; its reviewers see the changed parent contract and raise a blocker on B's §3; B routes to stage 2 and re-drafts; B's `seam_sha256` changes ⇒ **reopen(B)** fires. P re-drafts again ⇒ **A's** records go stale ⇒ A re-runs ⇒ A may re-draft ⇒ reopen(A). The cycle alternates child pairs forever; each iteration is a *different* class under D7's scoping, so CAP counts to 1, resets, counts to 1… **livelock**, at 6·(N+1) cold agents per lap. `S-F2.4` (`1.5-criteria.md:89-90`) encodes the narrow reading, so the criterion cannot see the contradiction, and `S-SC2`'s cross-file hand-diff compares each ID's claim across *its own* sites — not `SEAM`'s claim against `CAP`'s.
D7's stated termination argument is *"re-drafting to **satisfy** a seam **need not** change the seam again"* — "need not" is not "does not". With N children sharing one seam table, satisfying child A's constraint **structurally** perturbs child B's row; that is the normal case, not an adversarial one.
*Instead:* count reopens **per node**, not per child pair (which is what CAP already says), and make the counter explicit: a `seam_reopens` key in `_status.md` with a stated bound and a stated consequence, plus a rule that a parent re-draft that changes *another* child's row must re-declare that child's seam in the same re-gate (so one re-draft settles all children at once instead of one per lap).

**B-4 · BLOCKER · `BIND`'s parent clause is undefined at the root, so the root's records can never be current — and the single-node run (the scale-down shape, and this very loop's own execution shape) can never gate.**
D6 (`:136-139`): a record is current iff `reviewed_context_sha256[<node>/plan.md] == plan_sha256` **and** `reviewed_context_sha256[<parent>/plan.md] == parent.plan_sha256`. The root has **no parent**; `charter.md:97-99`'s closed input set for a root node contains no parent plan, so the map cannot contain that key, and `parent.plan_sha256` has no producer. Per the plan's **own** `PRD` consequence (`1-spec.md:39-42`: *"A predicate whose operand has no assigned producer is an **un-runnable check**"*), BIND at the root is un-runnable. `1.5-criteria.md:101-103` (S-F3.2) states the two-file requirement with **no root exception**.
*Trace (root only, the shape `LOOP-STATE.md:17-19` says every self-review cycle uses):* root gated at stage 5 ⇒ BIND evaluated ⇒ second conjunct false or undefined. Strict runner: records **stale ⇒ un-run ⇒ node un-gated** ⇒ re-dispatch ⇒ same result ⇒ **nothing can ever gate, on any tree, ever** (the exact shape of pass-1 blocker B/L-11, which pass 2 correctly fixed for `spawn_id` and re-introduced here). Lenient runner: skips the clause silently. Two runners, opposite verdicts.
*Instead:* one sentence — *"for the root node the parent clause is vacuously satisfied; the root's records bind to `tree/root/plan.md` only"* — plus a sub-part in S-F3.2 and a root arm in `X2`.

**B-5 · BLOCKER · The escalation relay above the originating node has no producer, no file and no reader — the `PRD` violation that pass 1's blocker class was defined by, in the rule (`D5`) titled "RAT3's missing wire".**
`:45` gives `escalation:` exactly one producer — **stage 5** — with trigger *"a blocker / cap tie / missing config unresolvable here"*, and reader *"parent's stage 6.5 → relay"*. D5 (`:128-131`) then says the parent *"**relays it verbatim upward** — recursively to the top orchestrator, then under RAT3 to the human."* **Where does the parent write it?** Not `escalation:` — that key's only producer is the parent's own stage 5, which has already run and whose trigger does not include "my child asked something". Not `subtree` — `:46` lists producers for `complete` (6/6.5) and `escalated` (stage 5 only), and D3 (`:109`) scopes stage 5's `escalated` to *"when it cannot clear the node"* (the parent's node **is** clear). No `_escalation.md`, no `plan/PENDING-QUESTION.md` exists in §1b.
*Trace (root → B → C, C a leaf).* C's gate produces a blocker ⇒ C writes `escalation:` + `subtree: escalated` and returns. B's 6.5 reads it, cannot resolve it (it is not a seam question), and must relay. B has no assignable write, so B "returns". Root's 6.5 sees B non-terminal with `updated > dispatched_at` ⇒ **not dead** (correctly) and **not terminal** ⇒ **no rule** ⇒ root polls B forever, or re-dispatches B (D4's post-restart rule, which root cannot distinguish from a live poll — see M-3), which re-runs the join, re-reads C's escalation, and relays into the same void. **The channel F1 says is undefined is still undefined one level up**, and D5's own reader column proves the author knew a relay write was needed.
*Instead:* give the relay a row: `relayed_escalation: <child path> + <question verbatim>` written by **stage 6.5**, read by *that* node's parent, with `subtree: escalated` set on the relaying node too (so the terminal-status ladder carries it), and a run-level `plan/OPEN-QUESTIONS.md` (single writer: the top orchestrator) so a later resume can tell "already asked, awaiting answer" from "fresh escalation".

**B-6 · BLOCKER · `assembled-plan.md` is composed from the root's *children*, so the deliverable ships **without the root node's plan** — including its §4 output locations. That contradicts a settled decision, and no criterion observes the deliverable's content.**
D12 (`:238-243`): each node's `_assembled.md` = *"its own `plan.md` **plus its children's** already-written `_assembled.md`"*; then *"The **top orchestrator** concatenates **its own children's** `_assembled.md` into `assembled-plan.md`."* With the root pinned to `tree/root/` (`S-C9`), `tree/root/_assembled.md` is already the whole deliverable; the final rule instead concatenates the level below it.
*Trace (root + A + B):* `assembled-plan.md` = `A/_assembled.md` + `B/_assembled.md`. The root node's §1–§7 — its Problem/intent, its **seam table between A and B** (the artifact the owner approved at TOP), and its **§4 output locations** — appear nowhere. The founding failure (*"a plan… missing a load-bearing section… §4"*) is re-introduced **into the deliverable** by the assembly fix. Alternatively, if `root/_assembled.md` is what is concatenated, every node's content appears **twice**.
This also contradicts the approved decision record: `/home/zero/.claude/plans/1-this-is-a-proud-scott.md:180` fixes `assembled-plan.md` as *"the collated deliverable: **root plan** + nested sub-plans → leaf task-specs"*, and baseline `stage-7:14-16` says *"the root `plan.md`, then each sub-plan nested under its parent"*. Architect's own severity model ranks a contradicted settled decision a **blocker** (`stage-5:11`, `charter.md:136`), and unlike the `tree/root/` move (declared at `0-baseline.md:189-194`) **this departure is not declared anywhere**. Add the single-leaf case from Trace 1(a) and the deliverable is broken at both ends of the scale axis the skill advertises.
*Instead:* `assembled-plan.md` **is** `tree/root/_assembled.md` (plus the consumer-agnostic header and the `DEP` execution-order section), stated once; add a criterion asserting the root node's §1–§7 appear exactly once in the deliverable, and an `X1`-intact sub-verdict on the assembled content, not just on "assemble / do not assemble".

## Trace 2–3, 6, 8 — the remaining assigned traces

**M-1 · MAJOR · `subtree: killed` has no producer, and the only candidate producer breaks the single-writer rule the whole concurrency story rests on. S-F1.4 institutionalises the omission by counting to three.**
`:46` lists producers for three of the four `subtree` values (`complete` ×2, `escalated`); **`killed` appears in the value list with no producer.** D3 (`:110`) answers *"stage 5 / **the parent** writes `killed`"* — but `:33` declares `_status.md`'s writer *"that node's own owner, **only**"*, and a killed node's owner is by definition not running. `1.5-criteria.md:63-65` requires *"the terminal-status producers are **all three** present"* — `killed` is not among the three, so the criterion cannot catch its absence.
*Trace:* P kills child B after a tie-break. B's owner has halted. If **P** writes `tree/P/B/_status.md: subtree: killed`, `_status.md` has two writers and the fencing token cannot arbitrate (P holds no `owner_dispatch_seq` for B). If **nobody** writes it, D2(c)'s `children.<c>.subtree == killed` is satisfied only by P's own observation field (`:49`, written by 6.5 *"each poll"*) — i.e. the parent **self-certifies** that its child is killed, in the one branch where the child's own file is never consulted, and the `_killed.md` marker (`:69`) is never bound to the child's status.
*Instead:* add `killed` to the producer column with the parent named **and** an explicit exception to the single-writer rule (parent-writes-child's-terminal-status-on-kill, fenced by `owner_dispatch_seq: ∞`/`killed`), or represent the kill only in the parent's own file and delete `killed` from the child's `subtree` domain.

**M-2 · MAJOR · D4's dead test declares an alive child dead the instant it is dispatched: there is no grace, no first-write window, and no blocking dispatch. The join can kill every child at t=0 — and each kill manufactures the double-owner race the fencing token exists to contain.**
`:114-116`: *"A child is **dead** iff `children.<c>.dispatched_at` is set **and** the child's `_status.md` is **absent**, or its `subtree` is non-terminal **and** its `updated` predates `dispatched_at`."* The second clause is sound (a working child writes `updated` on every stage entry, `:53`). The **first** clause has no time component, and the plan explicitly refuses one (*"it needs no wall-clock budget"*, `:120-121`). `_status.md` is created at the child's **stage 1** (`:34`) — after it is spawned, reads its config, and frames the node.
*Trace (root + 2 leaves):* root writes `dispatched_at` for A and B (`:47`, *"immediately **before** dispatching"*), spawns both, enters 6.5, polls. Neither `_status.md` exists yet. Both children are **dead** by the letter of the rule. Root re-dispatches both (`dispatch_seq` 2) — now **two live owners per node**, i.e. the precise condition the token was added for, created *by the guard*. Poll again: still absent (the children are seconds old) ⇒ *"then escalated"* ⇒ HALT + relay. **A clean two-leaf tree escalates before any child has drawn breath.** Nothing in §2 says when the parent may first poll, how long it waits, or that dispatch is synchronous — pass 1's M-4 offered `Data-Distiller/SKILL.md:69-70`'s blocking join (*"dispatches leaves, **waits**"*) and pass 2 neither adopts nor declines it in writing.
*Instead:* the cheapest fix needs no clock: make clause 1 *"`_status.md` absent **and** the child's owner has returned"*, or require the parent to **create** `<child>/_status.md` with `owner_dispatch_seq` + `stage: dispatched` at dispatch time (a defined parent-write, one row in the table) so "absent" becomes a genuine anomaly rather than the normal first state. Or make dispatch blocking and delete the poll.

**M-3 · MAJOR · "After a restart there are no in-flight children by construction" is an unstated assumption about the harness, and it is the join's *only* terminating condition — while nothing lets a parent tell restart-resume from a live poll.**
`:118-121`: *"After a restart there are no in-flight children **by construction** — RST already says in-flight cold agents die on shutdown — so a resuming parent re-dispatches every non-terminal child **idempotently**. **That is the join's terminating condition**."* Three attacks:
1. **`RST` says less than is claimed.** `stage-8:20-24` speaks of *"in-flight **cold agents**"* whose *"half-written outputs are ignored because stage-done is an output-exists check"*. A **sub-orchestrator** is not a cold review agent, and the plan's own record of the live failure is a **partial** death — *"branch B's sub-orchestrator returned while its agents were still running"* (`FINDINGS.md:41-42`). A child owner that dies **without** a session death (tool error, its own cap, early return) is exactly the observed case, and for it D4 offers only the dead test, which M-2 shows cannot discriminate.
2. **No on-disk marker distinguishes "I am a fresh owner" from "I am polling".** There is no `resumed_at`/generation key in §1a. A conforming parent that applies D4's rule on *every* poll re-dispatches every working child continuously, multiplying owners without bound — each new owner fenced on `_status.md` only (M-5).
3. **The property is about the harness, not the disk** — the one thing `RST` says state may never depend on (*"Chat history is not load-bearing"*, `stage-8:9-12`). If a future harness detaches or backgrounds subagents, the join's sole terminating condition silently evaporates.
*Instead:* record the resume explicitly (a `plan/decisions.md` run-level entry plus a per-node `owner_epoch`), state the re-dispatch-all rule as firing **only** on a recorded resume, and state the harness property as a **declared assumption with its consequence if false** rather than as "by construction".

**M-4 · MAJOR · The fencing token is not a compare-and-swap: the discard is enforced *by the superseded writer*, and the check is itself a read-modify-write over the field that decides it.**
`:122-125`: *"the child stamps `owner_dispatch_seq` on every write, and **a write whose `owner_dispatch_seq` is lower than the value already in the file is DISCARDED**."* Markdown files have no CAS. The actor that must read, compare and abstain is the **orphan** — the party the guard constrains, and the party most likely to be wedged, mid-tool-call, or reasoning from a stale read. That is the F5/F9 defect class (*"a gate whose satisfaction is asserted by the party it constrains"*) applied to the concurrency guard.
*Trace:* orphan O₁ (seq 1) reads `_status.md` at T0 (`seq: 1`) as part of its own check. New owner O₂ (seq 2) writes the whole file at T1 (`seq: 2`, `stage: 3`). O₁ writes at T2 from its T0 read: the file now says `owner_dispatch_seq: 1` and carries O₁'s stage — **the orphan has clobbered the token itself**, so every later comparison is against a downgraded value and the fence is silently off for the rest of the node's life. Nothing tells the **parent** to cross-check its own `children.<c>.dispatch_seq` (`:47`) against the child's `owner_dispatch_seq` — the one comparison that would detect it, and it is not in D4 or D2.
*Instead:* have the **parent** validate (`child.owner_dispatch_seq == children.<c>.dispatch_seq`, else the status is from a superseded owner and is ignored) — a parent-side check the parent can actually enforce — and specify write-temp-then-`rename` so a torn write is impossible.

**M-5 · MAJOR · The fence's SCOPE is one file. The re-dispatch leaves a second writer on `plan.md`, `completeness/{A,B,C}.md`, `adversarial/{A,B,C}.md`, `<node>/decisions.md` and `_assembled.md` — all fixed filenames — and "IMMUTABLE once written" is asserted with no enforcement.** (Pass-1 A-2 / C-A-1, fixed for **one** of six surfaces.)
`:60` marks the record files *"**IMMUTABLE once written**"*; `:364` claims the `_status.md` surface is covered by the token; `:368` disposes of the rest with *"each single-writer per §1b"*. But single-writer holds only if a node has one owner, and D4 **creates** second owners.
*Trace (the live-reproduced case):* B's owner returns early while its three adversarial agents run. Parent declares B dead (M-2 makes that easy) and re-dispatches. B′ finds `completeness/` present and BIND-current ⇒ stage-done ⇒ jumps to stage 4 and dispatches three fresh agents. Generation-1 agents now return and B writes `adversarial/A.md`; B′ writes `adversarial/A.md`–`C.md`. Final record set: a **mix of two generations under three filenames**, each internally well-formed. `BIND` passes (both generations reviewed the same `plan.md`). `IDN`'s dispatcher ids (`:211-212`) live in `<node>/decisions.md` — **which both owners append to**, and whose only assigned writer is *"stage 5 (that node's owner)"* (`:64`) with no token — so the id set the audit reads is itself interleaved. `PRV` then certifies *"records exist for both passes with the required provenance"* over a record set no single pass produced.
Pass 1 proposed generation-scoped record dirs (`completeness/gen-N/`); pass 2 declined without discussion.
*Instead:* either scope the record paths by `dispatch_seq` (`completeness/s<N>/A.md`) and define which generation the gate reads, or make immutability real (refuse to write a path that exists; a collision is an anomaly ⇒ escalate).

**M-6 · MAJOR · The *second* RES(a) rebind — explicitly permitted — cannot satisfy `BIND`, `rebound_from` has two homes, "valid" is undefined, and "pass-generation" is an operand with no producer. Pass-1's C/L-1 contradiction recurs at n=2.**
D6's discriminator (`:139-143`): *"**or** the node carries a **valid RES(a) rebind record** (D11) whose `rebound_from` contains the record's value and whose `rebound_to == plan_sha256`"*, and — stated as the whole point — *"the discriminator is the **presence** of a valid rebind record, not the hash."* D11(a) (`:225-230`) puts `rebound_from ∪= {old hash}` / `rebound_to` in **`_status.md`** and the audit in `<node>/rebind/<n>.md`. So the predicate's operands have **two homes** and the plan does not say which BIND reads.
*Trace (two rebinds, both legal — `:230`, "at most 2"):* records report `h0`. Rebind 1: `h0→h1`, `rebind/1.md` audits that diff. Rebind 2: `h1→h2`, `rebind/2.md` audits that diff. Now `plan_sha256 = h2`.
- Read the operands **from the rebind records**: `rebind/1.md` has `rebound_to = h1 ≠ h2` (fails clause 2); `rebind/2.md` has `rebound_from = {h1}`, which does **not contain h0** (fails clause 1). **No single valid rebind record satisfies both clauses** ⇒ the records are stale ⇒ un-run ⇒ the node is un-gated, and D11 permitted the very act that bricked it.
- Read them **from `_status.md`** (the union): the test passes — but then "valid rebind record" degrades to a **presence check** (D6's own words), so **one** cold audit of a trivial diff licenses an arbitrary second edit: the runner writes `rebound_from = {h0,h1}`, `rebound_to = h2` in the file it owns and points at `rebind/1.md`. **A stale rebind audit satisfies a later rebind** — the charter's exact question, answered yes. `X5`'s intact arm (`1.5-criteria.md:406`) tests **one** rebind, so neither horn is exercised.
And `pass-generation` (`:230`, `1.5-criteria.md:223`) occurs **twice in the whole corpus**, is defined nowhere, has **no `_status.md` key**, no counter and no reset rule; `rebind/<n>.md`'s `<n>` is the only counter and it lives in a filename. After fresh passes, is the next rebind #1 or #3? If re-numbered, the "immutable" audit history is overwritten; if not, `rebind/3.md` forbids rebinds forever. Two runners diverge.
*Instead:* make each rebind record carry `{from, to}` **and** the transitive `from` set, define BIND to read the **rebind record chain** (`h0 → … → plan_sha256`) rather than a presence flag, define "valid" (audit exists, is bound to both hashes, and its verdict is in-scope), and replace "pass-generation" with a counted key (`rebinds_since_last_pass_set: <int>`, producer stage 5, reset by stage 3).

**Wedge state (charter trace 8) · MAJOR · An escalation nobody can answer leaves a permanently un-assemblable run with no on-disk record that the question was ever asked — reintroducing exactly the F6 deadlock the abort fix removes, through the escalation path.**
`:98`: *"`escalated` is **terminal but NOT complete** — assembly blocks on it and it must be **resolved or killed**."* D2(c) (`:95-97`) admits only `complete` or `killed`. Under RAT3 with the owner asleep: *resolve* needs an answer (none); *kill* needs `_killed.md`'s **authority** = *"tie-break name, or the relayed owner answer verbatim + durable source"* (`:69`) — none; *abort* is correctly forbidden (`:180-184`, a runner-authored `ABORTED.md` is VOID). And `escalated` being **terminal** means D4's resume rule (*"re-dispatches every **non-terminal** child"*) does not touch it, so no later resume can advance it.
*On-disk end state:* `C/_status.md: subtree: escalated` + `escalation:`; B's status non-terminal with no relay row (B-5); no `_assembled.md` above C; no `assembled-plan.md`; no `ABORTED.md`; **no record that the question was relayed.** A later resume re-derives the same escalation and halts again, indefinitely, with no way to distinguish "awaiting an answer" from "fresh". **Nothing in the plan, the criteria or §6's risk table states this end state**, and `1-spec.md:83` claims the F6 fix removes *"permanent assembly deadlock"*.
*Instead:* name the state (`plan/AWAITING-OWNER.md`, single writer: the top orchestrator, content = the verbatim question + when relayed + what it blocks), state that a resume with an unanswered open question is a **no-op HALT**, not a re-derivation, and say plainly in the risk table that a delegated run with a sleeping owner terminates in this state rather than in a deliverable.

**Minor logical residues**
- **M-11 · MINOR · `_join.md` is a stage-done output with no invalidation, so a resume can skip a join that a reopen has already falsified.** `:66` makes `_join.md` 6.5's deterministic output; D6 tightens stage-done to *"exists AND BIND-current"* **only for review records** (`:144-145`). *Trace:* P's join clears, `_join.md` written; child A's re-plan changes its seam ⇒ reopen pending; HARDSTOP. Resume: `_join.md` exists ⇒ 6.5 is done ⇒ P proceeds to stage 7 with a child whose seam no longer matches. *Instead:* have `_join.md` record `plan_sha256` + each child's observed `seam_sha256`, and define 6.5 stage-done as *exists **and** those values still match*.
- **M-12 · MINOR · `DEC`'s two-consecutive-levels operand sits outside the node's ECON-readable surface, so the trip condition cannot be evaluated where it is assigned.** `:305-307` puts `elc` in `_status.md`, read by *"DEC at the child's stage 6"*, with *"**ONE trip condition:** two consecutive levels with `elc(child) >= 0.8 * elc(parent)`"*. Two consecutive levels needs three nodes. `METHODOLOGY.md:279-280` bounds a node's readable surface to *"its own subtree's `plan.md` + child seams + child `_status.md`"* — a node may read its parent's status (stated at `:306`) but **not its grandparent's**. *Trace:* root(40) → A(36) → B(33). B computes 33 ≥ 0.8·36 ✓ for its own level and cannot see whether root→A also failed to reduce. No key records "my level did not reduce", so the second operand has no local producer. `X8`'s fixture (`1.5-criteria.md:409`) hands one agent all three values — again supplying the artifact whose production is in question. *Instead:* add a boolean `level_nonreducing: yes|no` (producer stage 6, read by the child) so the condition is evaluable within ECON.

---

# LENS 3 — MISSED OPPORTUNITY

**MO-1 · Blocking dispatch deletes four of my findings at once, and it is still neither adopted nor declined in writing.** `Data-Distiller/SKILL.md:69-70` (per pass-1 B's verified citation): a coordinating node *"dispatches leaves, **waits**, then reads each direct child's `_status.md`"*. Making stage 6's dispatch synchronous removes the poll (M-2's t=0 kill), removes the need for a wall-clock or a "restart-only" re-dispatch rule (M-3), closes the premature-return window that creates the second writer (M-5), and makes "the owner returned" a real observable again. Pass 1 raised it (M-4); `2-plan.md` has no rejected-alternatives note. **One paragraph either way** — adopting it, or declaring why Architect's parents must be asynchronous — would be the highest-value edit in this lens.

**MO-2 · The seam operand is one design choice away from being producible** — see B-1's "Instead": have the parent's seam row be **quoted verbatim** into the child's §3 and hash *that*. It converts an always-unequal comparison into a genuine change detector, kills the mandatory first-join cascade, and makes the child's acceptance of its seam an auditable artifact rather than an inference.

**MO-3 · Generation-scoped record paths (pass-1's own suggestion) are cheaper than the immutability claim they replace** — see M-5. `completeness/s<N>/` costs one path change and makes "immutable" true by construction instead of by assertion.

**MO-4 · The one enforcement primitive available on a filesystem is unused.** Every write in §1 is "write the file". `mkdir`-atomicity is used **once** (the catalog lock) and write-temp-then-`rename` never. Specifying atomic replacement for `_status.md` would make M-4's clobber impossible and would make the "single writer" claim mechanically defensible rather than disciplinary.

**MO-5 · `DIV`'s frames are now checked by *what the reviewer was asked*, never by *what it produced* — and the structural check is free.** D13's oracle (`:269-271`) is a diff of the embedded prompts. That is a real improvement over pass 1's `frame:` label (name it: the dispatcher cannot fake brief-diversity), but it detects only a dispatch error. Pass 1's proposal — frame B's record must contain **the foreign section list it was handed plus a per-section verdict**, frame C's must contain **the enumerated questions** — makes "did frame B run" a structural property of the record. It was not adopted and not discussed.

---

# LENS 4 — UNSTATED ASSUMPTIONS & RISKS (both conditional lenses fire)

## Position / order sensitivity

The pass-1 finding is **closed**: `1-spec.md:172-176` pins `PRV`/`DIV` **before** `GBP` inside the block, requires the closing rationale sentence to be re-enumerated, and `S-SC3` (`1.5-criteria.md:354-361`) adds an intra-block-order assertion with a swapped-variant self-test. Mechanism named and adequate. Two residues:

**P-1 · MINOR · The canonical TOP predicate sentence is inserted into `stage-6`'s step 3, whose adjacency to step 4 is itself the statement of TOP's second half.** `stage-6:15-22` places *"block dispatch until the human approves"* (step 3) immediately before *"**If DECOMPOSE at a DEEPER split** — proceed red-team-gated, autonomous: **no human stop**"* (step 4); `METHODOLOGY.md:212-216` mirrors that adjacency and closes *"the gate fires at the top and **explicitly does not fire deeper**"*. D9 inserts a long content contract, an admissible-author clause and a re-ask clause into step 3. `S-F5.3/4` count sites and check the operative clause; nothing asserts that TOP's **does-not-fire-deeper** half retains its adjacency and emphasis. Under CP6 the elements to test include the ones that did not change.

**P-2 · MINOR · `stage-5`'s SEV table is a precedence list that gains conditions from `RES` and `SEAM`, and only `R2`'s P13 row covers within-file staleness.** `stage-5:22-34`'s row *"**Minor** | real but local | fix → proceed"* stands unqualified above the new RES(a) preconditions; `R2` (`1.5-criteria.md:383-388`) does name this exact risk for P13 — credit where due — but `SEAM`'s new gate-5 bounce class (D7(ii)) is **not** in the P-set (P-items cover CAP nowhere), so a CAP row that contradicts D7's scoping (B-3) is outside both regression criteria.

## Concurrency — the guard's scope is the claim I am challenging

`2-plan.md:359-369` enumerates seven surfaces and answers "yes" on six. Accessors and windows it leaves out:

**A-1 · MAJOR · `catalog-pending/<skeleton>.md` + `PROPOSAL.md`: keyed by skeleton and by run, written by *every* proposing node.** `:76` assigns them to *"**stage 6** (the proposing node's owner, **own file**)"* — but the path is keyed by **skeleton**, not by node, and `PROPOSAL.md` has no key at all. *Trace:* nodes N1 and N2 both instantiate `decomposition-node`, both patch a hole at their own stage 6, both write `catalog-pending/decomposition-node.md` and both write `PROPOSAL.md`. Last writer wins; one back-propagation is silently lost, and the run-level `PROPOSAL.md` the top orchestrator reads at run end (`:76`) has N writers — contradicting §1's *"`W` = the **only** writer permitted"* and §4's blanket *"each single-writer per §1b"*. *Instead:* `catalog-pending/<node-path>/<skeleton>.md` and one `PROPOSAL.md` per proposing node (or a `PROPOSALS/` dir), with the top orchestrator merging.

**A-2 · MAJOR · The `BROKEN-BY` marker turns a mis-broken *live* lock into a permanently unreleasable one, and the stale-lock test is still pid-liveness.** `:301` / `:366`: *"atomic `mkdir`; holder records run-root + pid; a stale lock with a dead pid may be broken only with an entry in **both** the breaker's `plan/decisions.md` **and** a `BROKEN-BY` file inside the lock dir."* *Trace:* run V holds the lock and is alive but in a different container/session, where pid liveness is not a valid test (pass-1 A-5, unaddressed). Run W judges it dead, writes `BROKEN-BY` **inside V's lock dir**, and proceeds. V finishes and releases by removing the lock dir — `rmdir` fails (non-empty), and **no rule defines release in the presence of `BROKEN-BY`**. The lock persists forever: every future run either blocks on the catalog or breaks the lock, making the lock a no-op. Also unfixed from pass 1: the holder record is still a **second write after** the atomic `mkdir`, so a reader can observe a lock dir with no holder file and cannot evaluate the stale rule at all. *Instead:* encode holder identity in the **directory name** (one atomic fact), replace pid-liveness with a heartbeat file + a stated staleness bound, and define release-under-`BROKEN-BY` (the breaker owns cleanup; the victim aborts its commit and re-reviews).

**A-3 · MAJOR · The catalog's read-and-commit still straddles a cold review — pass-1 L-15, re-worded, not fixed.** `:322-323` + `1.5-criteria.md:299-303`: committed *"only after a **cold review of the proposed diff**, only by the top orchestrator, only under the **lock**"* — the review is step (i), the lock is step (iii). The charter's named case verbatim: *"a read and a write that straddle a slow operation during which another accessor can mutate the state"* (`3-charter-given.md:143-146`). *Trace:* run 1 reads skeleton S, dispatches a review (minutes), takes the lock, commits. Run 2 committed a different edit to S in between; run 1's diff was reviewed against a base that no longer exists and applies over it. *Instead:* take the lock **before** re-reading, or re-verify the base hash after acquiring and re-review on a changed base — and add the base hash to the proposal record.

**A-4 · MINOR · Derived `index.md` regeneration is an unsnapshotted whole-tree read while every node writes its own `_status.md`; and stage 8 still orients from it first.** `:363` answers *"**yes** — the accessor set is removed"* — the **writer** set is removed; a new whole-tree **reader** is added. `S-C1` requires stage 8's false *"no single global cursor"* claim to be replaced, but does **not** require the restart procedure (`stage-8:32-33`, *"read `RUN.md` + `index.md`, walk `tree/`"*) to stop reading the derived file first or to declare the walk authoritative on resume. Bounded impact (the file is non-authoritative), but a resuming orchestrator's first orientation still comes from a possibly-torn file whose last writer died. (Pass-1 A-4, half-addressed.)

**A-5 · MINOR · `APPROVAL.md` is read by the auditor while the owner may still be writing it — the TOP predicate is a bare `exists` on a human-authored file with no completion marker.** D9's predicate (`:191-193`) is *exists + binds + passes the audit*. An owner saving a partial file satisfies `exists`; the auditor may audit a truncated quote and record `audited_approval_sha256` over it. *Instead:* require the approval to be **complete-on-arrival** (owner writes `APPROVAL.md.part` then renames, or the record carries a terminator line the predicate checks).

**A-6 · MINOR · `plan/topgate/` still has no creator, and nothing requires the relayed question to carry the path.** D9 (`:187`): *"Stage 1 **does not create** `plan/topgate/`"*; the run is fenced out (F1-1); no party is assigned to create the directory. Pass-1 A-8 raised exactly this and it is unaddressed. Compounding: RAT3 relays *"the question verbatim"* (`stage-6:66-67`) and no rule requires the question to contain the deterministic path, the content contract, or the durable-locus requirement — so the owner is asked to satisfy a contract nobody transmitted.

**A-7 · MAJOR (risk) · Fan-out: D7's "at most O(depth) re-gates" undercounts the cost by a factor of the sibling count, and the first-join case (B-1) makes it unconditional.** Each parent re-gate = 6 fresh agents at the parent **plus** D6's parent-hash invalidation of **all N children's** records = 6N more. A first join at a node with N children costs N·(6+6N) dispatches — 120 agents at N=4, on a tree where nothing was wrong — and CAP either halts it (B-3 broad) or cannot see it (B-3 narrow). `1-spec.md:165-168` declares the cost envelope out of scope, which is legitimate for **pre-existing** cost; this change *increases* the quantity, no criterion counts agents, and §6's risk table (`:383-390`) does not list it. (Pass-1 A-6, partially answered by a declaration that undercounts.)

**A-8 · MINOR · The charter's fork-provenance blockquote goes stale, breaking a section the skill itself declares load-bearing, and no criterion covers it.** `stages/charter.md:12-21`'s provenance block lists what was carried and closes *"**ADDED:** a standing sixth Completeness lens"*. Pass 2 adds `BIND`, `PRV`, `DIV`, `IDN` and the named spot-verify duty to that same charter (`1.5-criteria.md:100,170,183,204,289-293`). `grep -n "fork\|provenance" 1-spec.md 1.5-criteria.md 2-plan.md` → **no criterion, no plan step**. Meanwhile `examples/authoring-a-skill/planning.md:31-32` ships *"Charter provenance: for a forked charter, the fork-provenance blockquote (source commit + **carried vs. dropped**)"* as a Layer-2 **required section** for this very plan-type. Not a rule-ID and not a P-item, so `R1`/`R2`/`S-SC2` are all blind to it. (Pass-1 A/F1-8, unaddressed.)

**A-9 · MINOR · That the hardened TOP converts every previously-autonomous decomposing run into an owner-blocked one is acknowledged only in the loop's own state file, never in the artifact.** `LOOP-STATE.md:17-19` records it as an orchestrator call (*"a decomposing self-review would block on a sleeping owner"*); pass-1 C/D-8 asked for it to be stated. `1-spec.md:185` says only *"No new human gate. Every fix must be satisfiable by the existing gate set."* — literally true and materially incomplete: the existing gate moves from runner-satisfiable to owner-only, which is the whole point and belongs in the artifact's own failure-modes text.

---

# LENS 5 — FIDELITY (terms pinned to mechanisms)

| Loaded term (spec/owner source) | Pinned mechanism (owner intent / FINDINGS / approved record) | What pass 2 supplies | Verdict |
|---|---|---|---|
| **"fenced"** / "the runner **cannot** write" (`2-plan.md:14-16,188`) | An actual inability — the plan's own principle 1, *"remove the failure mode, don't guard it"* | A config list the corpus itself says is enforced by **naming only** (`METHODOLOGY.md:99-101`), authorable by the runner (`:105-107`), unvalidated | **PROXY** (F1-1) |
| **"human" / approval** (`FINDINGS.md:70-79`: owner-verbatim + a durable source, no self-authored approval) | An artifact traceable to the owner without an AI in the chain | Correct content contract + positive admissible author + a cold audit — but **no admissible locus exists** in the observed harness (F1-3), and the auditor is fenced out of its own output path (F1-2) | **PROXY under delegation** |
| **"join" / "a parent blocks"** (`FINDINGS.md:43-44`) | A decidable terminus for the fan-out | A poll whose dead test kills newborn children (M-2), whose terminating condition is a harness property (M-3), and whose terminal fact has no producible write (B-2) | **PROXY** — the predicates are decidable; reaching them is not |
| **"escalate-to-parent, distinguishable from death"** (`FINDINGS.md:40`) | A defined channel from any depth to the human | `escalated` as a 4th terminal value + the never-dead precedence rule — **genuinely fixes pass 1's collision** ✓ — but the relay above the originating node has **no producer** (B-5) | **PARTIAL** — half the wire |
| **"resolved"** (`FINDINGS.md:112-114`) | Must stop meaning an unreviewed author edit | Immutable records + `rebound_from/to` + a **cold rebind check** — real movement off the runner ✓ — undercut by a **presence-check** discriminator and an undefined "valid" (M-6) | **PARTIAL** |
| **"diversity"** (`FINDINGS.md:92-99`) | Blind-spot decorrelation, not decontamination | Disjoint mandates **and** disjoint input sets, real `section-sets/`, declared degradation, and an oracle on the **embedded prompt** rather than a self-declared label | **GENUINE ✓** (pass-1 Fid-2/F-4 closed; residual MO-5 is an improvement, not a proxy) |
| **"proven" → `PRV`** (`FINDINGS.md:89-99`) | Say only what the gate establishes | Four labelled strength tiers + an explicit NOT-established list incl. *"a negative no finite review can prove"* and *"N same-model instances are not N independent minds"* | **GENUINE ✓** — pass-1 Fid-1/C-D-2 **closed**. One residue: *"the reviewers had no shared context"* sits under "checked to the extent the surface exists" while the sibling-read detector fires only on a **citation** (`:218-221`), so an uncited read remains undetectable; the hedge is honest, the wording could name that specific limit. |
| **"induction, not recursion"** (pass-1 L-8) | O(children), so ECON is true not asserted | D2's direct-children-only predicate with the trust argument stated in the text | **GENUINE ✓** |
| **"a hash, not a judgment"** (F2) | A mechanical detector the node cannot self-certify | `seam_sha256` equality — the *shape* is right; the *left operand* is unproducible (B-1) | **PARTIAL** |
| **"single writer"** (`:27-28`) | One writer per mutable surface | True for `_status.md` modulo M-4; **false** for five other surfaces the change itself creates second writers on (M-5, A-1) | **PROXY** |
| **"cold granularity check per node"** (approved record `:96`) | Coldness on the sizing call | `elc` = *"a **self-declared** integer"* (`:305-306`), honestly labelled — but the departure from `:96`'s "cold" is **not declared**, though `0-baseline.md:189-194` sets the precedent of declaring exactly this class of departure | **PARTIAL / undeclared departure** (minor) |
| **"the collated deliverable: root plan + nested sub-plans"** (approved record `:180`) | `assembled-plan.md` contains the root node | D12 concatenates the level **below** the root (B-6) | **CONTRADICTED, undeclared** |
| **"declared, not silently dropped"** (`LOOP-STATE.md:20-21`) | Every deferral on the record | All 14 Tier-3 items scoped or declared; the mis-filed two-pass item corrected and queued | **GENUINE ✓** |
| **"no new human gate" / F8 untouched** (`1-spec.md:185`, `LOOP-STATE.md:43-46`) | The owner decides F8; no fix pre-shapes it | Nothing implements F8; both widening surfaces declared in the spec **and** queued in LOOP-STATE | **GENUINE ✓** (residual A-9: the existing gate's changed reachability is undeclared) |

**Fid-1 · MAJOR · The F7 fix no longer reproduces the F7 defect — but principle 1 does.** `PRV` is now honest (above). The overclaim has **moved**: `2-plan.md:14-16` asserts a mechanism ("cannot", "removed") that is a prohibition addressed to the constrained party, and `:363-368` records it as coverage. That is the same move F7 names — *a claim stronger than its mechanism* — relocated from the frontmatter to the design principles, where no criterion audits it (`S-F7` audits the frontmatter and `README`; nothing audits `2-plan.md`'s own principle statements, which become `METHODOLOGY` prose).

---

# COVERAGE CHALLENGE (CH8)

Behaviours this change could plausibly alter that **no criterion observes** — each with a scenario, ranked by impact:

| # | Sev | Unmeasured behaviour | Scenario |
|---|---|---|---|
| 1 | **Blocker** | **That `declared_seam_sha256` has a producible initial value.** `S-F2.3` asserts *"both operands have producers"* — satisfied by the §1a row existing; `X4` pre-supplies both hashes. | Every first join reopens; the cascade reaches the root and re-fires the human gate. Nothing fails a criterion. |
| 2 | **Blocker** | **That the terminal status is actually writable at the stage it is assigned to, and re-producible after a restart.** `S-PRD.3` checks that the key's name appears in the stage's file; `S-F1.4` names the wrong producers. | B-2: the status write is lost across a HARDSTOP and no oracle can see it. |
| 3 | **Blocker** | **That BIND is evaluable at the root.** `S-F3.2` requires both files with no root case; `X2` has no root arm. | B-4: the root never gates on any single-node run. |
| 4 | **Blocker** | **That `SEAM`'s bounce class agrees with `CAP`'s.** `S-F2.4` restates D7's narrowing; `S-SC2` diffs each ID against **its own** sites, never against another ID's rule. | B-3: universal halt or uncapped ping-pong. |
| 5 | **Blocker** | **That the escalation relay has a write target above the originating node.** `S-F1.6` asserts the precedence rule and the dead definition; nothing asserts a relay producer; `X1`'s escalated verdict is *"NOT dead, relay upward"* — the fixture asks for the **verdict**, not the artifact. | B-5: root polls a relaying parent forever. |
| 6 | **Blocker** | **What `assembled-plan.md` contains.** No criterion reads the deliverable; `X1`-intact stops at *"⇒ assemble"*. | B-6: the root node's §4 is absent from the shipped plan. |
| 7 | **Major** | **`subtree: killed`'s producer.** `S-F1.4` counts three producers for a four-value domain. | M-1: parent self-certifies a kill, or writes another node's file. |
| 8 | **Major** | **The dead test against a newborn child.** `X1`'s holed leaf is *"gated clean but wrote **no** terminal status"* — i.e. a late-stage child, never a t=0 one. | M-2: a clean two-leaf tree escalates before either child writes. |
| 9 | **Major** | **Restart-vs-live-poll discrimination.** `S-F1.6` asserts the post-restart rule as *stated*; no arm distinguishes a resuming parent from a polling one. | M-3: a live parent re-dispatches every working child on every poll. |
| 10 | **Major** | **The fence's coverage of the record files under re-dispatch.** `S-C1` asserts `_status.md` + the token; the record files are covered by the word "immutable". | M-5: a two-generation record set passes every gate. |
| 11 | **Major** | **The second rebind.** `X5` exercises exactly one. | M-6: the legal second rebind bricks the node, or one audit licenses an arbitrary edit. |
| 12 | **Major** | **The concurrency guard has no behavioural arm at all** — the lock, the ordering, `BROKEN-BY`, `catalog-pending` collisions, the seed race. `S-C1` is *prose only*; `2-plan.md:366` answers "yes" and the verification is a grep of the sentence that makes the claim. | A-1, A-2, A-3. |
| 13 | **Major** | **Agent fan-out.** No criterion counts dispatches; `S-C?` none exists. | A-7: 120 agents on a clean 4-child node. |
| 14 | **Minor** | **`_join.md`'s validity after a reopen.** `S-F1.8` asserts *"a deterministic output and a stage-done predicate for it"*, not its invalidation. | M-11: resume skips a falsified join. |
| 15 | **Minor** | **`DEC` evaluability within ECON.** `S-C2` asserts one trip condition stated identically; `X8` supplies three levels to one agent. | M-12: the condition cannot be evaluated where assigned. |
| 16 | **Minor** | **`plan/topgate/`'s creation and the transmitted contract.** `S-F5.1` asserts only that stage 1 states it does **not** create it. | A-6: the predicate stats a path nobody produces. |
| 17 | **Minor** | **The charter's fork-provenance block.** Zero criteria. | A-8: a shipped Layer-2 required section goes stale. |
| 18 | **Minor** | **`APPROVAL.md`'s completeness on read.** `X3` supplies whole files. | A-5: the audit passes on a truncated quote. |
| 19 | **Minor** | **The run-complete marker for a single-leaf run.** `S-C3` names the artifact per shape only. | Trace 1(a): no completion marker; resume semantics undefined. |
| 20 | **Minor** | **`off_limits_paths`'s new third semantic.** `S-F4.3` asserts de-conflation of two meanings. | F1-4: an intra-run fence documented as external read-only context. |

---

# LABEL AUDIT (CH9 / CH10)

**Advisory half — a real all-clear.** Every criterion in `1.5-criteria.md` is marked `[gating]` (checked: Parts A–E, plus `R1`/`R2`); `1.5-criteria.md:411-417` states the degraded route (`verified = no` + a **declared deferral**, with the honest note that **no risk-acceptance is grantable** under RAT3 with the owner asleep — pass-1's 3/3 minor, **closed**). So the "relabel a gate as advisory" dodge does not occur. ✓

**Per gating criterion — governed path, what the planned verification exercises, evidence I checked:**

| Criterion | Governed path | What the verification exercises | Verdict |
|---|---|---|---|
| **S-PRD** | Every read fact has a producer | `check.sh` parses the canonical fenced block and asserts each (key,stage) pair; baseline replay yields zero pairs | **Earned on its stated scope** ✓ — but the scope is one-directional: it checks *schema key → stage*, never *written fact → table row*, so F1-5 (`assembled-plan.md`, the `decisions.md` spawn-id write) and M-1 (`killed`) pass. **Sub-part 3 → unverified as a closure property.** |
| **S-F1** | The join's behaviour | prose per-site + **X1** (real decision arms) | X1 is a genuine execution arm ✓. But sub-part 4's producers are **wrong** (B-2) and the fixture teaches them; `killed` is uncounted (M-1); the relay producer is unasserted (B-5). **Sub-parts 4 and 6 → unverified.** |
| **S-F2** | Reopen + cross-node check | prose + **X4** | X4 exercises the assembly-time three-way diff ✓. The **reopen trigger is verified against pre-supplied hashes** (B-1) and the CAP class is restated rather than reconciled (B-3). **Sub-parts 2–4 → unverified.** |
| **S-F3** | BIND at three points | prose + **X2** (hash mismatch + post-approval re-draft) | X2 now exercises the gate-artifact half ✓ (pass-1 L-6 **closed**). No root arm (B-4); no two-rebind arm (M-6). **Sub-part 2 → unverified at the root.** |
| **S-F4** | `redteam_context` as a contract key | **column-0 YAML-key** assertion + a replay designed to defeat the naive-substring oracle (`:125-128`) | **Genuinely earned** ✓ — the one oracle whose design demonstrably discriminates the conflation it targets. The governed fact is structural; no X- arm needed (and `X7(iii)` adds the stop-the-run behaviour anyway). |
| **S-F5** | The topgate | prose (5 sites) + **X3** (VOID arm + an *adjacent-answer* arm) | X3's second variant is a real improvement ✓ (RAT1 re-ask exercised). But the governed path is *"the runner cannot write it"*, verified by grepping that the **guidance** recommends fencing (`:132-135`) — CH9's named proxy, and F1-1/F1-2 show the fence is not the mechanism. No arm for a config that omits the fence; no arm for the auditor's own write. **Sub-parts 2 and 8 → unverified.** |
| **S-F6** | Kill + abort | prose + **X1** (killed sibling) | `killed_handled_at == plan_sha256` is now a **string comparison** ✓ (pass-1 L-4 **closed** — the fixture can carry it). ABORTED authorship is asserted in prose with no arm; the escalated-wedge end state is unmeasured. **Sub-part 4 → unverified behaviourally.** |
| **S-F7** | Softened claim + real diversity | normalized absence sweep **with a bold+wrap re-insertion self-test** + prose + **X6** | The sweep is well designed ✓; `section-sets/` + declared degradation closes the 3/3 collapse ✓; the prompt-diff oracle is a real audit surface ✓. Residual: nothing checks that a frame was **performed** (MO-5). **Earned, with one named limit.** |
| **S-F9** | Dispatcher id + sibling ban | prose + **X6** (incl. an all-"unavailable" self-report arm) | **Earned** ✓ — pass-1 blocker B/L-11 closed by mechanism (dispatcher-recorded + declared-degraded), and X6's second record set exercises exactly the unavailable case. |
| **S-F10** | "Resolved" | prose + **X5** | X5 exercises the untraced-edit case ✓ and the `clean-demoted` distinction ✓ (pass-1 C/D-4 closed). Cannot discriminate a **plausible-but-false** trace, and tests only one rebind (M-6). **Sub-parts 2–3 → partially unverified.** |
| **S-C1** | Concurrency partition + lock | **prose only** | The governed path is a concurrency behaviour and the baseline text asserts the opposite of the truth, so the corrected statement *is* part of the fix — but "the corpus says there is a lock" is not coverage of A-1/A-2/A-3. **Unverified for the lock, the ordering, and the cross-run claim.** |
| **S-C2** | DEC operand | prose + **X8** | Honest-label sub-part is real ✓ (pass-1 Fid-3 closed). X8 exercises the comparison on pre-written values; **evaluability within ECON is unexercised** (M-12). **Partially unverified.** |
| **S-C3** (XPM) | The terminus | **prose only** | `FINDINGS.md:137` calls this the skill's most likely real invocation path; the single-leaf shape now has no completion marker and no arm. **Proxy.** |
| **S-C4** (IGM) | Ingest procedure | prose + **X7(ii)** | **Now has a behavioural arm** ✓ (pass-1 coverage #14 closed) — the invented-unmarked-§4 fixture exercises the duty. |
| **S-C5** | Layer-2 slots in 3 skeletons | prose | Structural, and the 3-of-3 correction is verified against `0-baseline.md:183-185`. **Earned** ✓ |
| **S-C6** | Spot-verify duty | prose (numbered step + floor) + **X7(i)** | **Earned** ✓ (pass-1's "assignment is structural, performance out of reach" now has an arm citing-nothing vs citing-two). |
| **S-C7** | TPL3 propose-then-commit | **prose only** | Highest-consequence path in the change (a write outside the run root, into a shared cross-project repo) and the ordering defect A-3 lives exactly here. **Unverified.** |
| **S-C8** (DEP) | Dependency DAG + order section | prose | Structural (a column, a section, a cycle rule) ✓ — but "a cycle is a **blocker** at its gate" is a runner behaviour with no arm. **Mostly earned; cycle-detection unverified.** |
| **S-C9** (root pinned) | Path pinning | prose + the declared departure in `decisions.md` | **Earned** ✓ |
| **S-C10** (§4 heading) | One canonical string | **pinned extraction predicate** + distinct-normalized count = 1, baseline = **6** | **Earned** ✓ — pass-1 F-5/A/C-L-3 closed (predicate now defined, count corrected). |
| **S-SC1** | Frontmatter validity | `quick_validate.py` + length/bracket assertions + injected-`<` self-test | **Earned** ✓ |
| **S-SC2** | Cross-file consistency, 21+15 IDs | `ruleid-sitemap.sh` (word-boundary + **reported** phantom exclusions) + `idcollide.sh` + a recorded hand-diff | **Well earned** ✓ — pass-1 F-1/F-2/F-3 and A/F1-1/F1-2 all closed, with the two lessons promoted into instruments carrying can-fail tests. **Gap:** the hand-diff compares each ID against its own sites, so an ID-vs-ID contradiction (B-3: `SEAM` vs `CAP`) is out of scope. |
| **S-SC3** | Position, incl. intra-block | offsets + intra-block order + rationale enumeration + two self-tests | **Earned** ✓ (pass-1 3/3 closed). |
| **S-SC4** | live == source | `diff -rq` **before and after**, direction asserted | **Earned** ✓ — with the caveat that the pre-state is measured on a **dirty** worktree for one file (F1-6). |
| **S-SC5** | The description still triggers | vocabulary + suggest-clause assertions + a stripped-clause self-test | **Earned** ✓ (pass-1 C/A-3 closed). |
| **R1** | Site-set non-erosion | `ruleid-sitemap.sh` vs **B0.6**, and *"the reference table must itself pass the baseline replay"* | **Now earned** ✓ — the defect that made pass 1's R1 `verified = no` is fixed by B0.6 + B0.7. |
| **R2** | Deliberate-change completeness | per-P paired positive-at-every-old-site + normalized absence, incl. within-file | **Earned** ✓ — the P-set is now stated once authoritatively (11 items), ending pass 1's three-way disagreement. **Gap:** `assembled-plan.md`'s composition (B-6) and CAP's class (P-2) are outside the P-set. |
| **X1–X8** | The behavioural halves | separately-spawned holed + intact arms, per-item verdict tables, *"both arms the same verdict ⇒ `verified = no`"* | The **protocol** is sound and now pinned (`2-plan.md:353-357`) ✓. The **recurring flaw** is unchanged from pass 1 and now appears in five arms: **X1, X4, X5, X6(second set), X8 pre-supply the very artifact whose production is in question** (terminal statuses; both seam hashes; a single rebind's fields; a fetchable owner locus in X3-intact; three levels of `elc`). A fixture that hands the agent the fact cannot test whether any stage produces it. |

**Summary.** Real, earned progress: **S-F4, S-F9, S-C4, S-C5, S-C6, S-C9, S-C10, S-SC1, S-SC2, S-SC3, S-SC4, S-SC5, R1, R2** — fourteen criteria whose oracles now discriminate what they claim (up from three in pass 1). But **four gating criteria whose governed path is a runner or concurrency behaviour remain grep-only** (S-C1's lock/cross-run half, S-C3, S-C7, S-F5's fence half), and **eleven sub-parts of Tier-1/Tier-2 criteria are verified against fixtures that pre-supply the artifact in question** (S-PRD.3 as a closure property, S-F1.4/.6, S-F2.2–.4, S-F3.2-at-root, S-F5.2/.8, S-F6.4, S-F10.2–.3, S-C2's ECON half). Per CH9's closing rule each is treated as **unverified**.

---

# RATIFICATION AUDIT (CH11 / CH12)

**No recorded owner-ruling is relied on by the spec, criteria, or plan** — `grep -n "OWNER RULING\|owner ruling\|ratif"` over `1-spec.md 1.5-criteria.md 2-plan.md` returns three hits, all of them *references to the absence or the queueing of a ruling* (`1-spec.md:168` the unaudited two-pass ruling, queued; `:196-198` `Guarded_change`'s RAT1 cited as **prior art for the fix**, not as a ruling about this change). So there is no ratification record to audit as such. Four adjacent artifacts audited anyway:

1. **The only owner text in the closed set** is `LOOP-STATE.md:3-5` and `:23-25`, both verbatim with timestamps, in a file the guarded-change runner does not author. Neither is used as a decision authority in the artifacts under review.
2. **Pass 1's inflation is corrected in the spec — and reproduced in the charter given to *this* review.** `LOOP-STATE.md:64-67` rules that the broad scope reading is an *"Interpretation … stated so Roy can correct it"*, that `1-spec.md` had inflated it to *"the approved cycle-1 scope"*, and that *"the hedge is authoritative; the spec's phrasing is not. The next pass must carry the hedge."* `1-spec.md:188-191` **carries the hedge correctly** ✓ (pass-1 A/RAT-1, C/D-7 closed). But `3-charter-given.md:206` — the reviewer charter, authored by the same author for this pass — opens the scope-fidelity instruction with *"**The approved cycle-1 scope** is in `LOOP-STATE.md` + this run's `1-spec.md`."* **MINOR (RAT-1):** the unratified label was removed from the place a criterion looks and re-introduced in the place that **frames the cold reviewers' scope test**. That is the RAT2 shape (an author's expansion presented as the owner's, one artifact removed from where it was ruled out) and it is load-bearing precisely because it tells three reviewers what "in scope" means. *Instead:* the charter should quote LOOP-STATE's hedge verbatim, as the spec now does.
3. **The narrowing of "until nothing surfaces"** is carried correctly as a **continue** trigger only (`1-spec.md:190-191`, per `LOOP-STATE.md:60-63`) ✓ — no inflation.
4. **The autonomous-restart authorization** is not over-extended: `LOOP-STATE.md:29-30` bounds it to restarting runners, and nothing in the plan uses it to answer a queued question ✓.
5. **Pass-1 B/RAT-4 is closed:** the one unaudited owner ruling FINDINGS identifies (*"two passes"*, `FINDINGS.md:157-159`) now has a re-ask path — `LOOP-STATE.md:70-71` queues it explicitly, not answered ✓.
6. **Spot-verification of owner quotes:** both `LOOP-STATE.md` quotes carry timestamps and owner attribution in a durable file outside the change folder. I **could not** verify them against a chat transcript (not in the closed set) — flagged as unverifiable, not accepted silently. This is the same limit that makes F1-3 a finding: the loop's own best owner locus is orchestrator-authored.

---

# FLAGGED AS UNVERIFIABLE

1. **`oracles/*.sh` and `fixtures/X1…X8/` do not exist** (`<CF>/oracles/` and `<CF>/fixtures/` are **empty**), nor do `stages/stage-6.5-join.md` or `templates/seed/section-sets/`. Every claim about what an oracle *will* assert is a plan; my label audit evaluates the **stated design**, not running code.
2. **Prior art.** I did not open `Guarded_change/`, `Dragonfly/` or `Data-Distiller/` this pass. The plan's claims about RAT1/CH11 and about Data-Distiller's join are carried from pass-1 B's verified citations, not from my own reading; MO-1's Data-Distiller citation is inherited, not re-verified.
3. **Reviewers A and C's bodies** (I read only their ranked summaries) — see the header. Any A/C finding whose substance is not in its summary row is outside my carry-forward check.
4. **Whether the harness exposes a dispatcher-side spawn id in all configurations** — I cannot report my own; `3-redteam-plan.md:6-10` shows three dispatcher-recorded ids **do** exist for this very pass, which is the positive evidence D10 needed and got. Not a finding; recorded as verified-by-the-record.
5. **Whether any non-agent-authored durable owner locus exists in this environment** (F1-3). I could not find one in the closed set; I cannot prove none exists outside it.

---

# RANKED SUMMARY

| # | ID | Sev | One line |
|---|---|---|---|
| 1 | **B-1** | **Blocker** | `children.<c>.declared_seam_sha256` has no derivable initial value: the SEAM reopen fires on **every** parent–child pair at the first join (cascading to the root and re-firing the human gate), or is adopted from the child — in which case **F2 is unfixed**. `X4` pre-supplies both operands. |
| 2 | **B-2** | **Blocker** | The terminal `subtree: complete` is assigned to stages 6/6.5, which run **before** stage 7 produces its precondition (`_assembled.md`), and it is the one write with **no stage-done predicate** — so root+1-leaf either escalates a clean leaf or polls forever, depending on the runner. `S-F1.4` and `X1` freeze the wrong producer. |
| 3 | **B-3** | **Blocker** | D7 scopes the SEAM bounce to *"the same child pair"* while `CAP` is defined as *"same gate + same targeted node **section**"* — so either every 2-child node's first join trips the human tie-break, or the A↔B seam ping-pong is uncapped and never terminates. |
| 4 | **B-4** | **Blocker** | `BIND`'s parent-hash clause is undefined at the **root** (no parent plan in the charter's closed set, no producer for `parent.plan_sha256`) ⇒ the root's records can never be current ⇒ a single-node run — the scale-down shape **and this loop's own** — can never gate. |
| 5 | **B-5** | **Blocker** | The escalation relay above the originating node has **no producer, no file, no reader**: `escalation:` is written by stage 5 only, and a relaying parent has no assignable write — F1's *"channel no stage defines"* survives inside the rule titled "RAT3's missing wire". |
| 6 | **B-6** | **Blocker** | `assembled-plan.md` is composed from the root's **children**, so the deliverable ships without the root node's plan — including its §4 output locations — contradicting the approved record (`:180`) and baseline `stage-7:14`, undeclared; and it is **empty** for a single-leaf run. No criterion reads the deliverable. |
| 7 | **F1-1** | **Blocker** | *"The runner **cannot** write `APPROVAL.md` because the path is fenced"* is false against the source: `METHODOLOGY.md:99-101` says naming **is** the fence and no guard catches a stray write; the config is runner-authorable; nothing validates the fence's contents. |
| 8 | **F1-2** | **Blocker** | The fence and the cold audit are mutually exclusive: `AUDIT.md` is written **into** the fenced dir by a runner-dispatched agent ⇒ either the fence admits run-dispatched writers (F5 reproduced) or `AUDIT.md` is unwritable and TOP is unsatisfiable forever. |
| 9 | **M-1** | **Major** | `subtree: killed` has **no producer** in §1a; D3's answer (the parent writes it) breaks the single-writer rule; `S-F1.4` requires *"all **three**"* producers for a four-value domain. |
| 10 | **M-2** | **Major** | D4's dead test declares an alive, freshly-dispatched child dead (status absent, no grace, no blocking dispatch) — so the join can kill every child at t=0, manufacturing the double-owner race the fence exists to contain, then escalate. |
| 11 | **M-3** | **Major** | *"After a restart there are no in-flight children **by construction**"* — the join's sole terminating condition — rests on an unstated harness property `RST` does not assert for sub-orchestrators, and nothing lets a parent tell restart-resume from a live poll. |
| 12 | **M-5** | **Major** | The fencing token covers **`_status.md` only**; the re-dispatch leaves a second writer on `plan.md`, `completeness/*`, `adversarial/*`, `<node>/decisions.md` and `_assembled.md`, all fixed filenames, with "IMMUTABLE once written" unenforced. (Pass-1 A-2 fixed for 1 of 6 surfaces.) |
| 13 | **M-6** | **Major** | The **second** RES(a) rebind — explicitly legal — cannot satisfy BIND's rebind-record clause, `rebound_from` has two homes, "valid" is undefined, and a **presence-check** discriminator lets one stale audit license a later edit. `pass-generation` is an operand with no producer, key, counter or reset. |
| 14 | **M-4** | **Major** | The fencing token is not a compare-and-swap: the discard is enforced *by the superseded writer*, the check is itself a read-modify-write, and the orphan can clobber the seq field — while no rule has the **parent** cross-check its recorded `dispatch_seq`. |
| 15 | **F1-3** | **Major** | No admissible owner locus is demonstrated to exist: the only durable owner text in the closed set is orchestrator-authored, so D9's fix is satisfiable only by an agent transcription — the live F5 shape. |
| 16 | **A-2** | **Major** | `BROKEN-BY` inside the victim's lock dir makes a mis-broken **live** lock unreleasable (non-empty `rmdir`, no release rule) ⇒ a permanent catalog lock; pid-liveness is still an invalid cross-session test and the holder record is still a second write after the atomic `mkdir`. (Pass-1 A-5 unaddressed.) |
| 17 | **A-3** | **Major** | The catalog's read-and-commit still straddles the cold review (review at step i, lock at step iii) — the charter's named read-modify-write-across-a-slow-op case, verbatim. (Pass-1 L-15 unaddressed.) |
| 18 | **A-1** | **Major** | `catalog-pending/<skeleton>.md` and run-level `PROPOSAL.md` are keyed by skeleton/run but written by **every** proposing node ⇒ two writers on one path, contradicting §1's single-writer invariant. |
| 19 | **A-7** | **Major** | Fan-out: D7's *"at most O(depth) re-gates"* undercounts by the sibling count — a first join at N children costs N·(6+6N) dispatches — and no criterion counts agents. (Pass-1 A-6 answered by a declaration that undercounts.) |
| 20 | **Wedge** | **Major** | An unanswerable escalation leaves a terminal-but-not-complete node that no resume can advance, no authority can kill and no runner may abort — a permanent assembly deadlock with **no on-disk record the question was asked** — while the spec claims F6's fix removes exactly that. |
| 21 | **F1-4** | **Major** | `off_limits_paths` is repurposed for a run-**internal** path against its documented semantics, inside the fix that claims to de-conflate that key. |
| 22 | **Fid-1** | **Major** | `PRV` is now honest, but the overclaim moved: design principle 1's *"cannot" / "the failure mode is removed"* is a prohibition addressed to the constrained party, recorded as coverage in §4 — the F7 shape, relocated to where no criterion audits it. |
| 23 | **M-11** | **Minor** | `_join.md` is a stage-done output with no invalidation rule, so a resume can skip a join a SEAM reopen has already falsified. |
| 24 | **M-12** | **Minor** | DEC's two-consecutive-levels operand lies outside the node's ECON-readable surface, so the trip condition cannot be evaluated where it is assigned; `X8` supplies three levels to one agent. |
| 25 | **A-6** | **Minor** | `plan/topgate/` still has **no creator** (pass-1 A-8 unaddressed), and nothing requires the relayed question to carry the path or the content contract. |
| 26 | **A-8** | **Minor** | The charter's fork-provenance "ADDED" block goes stale as the charter gains BIND/PRV/DIV/IDN — breaking a Layer-2 required section the skill itself ships — with **zero** criteria covering it. (Pass-1 A/F1-8 unaddressed.) |
| 27 | **A-4** | **Minor** | Derived `index.md` regeneration is an unsnapshotted whole-tree read while nodes write `_status.md`, and `stage-8` still orients from it before walking `tree/`. (Pass-1 A-4 half-addressed.) |
| 28 | **A-5** | **Minor** | `APPROVAL.md` is read by the auditor with no completeness marker — the predicate can pass, and the audit can bless, a truncated owner quote. |
| 29 | **A-9** | **Minor** | That the hardened TOP converts every previously-autonomous decomposing run into an owner-blocked one is recorded only in `LOOP-STATE.md`, never in the artifact. (Pass-1 C/D-8 unaddressed.) |
| 30 | **RAT-1** | **Minor** | The unratified scope label *"the approved cycle-1 scope"* — removed from `1-spec.md` per `LOOP-STATE.md:64-67` — is **re-introduced in `3-charter-given.md:206`**, the artifact that frames the cold reviewers' own scope test. |
| 31 | **F1-5** | **Minor** | The `PRD` table omits `assembled-plan.md` (the run's deliverable) and D10's `decisions.md` spawn-id write, while §1 closes *"Nothing in §2–§3 reads a fact absent from this table."* |
| 32 | **Fid-6** | **Minor** | `elc` as a self-declared integer departs from the approved record's *"a **cold** granularity check per node"* (`:96`) without the declaration `0-baseline.md` B0.8 sets the precedent for. |
| 33 | **P-1 / P-2** | **Minor** | Position residues: the TOP insertion perturbs `stage-6` step 3/4 adjacency (TOP's does-not-fire-deeper half) and `SEAM`'s new gate-5 bounce class is outside both regression criteria. |
| 34 | **F1-6** | **Minor** | `0-baseline.md`'s *"worktree clean at run start"* is still false (` M Architect/guarded-change.architect.md`), which now affects `S-SC4`'s direction assertion and the baseline replay for that file. (Pass-1 F-6 unaddressed.) |
| 35 | **MO-1…MO-5** | **Minor (opp.)** | Blocking dispatch (deletes M-2/M-3/M-5, still neither adopted nor declined); quote-the-parent's-seam (fixes B-1); generation-scoped record paths; write-temp-then-rename; frame-performance checks on the record. |
| 36 | **F1-7** | **Nitpick** | `2-plan.md:73` cross-references *"§3 D7"* for the topgate; the topgate is **D9**. |

**Pass-1 findings I confirm are genuinely CLOSED, with the mechanism named** (so the author is not asked to re-litigate them): B/L-2 → the 4th terminal value `escalated` + the *"a status carrying `escalation:` is NEVER dead"* precedence rule stated **first**. B/L-8 → D2's direct-children induction with the trust argument in the text (ECON now true, not asserted). B/L-9 → `_assembled.md` inside the terminal condition + mid-tree GBP scope = own subtree. B/L-7 → `_orphan-<name>/` archiving, skipped by both the walk and assembly. B/L-4 → `killed_handled_at == plan_sha256`, two recorded strings. B/L-6 + C/L-2 → `approved_root_plan_sha256` / `approved_root_seam_sha256` / `audited_approval_sha256`, with the re-fire consequence stated honestly. B/L-11 → dispatcher-recorded `spawn_id` + *"three 'unavailable' self-reports are NOT evidence"* + declared-degraded. B/L-12 → `ABORTED.md`'s authorship contract + VOID rule + cold audit. B/L-14 → `template` and `granularity` added to the schema. B/F-1/F-2/F-3 + A/F1-1/F1-2 → B0.6's re-captured 21-ID map and B0.7's two instruments with can-fail self-tests. B/F-4 + A/F1-4 + C/D-3 → `section-sets/` + a stated source order + declared degradation. B/Fid-1 + C/D-2 → `PRV`'s four strength labels and NOT-established list. B/Fid-2 → the diversity oracle is the **embedded prompt**, not the label. B/Fid-3 + C/A-4 → `elc` labelled self-declared with the honest consequence. B/A-1 + C/A-2 → `S-SC3`'s intra-block order + rationale enumeration. B/A-3 → all five new surfaces enumerated in §1b/§4. B/A-7 + C/L-4 → the risk-acceptance/deferral asymmetry stated up front. B/F-5 + C/L-3 → `S-C10`'s pinned extractor and the corrected count of 6. B/RAT-4 → queued in `LOOP-STATE.md:70-71`. C/D-4 → `clean-demoted` + `demoted_findings`. C/A-3 → `S-SC5`. C/D-5/D-6 + A/F4-2/U-1 → the F8 migration surface declared in the spec **and** queued, and the approved-decision record added to the closed set. C/F1-2 → the two-pass mis-filing corrected. A/F2-2 → *"while a reopen is pending the parent does not re-dispatch"*. A/F4-1 → the ID grep extended to `templates/` + `examples/`. A/C3 → the dead test moved onto disk (`updated` vs `dispatched_at`), modulo M-2/M-3.

**Not closed despite being carried forward (fixed by re-wording, or fixed for one instance of a class):** B/A-2 (fenced for `_status.md` only — M-5); B/L-15 (lock ordering unchanged — A-3); B/A-5 (torn lock holder unchanged — A-2); B/A-8 (topgate creator — A-6); A/F1-8 (fork provenance — A-8); C/D-8 (TOP's changed reachability — A-9); B/F-6 (stale baseline claim — F1-6); B/A-4 (index.md read order — A-4); B/A-6 (fan-out declaration undercounts — A-7).

**WORST SEVERITY: blocker**

**The one root cause worth naming.** `PRD` was the right diagnosis and it is genuinely implemented as a *table*. What pass 2 did not add is `PRD`'s second half: **a producer needs a derivable value, a stage that can actually run at that point in the sequence, and a stage-done predicate that survives a restart.** Every blocker above is one of those three missing: B-1 is a producer with no derivable value; B-2 and B-6 are producers scheduled against the stage order; B-4, B-5 and M-1 are operands with no producer at all in the case that matters (the root, the relaying parent, the killed child); B-3 is a producer whose *consumer* (CAP) disagrees about what it produced. The `S-PRD` oracle checks the direction that cannot catch any of them — *does each schema key name a stage?* — rather than *can that stage produce that value, at that point, recoverably?* If the orchestrator routes this back, the minimal addition is a **fourth column** in §1a — *"derivable from what, at what point in the stage order, and what output-exists check proves it happened"* — plus one criterion that the column is filled for every key. That single column falsifies B-1, B-2, B-4, B-5, B-6 and M-1 at draft time, on paper, before any fixture is written.agentId: a6895458a4115080f (use SendMessage with to: 'a6895458a4115080f', summary: '<5-10 word recap>' to continue this agent)
<usage>subagent_tokens: 266470
tool_uses: 33
duration_ms: 1305080</usage>