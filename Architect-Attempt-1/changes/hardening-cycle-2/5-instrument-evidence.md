# Pass 3 — INSTRUMENT EVIDENCE (built, then executed, then described — in the past tense)

Every claim below is backed by the command and its real output, captured at build time. Nothing here
describes a script that does not exist: that inversion is what tripped the cap on pass 2.

Generated: 2026-07-25T16:31:27Z · base commit `b08f5a9` · corpus pinned by `oracles/lib-corpus.sh`

## Files that now exist (they did not when pass 2 described their behaviour)
```
total 76
drwxrwxr-x 2 zero zero 4096 Jul 25 12:30 .
drwxrwxr-x 4 zero zero 4096 Jul 25 12:31 ..
-rwxrwxr-x 1 zero zero 1555 Jul 25 12:30 baseline-replay.sh
-rwxrwxr-x 1 zero zero  349 Jul 25 12:28 check.sh
-rw-rw-r-- 1 zero zero 4042 Jul 25 12:29 checklib.py
-rw-rw-r-- 1 zero zero 8946 Jul 25 12:29 criteria.tsv
-rw-rw-r-- 1 zero zero  126 Jul 25 12:25 expected-sites.phantoms.txt
-rw-rw-r-- 1 zero zero 2847 Jul 25 12:25 expected-sites.txt
-rwxrwxr-x 1 zero zero 1103 Jul 25 12:25 gen-expected-sites.sh
-rwxrwxr-x 1 zero zero  575 Jul 25 12:29 gen-preserve-counts.sh
-rwxrwxr-x 1 zero zero 1230 Jul 25 12:30 gen-sweep-rows.sh
-rwxrwxr-x 1 zero zero 2806 Jul 25 12:27 idcollide.sh
-rw-rw-r-- 1 zero zero  589 Jul 25 12:25 lib-corpus.sh
-rwxrwxr-x 1 zero zero 3425 Jul 25 12:30 lockrace.sh
-rw-rw-r-- 1 zero zero  332 Jul 25 12:29 preserve-counts.txt
-rwxrwxr-x 1 zero zero 1663 Jul 25 12:26 ruleid-sitemap.sh
-rw-rw-r-- 1 zero zero 3584 Jul 25 12:30 sweep-rows.generated.md
```
## 1. expected-sites.txt — GENERATED, not authored
```
$ oracles/gen-expected-sites.sh . > oracles/expected-sites.txt   # phantoms to stderr, reported
# GENERATED by oracles/gen-expected-sites.sh — do not hand-edit.
# command: changes/hardening-cycle-2/oracles/gen-expected-sites.sh .
# generated: 2026-07-25T16:25:37Z
# format: <ID> <space-separated relative file paths>
...
PHANTOM-EXCLUDED TOP METHODOLOGY.md:79 [ON TOP OF]
PHANTOM-EXCLUDED TOP examples/authoring-a-skill/planning.md:25 [ON TOP OF]
```
The 21 rows it produced match `0-baseline.md` B2 exactly — the generator and the hand measurement agree.

## 2. ruleid-sitemap.sh — was a printer (`exit 0`), is now a checker. Mutations RUN:
```
$ oracles/ruleid-sitemap.sh .                      -> SITEMAP: OK ... ; EXIT=0
$ # MUTATION 1: delete GBP entirely from stages/stage-5-gate.md (site erosion)
MISSING    GBP stages/stage-5-gate.md
SITEMAP: FAIL — site set eroded or drifted
EXIT=1        <-- pass 1 AND pass 2 both exited 0 here
$ # MUTATION 2: stray GBP appended to templates/seed/leaf-task-spec.md (site drift)
UNEXPECTED GBP templates/seed/leaf-task-spec.md
EXIT=1
$ # MUTATION 3: delete GBP row from METHODOLOGY index -> ID stays in scope (E/18 closed)
SITEMAP: OK ; EXIT=0   (the expectation is a FROZEN generated data file, so deleting a row cannot shrink scope)
```

## 3+11. check.sh — positive per-site assertion, MEASURED sites, POLARITY GUARD
Site sets are never typed: each row carries an ERE over the OLD text and the site set is whatever
matches it in the tree under test. The polarity guard was executed:
```
$ # the pinned S-OFL sentence inserted at METHODOLOGY.md behind a foil marker, and stated straight elsewhere
FAIL S-OFL            kind=NEW      measured_sites=3
       FOIL-ONLY    METHODOLOGY.md  (pinned string present but negated — polarity guard)
       ABSENCE-VIOL METHODOLOGY.md~Naming is the fence
```
Pass 2's checker would have accepted that foil. It is now rejected.

## 4+13. baseline-replay.sh — BOTH halves of Layer-2 config item (6), executed
```
BASELINE REPLAY @ b08f5a9  (changes/ removed from the tree, so the corpus pin is proven not assumed)
  NEW rows failing at baseline      : 42   <- must be ALL of them
  PRESERVE rows passing at baseline : 8   <- must be ALL of them
  NEW rows WRONGLY passing          : none
  PRESERVE rows WRONGLY failing     : none
REPLAY: OK — every assertion discriminates
```
`changes/` is **deleted** from the materialised tree before the run, so the corpus pin is proven, not
assumed. **42 of 42 NEW rows fail at baseline; 8 of 8 PRESERVE rows pass; zero wrong either way.**
The PRESERVE half is a real non-erosion check: counts come from `gen-preserve-counts.sh` (generated),
and deleting the phrase from a file drops the count below baseline and FAILS.

## 5. The catalog lock DESIGN was fixed, then tested
Reviewer E executed two of pass 2's promised outcomes and both were false. So the design changed:
the lock is an **atomic symlink whose target is the holder's pid** (lock and owner appear in one
indivisible step, closing the mkdir-then-write-pid window D/5 found), a lock whose target pid is not
alive is **stale by definition and self-breaking** (a killed holder cannot deadlock the next run), and
the lock sits **beside** the catalog so a first run can take it. The H4 control is now a genuine
read-modify-write, not an atomic append.
```
== CASE 1: two concurrent contenders, first run, catalog dir absent
  winners=1 (want exactly 1)
== CASE 2: holder is SIGKILLed (an Architect HARDSTOP). Pass 2's design LEAKED here.
oracles/lockrace.sh: line 25: 92240 Killed                     bash -c 'ln -s $$ "'"$LOCK"'"; kill -9 $$' > /dev/null 2>&1
  lock present after kill: yes  target pid: 92240
  (broke stale lock of dead pid 92240)
  next run ACQUIRED -> NOT deadlocked  [PASS]
== CASE 3: holder is alive -> must NOT be broken
  refused to break a live holder  [PASS]
== CASE 4: H4 unguarded control — a genuine READ-MODIFY-WRITE, so it MUST fail without the lock
  unguarded: 44/120 — LOST UPDATES, control fails without the guard  [PASS]
  guarded:   120/120 — no lost update  [PASS]

LOCKRACE: ALL 4 CASES PASS
```

## 6. idcollide.sh — the pre-blessing leak reviewer E demonstrated is closed
```
$ # GRAND now contains ONLY tokens that actually exist at baseline:
34:GRAND="DEC DECOMPOSE|DEC DECOMPOSES|TOP HARDSTOP|TOP TOP-LEVEL"
$ oracles/idcollide.sh <tree-with-TOPGATE-added> TOP
COLLISION       : TOP  is a substring of  TOPGATE
EXIT=1        <-- pass 2 printed "grandfathered ... exit 0" for this exact input
$ oracles/idcollide.sh . KIL ING     -> 3 COLLISIONs, EXIT=1
$ oracles/idcollide.sh . BIND IDN RES CTX CNC DEP IGM PRV XPM HG2 SPV OFL -> OK, EXIT=0
```

## 8. The sweep row set is GENERATED, so totality is by construction
```
$ oracles/gen-sweep-rows.sh > oracles/sweep-rows.generated.md

TOTAL ROWS REQUIRED: 68
```
**68 rows are required** — 21 baseline rule IDs + 42 new criteria + **5 predicates that carry no rule
ID** (`closed-input-set`, `stage-done-iff-output-exists`, `path-validation`, `catalog-pending`,
`run-end`). Those five are the residual pass 2's reviewers named: an ID-driven generator cannot see
them, so they are listed explicitly in the generator rather than left to diligence. Pass 2's table had
26 rows and was missing 15 baseline IDs; three live defects were hiding in that remainder.

---

# APPENDIX A — pass 3, second half (the four document edits + the sweep). Built, run, THEN described.

Captured 2026-07-25. Same rule as above: nothing here describes a thing that does not exist, and every
claim is a **past-tense** report of a command whose output is pasted.

## A1. `ruleid-sitemap.sh` — the ONE unconfirmed item from the first half, and it was a REAL DEFECT

The first half's header comment said phantom hits are *"excluded and **REPORTED**, never silently dropped."*
The orchestrator's independent run produced **0** such lines and flagged it unconfirmed. **It was false as
shipped.** Cause, located:

```
$ grep -n '2>/dev/null' oracles/ruleid-sitemap.sh
19:    done < <(grep -now -- "$id" "$f" 2>/dev/null)
24:  got=$(measure "$id" 2>/dev/null)      <-- the caller DISCARDED measure()'s stderr,
                                              which is where the phantom lines were written
```

`gen-expected-sites.sh` reported its 2 exclusions correctly; **the checker suppressed its own.** Fixed with
a **phantom ledger** written to a temp file and printed unconditionally, then re-mutation-tested:

```
$ oracles/ruleid-sitemap.sh .
PHANTOM LEDGER: 2 excluded hit(s) — listed, never silently dropped:
  PHANTOM-EXCLUDED TOP examples/authoring-a-skill/planning.md:25 [ON TOP OF]
  PHANTOM-EXCLUDED TOP METHODOLOGY.md:79 [ON TOP OF]
SITEMAP: OK — every expected site present, no unexpected site (expectation: expected-sites.txt)
EXIT=0
$ # MUTATION erosion: sed 's/GBP//g' on a COPY of stages/stage-5-gate.md
MISSING    GBP stages/stage-5-gate.md
PHANTOM LEDGER: 2 excluded hit(s) …
SITEMAP: FAIL — site set eroded or drifted        EXIT=1
$ # MUTATION drift: stray GBP appended to templates/seed/leaf-task-spec.md
UNEXPECTED GBP templates/seed/leaf-task-spec.md
SITEMAP: FAIL — site set eroded or drifted        EXIT=1
```

**The ledger also self-reports the empty case** (`"(none — if this is 0 the exclusion path did not fire;
that is a REPORT, not a pass)"`), so the same silent-zero cannot recur unnoticed.

## A2. `checklib.py` — two additions, both mutation-tested

**(i) VACUOUS-SITE GUARD.** A `NEW`/`COOC` row whose SITE_PATTERN measures **zero** sites asserts nothing
and now **FAILS**. This is the `S-CTX-VAC` insight (*"path-validation over an absent key checks zero paths
and passes trivially"*) turned on the checker itself.

**(ii) `--sites` MODE.** Emits `ID · KIND · count · measured file set`. This is what makes the SITES column
**generated** instead of typed — reviewer F/1's blocker.

```
$ python3 oracles/checklib.py . oracles/criteria.tsv --sites | head -4
S-BIND       NEW   3   ch s3 s4
S-BIND-DISP  NEW   3   ch s3 s4
S-BIND-ONE   NEW   3   ch s3 s4
S-BIND-EXIT  NEW   4   S s5 s7 s8
```

**Anchor tuning was done against the measurement, not guessed** — `oracles/ere-probe.py` was written for
exactly that, and **where `0-baseline.B7-measured-sites.md` measured the same class the FILE SET matches B7
exactly**: PRV 4 files · XPM 9 · HG2-ONLY 7 hits/4 files (identical to B7's P12) · `index.md` 11 hits/5
files (identical to B7's P9a) · gate log 18 hits/9 files (identical to B7's P9b) · catalog 3 files · SPN 7
hits/6 files (identical to B7's P18) · SPV 3 hits/1 file · CTX 6 hits/4 files · OFL 13 hits/5 files ·
IGM 4 hits/3 files · `required_sections` 15 hits/7 files (identical to B7's P14).

**One anchor was WRONG on the first try and the probe caught it:** `PROVEN` compiled with `re.I` matches
*"proven"* inside **"provenance"**, inflating PRV's site set from 4 files to 9. Word-bounded to
`\bproven\b|completeness proven|proven, not asserted|prov(es|ing) that completeness`, which measures
**8 hits in 4 files — exactly B7's P6.** This is the concrete realisation of the risk B/L16 raised in pass 1.

## A3. The whole criteria family, re-run after the rewrite

```
$ oracles/baseline-replay.sh
BASELINE REPLAY @ b08f5a9  (changes/ removed from the tree, so the corpus pin is proven not assumed)
  NEW+COOC rows failing at baseline : 60   <- must be ALL of them
  PRESERVE rows passing at baseline : 8   <- must be ALL of them
  NEW+COOC rows WRONGLY passing     : none
  PRESERVE rows WRONGLY failing     : none
REPLAY: OK — every assertion discriminates          EXIT=0
```

```
$ oracles/gen-criteria-table.sh <tree-root>
INFO S-HG2-NOSELF (COOC) measures 0 sites — EXPECTED before the build; check.sh still FAILS the row
     there, which is what makes it discriminate
ROWS EMITTED: 68 — SITES columns produced from measurement, not typed.
CRITERIA-TABLE: OK — every NEW obligation row has a measured anchor in this tree      EXIT=0

$ # MUTATION: give a NEW row an anchor that matches nothing
VACUOUS-NEW S-SPV measures 0 sites — an obligation row with no anchor in the OLD text  EXIT=1
```

```
$ oracles/idcollide.sh . BIND IDN RES CTX CNC DEP IGM PRV XPM HG2 SPV OFL
IDCOLLIDE: OK (12 ids vs 62 corpus tokens)                                             EXIT=0
$ oracles/idcollide.sh <tree-with-TOPGATE-planted> TOP
COLLISION       : TOP  is a substring of  TOPGATE                                      EXIT=1
$ oracles/idcollide.sh . KIL ING          -> 3 COLLISIONs                              EXIT=1

$ oracles/lockrace.sh
CASE 1 winners=1 · CASE 2 SIGKILLed holder's stale lock broken, next run ACQUIRED [PASS] ·
CASE 3 refused to break a live holder [PASS] · CASE 4 unguarded 45/120 LOST UPDATES [PASS],
guarded 120/120 [PASS]
LOCKRACE: ALL 4 CASES PASS                                                             EXIT=0
```

## A4. Item 12 — the description budget, MEASURED ON A CANDIDATE (the artifact is UNEDITED)

**Read this line before the numbers below.** The **shipped** `SKILL.md` description is still **954**
chars. **997 is CANDIDATE text validated out of tree** — written into a scratch copy of the skill
directory and validated there. `git diff` vs `b08f5a9` over
`SKILL.md`/`METHODOLOGY.md`/`README.md`/`stages/`/`templates/`/`examples/` is **empty**. Nothing in this
section is a completed edit; the edit is **PENDING BUILD**.

```
current  : 954
candidate: 997 cap 1024, slack 27
  trigger 'plan': PRESENT | 'design': PRESENT | 'decompos': PRESENT | 'Proactively SUGGEST': PRESENT
  'PROVEN' present: False | 'top-level split ONLY' present: False | angle brackets: False

$ quick_validate.py <scratch>/architect        # the candidate description
Skill is valid!    EXIT=0
$ quick_validate.py <the unedited artifact>    # for comparison
Skill is valid!    EXIT=0
```

The two edits are: *"completeness is **PROVEN**, not asserted"* → *"completeness has been **gated**, not
asserted"*, and *"a human gate on the top-level split **ONLY** and"* → *"a human gate on the top-level
split, **a second human gate on the finished plan**, and"*. **Residual stated: 27 chars of slack is thin and
no criterion enforces a reserve.**

## A5. Item 7 — the cluster map, reconciled from the directory listing

```
$ ls -d X*/                  -> X1/ X2/ X3/ X4/      (4 clusters, not 3)
$ ls -d X*/*/ | wc -l        -> 8                    (8 arms)
$ echo $((8 * 2))            -> 16                   (2 spawns per arm)
$ grep -n 'Pass status' X3/holed/tree/root/completeness/*.md   -> (no output; E/16 give-away deleted)
$ diff -rq X4/holed X4/intact
Files X4/holed/config/planning.md and X4/intact/config/planning.md differ
Files X4/holed/tree/root/plan.md and X4/intact/tree/root/plan.md differ
Only in X4/intact/tree/root: plan.md.ingested          # E/15: holed now HAS a plan tree
```

**The pass RULE was fixed, not X3's fixture** — reason recorded in `fixtures/README.md`.

## A6. The sweep — generated row set, ENFORCED join, generated tally

```
$ oracles/gen-sweep-rows.sh | tail -1
TOTAL ROWS REQUIRED: 86        # 21 baseline IDs + 60 NEW/COOC criteria + 5 predicates with NO rule ID

$ oracles/gen-sweep-table.sh | tail -6
VERDICT TALLY: DECLARED GAP       3
VERDICT TALLY: FIXED              64
VERDICT TALLY: OK                 15
VERDICT TALLY: PARTIAL+DECLARED   4
ROWS EMITTED: 86   (generated row set: 86; authored answers: 86)
SWEEP: OK — the row set is generated and every generated row is answered      EXIT=0

$ # MUTATION 1: delete the closed-input-set answer (the omission that hid 3 of pass 2's 9 blockers)
SWEEP: FAIL — generated rows with NO authored answer: closed-input-set                 EXIT=1
$ # MUTATION 2: an authored answer for a predicate that no longer exists
SWEEP: FAIL — authored answers with NO generated row: S-GHOST                          EXIT=1
```

**An error of pass 3's own, owned:** the first draft of `2-plan.md` §1.1 **typed** the verdict counts as
55/20/4/4. Measured, they are **64/15/4/3**. The tally is now emitted by the generator, because *a tally
that is typed is a tally that drifts* — the same defect class as a typed SITES column, found in this pass's
own prose.

## A7. What is still PENDING BUILD, stated plainly

**No artifact file has been edited in any pass.** `git diff` over
`SKILL.md`/`METHODOLOGY.md`/`README.md`/`stages/`/`templates/`/`examples/` is **empty**; HEAD is `b08f5a9`.
Therefore: the 60 NEW/COOC criteria are all currently **FAILING by design** (that is the replay result), the
**16 fixture arms have not been run** (they are stage-8 work), and SC4's live-copy re-sync has not happened.
Every one of those is labelled **PENDING BUILD** in `1.5-criteria.md` and **none** is reported as verified.

## A8. `freeze-verify.sh` — built and mutation-tested, because stage 8 must prove the criteria did not drift

Gate 4 must *"freeze `1.5-criteria.md` and record its sha256"*, and stage 8 must show that the criteria it
verified against are the criteria that were reviewed. **A frozen file whose hash is only asserted is not
frozen**, so this is a script. The frozen set deliberately includes **the data the criteria are made of** —
`criteria.tsv`, `preserve-counts.txt`, `expected-sites.txt`, `sweep-answers.tsv`, `checklib.py`,
`lib-corpus.sh` — because moving a criterion's goalposts by editing the data file it reads is the obvious
evasion, and a hash over the prose alone would not see it.

```
$ oracles/freeze-verify.sh freeze     # mechanism proof only
FROZEN 9 files -> oracles/FROZEN.sha256
$ oracles/freeze-verify.sh verify
FREEZE-VERIFY: OK — all 9 frozen files match the gate-4 manifest              EXIT=0
$ # MUTATION 1: append one comment line to criteria.tsv
…/oracles/criteria.tsv: FAILED
FREEZE-VERIFY: FAIL — a criterion changed after the gate-4 freeze (criteria drift)   EXIT=1
$ # restored
FREEZE-VERIFY: OK — all 9 frozen files match the gate-4 manifest              EXIT=0
$ # MUTATION 2: manifest absent
FREEZE-VERIFY: FATAL no manifest — gate 4 never froze the criteria            EXIT=2
```

**The dry-run manifest was then DELETED.** Its header line says *"FROZEN at gate 4"*, and gate 4 has not
happened; leaving it on disk would be a small instance of exactly the defect this pass exists to remove — an
artifact asserting a step that has not occurred. **The real freeze happens at gate 4, on route-to-build.**
*Known cosmetic property, recorded rather than left to be discovered: the manifest stores absolute paths, so
it is not portable across checkouts.*
