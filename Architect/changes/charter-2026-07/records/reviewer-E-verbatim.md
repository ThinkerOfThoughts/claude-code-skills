## Context files read + sha256

All paths relative to `<wd>` = `/home/zero/Desktop/claude-code-skills/.claude/worktrees/recursing-visvesvaraya-b40a0c` unless absolute.

| File | sha256 |
|---|---|
| `Architect/changes/charter-2026-07/0-baseline.md` | `b22b251bdc1d320680a6abe38cc8454122bddefc24465b6fa888d16646f24dee` |
| `Architect/changes/charter-2026-07/1-spec.md` | `e45da3352220dd3729808e1cee20515f8cefeb55f1e49ab4f0bfcdd60627fe5f` |
| `Architect/changes/charter-2026-07/1.5-criteria.md` | `d070b714ee0322c850f5309716bbce11de9225f8578872e3b5d61468c3e2120f` |
| `Architect/changes/charter-2026-07/2-plan.md` | `f01093d2a003405433966e3d2345ed0490e55dad4dad433b2f7e652b239c73ca` |
| `Architect/changes/charter-2026-07/decisions.md` | `34a0def184875510aa17452af010fae7eb2b4422c6eb8b67c59acdf7bf80e10c` |
| `/home/zero/Documents/Architect.md` | `986512f56876531b1e8596c89208dadefd8bf47517d725d56d5a0269c58bb78b` |
| `Guarded_change/stages/charter.md` | `0e73bacf3af87d8e852e3a9723deda8cdd54f102de54af3880456f7024adc590` |
| `Guarded_change/stages/stage-3.md` | `16dbb31b9ccd2ee83296bf24e0941e05556d3a545f55901e8f28e519b6554df3` |
| `Guarded_change/stages/stage-4.md` | `175061c27bb6980ff76c6358d403299d343c409b2952308ffea7485fff880675` |
| `Guarded_change/stages/stage-1.5.md` | `c6b22f7f43ca3f611cc3b4505202b5d834a1594c3a3254e1f35d0f00323a860b` |
| `Guarded_change/stages/stage-8.md` | `8160b9be9cc875eed958217cdc2611b5c38cea263c50a9ddf9757fe02e92fcd3` |
| `Architect/guarded-change.architect.md` | `42f289a51ed4e5f706d3b70bc8ca47a2e8dc16d2aba4f3f3ac76a02d0cb0429c` |
| `Dragonfly/stages/charter.md` | `c5d1e2068f35760e373517e243003800c264c64bf3d93d6d8ea6b951990af870` |
| `Architect/stages/charter.md` (UNVETTED DRAFT — banner present at L3–6, so I am holding the draft) | `6a1981f3fd4db5a496ed23928b90ca8b575c30d5afb3765f792e09d543a19212` |
| `Architect/ATTEMPT-2-STATE.md` | `1d27d50f2281051cfaa7fc46f603f70248c33fef05683e570504e8b6f4807269` |
| `/home/zero/architect-dogfood-2026-07-24/FINDINGS.md` | `94cb55e86b3fbb1dd9cdf76f3956dcc62ac7aae6f84831f29642f8123ddb13c8` |
| session transcript `…/45cb99a2-543d-4447-a3e3-2a38963b0775.jsonl` (1534 records) | `eb769686c4f19bb7fdf81a4a492c99848d0f52872c82b8fb56088c452ff26910` |

**Not read:** `Architect-Attempt-1/stages` (context item 12) — nothing in this pass turned on it. `3-redteam-plan.md` was deliberately **not** read: it is not in the closed set, and the carried-forward findings were taken from the prompt and `decisions.md` as the rule requires.

**Sources I used OUTSIDE the closed set — declared:**
- `Architect/README.md` (sha256 `cfd3ee9613ff6a999597655e223c8ef7fe59024df89e71c19988c916cf7e19a7`) — read to check whether OOS-1/OOS-5 are still true. They are not (F18).
- Git metadata: `git show 8d73e5d:Guarded_change/stages/charter.md | sha256sum`, `git cat-file -t d044654`, `git log --oneline -1 d044654`, `git show d044654 -- Architect/guarded-change.architect.md`.
- Directory listings: `ls Architect/stages/`, `ls -R Architect`, `ls Architect/changes/charter-2026-07/{fixtures,oracles,records}`.

---

## Lens 1 — Factual

**Verified clean (source evidence actually consulted):**
- Fork source is **103** lines; `git show 8d73e5d:…` sha256 matches the config's asserted `0e73bacf…adc590` and HEAD byte-for-byte. §0.1's correction is right.
- §0.2's pasted `grep -n -E "^[0-9]+\.|^- \*\*|^The reviewer is graded"` **reproduces exactly**, line-for-line, at HEAD.
- All seven pre-existing owner quotes (records 55, 1128, 1148, 1175, 1258, 1274, 1188) are **character-exact** at their indices. Record **1449**'s five items are character-exact.
- B-F04's citation correction is **correct**: record 51's decision #2 option (b) is *"a cold completeness-critic pass"*; record 55's *"option be should be done by three independent cold agents"* attaches there, and the general adversarial red-team's three agents come from item **6** of record 55 (*"an adversarial stage where three independent cold agents try to poke holes in the plan"*).
- C-01's Dragonfly attribution is correct: `Dragonfly/stages/charter.md` L8–12 names commit `3d6889b`, states **four** deliberate drops, and contains **no sha256**.
- `d044654` exists, is the config fix, and the config at HEAD is stage-scoped with the ~85% strike.
- `ls Architect/stages/` returns `charter.md` only; there is no `decisions.md` anywhere under `Architect/` outside the run folder. X7/D12's premise holds.

**Findings:** F1, F5, F7, F16, F17, F18, F19 below.

## Lens 2 — Logical

Findings F3, F6, F8, F11, F12 below. The sharpest is **F3**: `1.5-criteria.md` C-08 (L48) makes *"a silent unilateral demotion … a gate violation and the reviewer's routing stands"* a gating criterion, and C-12 (L53) makes it a gating criterion that `Union` **unilaterally prevents a finding from passing to `Severity` as blocker|major**. Both are shipped in the same document. Nothing reconciles them.

## Lens 3 — Missed opportunity

- **C-19's length criterion could have had a bar without inventing one.** The fork source is 103 lines and is the declared regression bar; "≤ N× the fork source, or state why" is a defensible comparative threshold that needs no cost measurement. Reported instead as an unanchored number. (nitpick, F21)
- **The provenance blockquote is spending the head position on auditor-facing metadata.** Block 1 (path, commit, sha256, carried/changed/dropped) serves the *fork auditor*; the reviewer that reads the charter on every spawn pays for it in the highest-attention position. Dragonfly's precedent is a stage file read by a runner; Architect's charter is a live prompt. The plan notes "kept compact" (§1.1 block 1) but does not consider the alternative of a short head-line pointer + the full blockquote at the foot, which would cost nothing under C-16a if C-16a were written as "the first block is provenance-bearing" rather than "the head block *is* the blockquote."
- **`forkdiff.sh` (C-02b) could have been made bidirectional at zero extra cost** and would then also serve as the C-03 DROP checker. See F1.

## Lens 4 — Unstated assumptions & risks

**Position lens — FIRES (confirmed).** The artifact is a prompt; `2-plan.md` §1.1 reorders ten blocks relative to the fork source, inserts three new ones (floor, RAT1/RAT2, callers), and moves two that did not themselves change (the conditional lenses leave the discipline-bullet list; B18 is displaced and restored). Findings **F8, F9** are position-lens findings. Note that the fork source's *own* worked example — "an added tail block displaces the old last element; a removal changes a neighbor's adjacency" (`Guarded_change/stages/charter.md` L84–86) — applies to **relocation** as removal+insertion, and `2-plan.md` §4 scopes its confound-recording rule to **deletions** only ("a deletion changing a neighbour's adjacency is recorded as a known confound"), while asserting for relocation that "the only variable is position." That assertion is false: relocating block 3 changes three adjacencies (the gap left behind, and both new neighbours). This is not fatal to B-5/B-6 — they do vary position, which pass 1 did not — but the confound is unrecorded because the rule that would record it does not cover relocation. (folded into F4)

**Concurrency lens — the author's revised stand-down is CHALLENGED and I find it holds, on the corrected premise.** `Architect.md` L26–33 states one-writer-per-`node_id`, written after the value exists, read only by a restart of that same node; L10 confirms leaves "operate in paralell within that slot", so the pass-1 premise was indeed false and the correction in §0.3's B17 note is right. For **this run's harness**, the 28 arm agents each own a distinct record path and `decisions.md` writes are serialised after joins (Part D). One gap the revised premise does not cover: `Memo_write` is called **twice per iteration** (L96, L110) and `Memo_read` at L37 — on a crash between L96 and L110 the memo holds `iter` from the previous iteration with a `plan` from this one. That is a single-writer replay concern, not a shared-state race, so ST1.5e still does not fire. **Stand-down upheld.**

Other unstated assumptions: F4 (sequential-reading model of the reviewer + probabilistic effect treated as binary), F13 (every plan under review has a citable "underlying source"), F10, F12.

## Lens 5 — Fidelity

**Loaded operational terms pinned:**

| Term | Owner mechanism (`~/Documents/Architect.md`) | Artifact implements | Verdict |
|---|---|---|---|
| "cold, independent reviewer" | `Spawn_redteam` ×3, L100–103; separately spawned | C-15 text assertion + separately-spawned arms | **pinned, OK** (audit surface still thin — dogfood F9, `FINDINGS.md` L106–110; D6+model-pinning is a partial answer) |
| "granularity floor" | L1–8: bounds three things incl. `Spawn_redteam`; "threaded down so a branch can override it" | C-06a covers branch-override + absent-floor | **pinned, OK** |
| "the two callers" | `Spawn_redteam` L24, `Divisible` L14 | S12/C-14 + arm B-7 | **pinned, OK** |
| **"human"** (in "the human tie-break", S14/D10) | **`Human_gate` only** — L16, division-only, `depth <= gate_depth` | D10 pins it to **RAT3**, a property of *this guarded-change run's* config, not of Architect | **PROXY — F2** |
| **"consumer"** (B14's spot-verify) | `Union` L20, signature `Union(vector<string> _issues)` | D11/S15 assigns the duty to `Union` | **PROXY — F2**: nothing routes the charter to `Union`; §1 says the charter is read by "every cold reviewer Architect dispatches", and `Union` is a merge agent, not a reviewer |
| **"verbatim"** (charter core given verbatim, B19/S17) | not in `Architect.md`; the word "charter" appears nowhere in its 115 lines | S17 says core-verbatim **and** D8 says a conditional lens is given only when its trigger fires | **CONTRADICTORY — F8** |
| **"closed set"** | B15 enumerates; `Architect.md` supplies no analogue | D3′ = "whatever review-context paths your caller supplies" | **PROXY — F6**: an enumeration replaced by a tautology |
| "un-run" (record missing an element ⇒ un-run) | no enforcer in `Architect.md`; L98–107 is `task = Severity(Union(...))` with no record check | S9/C-11 ship the consequence | **no substrate — F2** |

Fidelity is **not clean**. See F2, F6, F8, F14.

---

## Pass-1 fix audit — required section

| Pass-1 finding | Claimed fix | Does it close? |
|---|---|---|
| **BLOCKER**: C-17's "executed half" varied the fixture, not the position | arm **B-5** relocates the floor block | **Partially.** The arm now genuinely varies position — that half is closed. But the pass rule (n=2 + within-arm agreement) drops ST1.5d's explicit rate requirement and biases hard to false-negative (**F4**), and `2-plan.md` §4's claim that relocation makes "the only variable … position" is false (relocation = removal + insertion; three adjacencies change) with the confound-recording rule scoped to deletions only. **Residual: major.** |
| B19 missing from inventory (3/3) | B19 added + independent probe **C-02b** | **B19 add: closed** — I re-derived the inventory against all 103 fork-source lines and found no *further* missed rule (one arguable exclusion, F19). **C-02b: NOT closed** — specified in the wrong direction; the claim "had it existed in pass 1 it would have surfaced B19" is false (**F1**). **Residual: major.** |
| Mutation test tautological | Part C rebuilt on semantic + insertion mutants | **Closed on design.** C-M1(a)–(d) + C-M2 + C-M3 are the right shapes. **New defect introduced**: the probe-rebuild loop is unbounded and self-administered (**F11**) — the *same* defect (B-F08) fixed for fixtures, unfixed one layer down. |
| n=1 arms + unbounded self-administered rebuild | n=2, within-arm agreement, one-rebuild bound | **Rebuild bound: closed** for fixtures, **not** for probes (F11). **n=2: not closed** (F4). |
| **D3**: closed set granted `Divisible` an input its 2-arg signature can't supply (3/3) | **D3′** | **Relabelled, not closed.** The false grant is gone; the enumeration is replaced by "whatever review-context paths your caller supplies", which is true under any signature *because it asserts nothing* (**F6**). **Residual: major.** |
| C-18's advisory reason covered 1 of 3 sub-clauses (3/3) | split into C-18a gating / C-18b advisory | **Mostly closed.** The two mechanizable sub-clauses are now gating. C-18b's reason is defensible for a *mechanical* bar but ignores ST1.5a's second means (human-judged rubric) for the only unenforced half of X4 (**F20, minor**). |
| CHANGE class's "difference declared" checked nowhere | **C-03b** | **Closed in principle, defective in instance.** C-03b hard-types a CHANGE membership list that disagrees with the table it must read *and* with `2-plan.md` §1.3 — the exact hand-typed-vs-generated divergence C-02's fix closed one row above (**F5**). **Residual: major.** |
| B18's terminal position displaced (2/3) | D9 + arm **B-6** | **Closed on design**; inherits F4's power problem and F9's untested-neighbour problem. |
| `Divisible` caller had zero behavioral verification (3/3) | arm **B-7** | **Closed on content, not on aiming.** B-7 is fixture-varying against the shipped charter, so it cannot detect that the split reviewer is mis-aimed by block 9's position or by eight preceding blocks written about "the plan" (**F9**). |

## Coverage challenge (CH8) — required section

Behaviours this change could plausibly alter that **no criterion observes**:

1. **The reviewer cannot clear the factual lens on a non-computational plan → permanent re-run.** B01 gives read access to "the underlying source (code, data, prior docs)"; B12 (carried, C-10) says a clean factual verdict with **zero source citations** is "treated as an un-run review and re-run." The owner's own worked example is *"standing->walking->opening the door"* (record **1128**) and he explicitly said plans "could be used by me to do something entirely outside a computational context" (record **55** item 1). For such a plan there are no file:lines. Scenario: `Node` reviews a physical-process plan; all three reviewers file no factual issue and cite nothing; all three reviews are un-run; they re-run; there is **no backstop cap** (record 1258). **Impact: non-termination on the owner's own example.** No criterion, no charter provision, no OOS note. *(major)*
2. **Severity *calibration* is unobserved.** B-3 checks a severity is *present*; B-6 checks finding *count*. Nothing observes the *distribution*. `Severity()` (L22) passes blocker|major; six minors severed as majors keep `task` non-empty forever. Scenario: the charter's four aggression-licensing sections (completeness lens, "report it even if you're alone", tier-(iii) generative sweep, RAT1/RAT2) push a reviewer to sever conservatively-upward. **Impact: non-termination.** *(major)*
3. **`Divisible`'s internal red-team loop has no termination observation.** L14: `Divisible` "red-teams result (**looping until no major issues are found**)", called at L58, L83, L107. B-7 tests whether the split reviewer *catches* an orphaned remainder; nothing tests that it ever *stops*. A split reviewer that finds a seam major every round hangs `Node` before any human gate is reached at `depth > gate_depth`. **Impact: hang, no cap.** *(major)*
4. **Floor × completeness-tier-(iii) interaction.** C-06b forbids findings whose only remedy is decomposition below the floor; C-05 requires a generative sweep for sections *neither list names*. A generated missing section that sits below the floor is a direct conflict and no criterion says which rule wins. B-1 and B-2 are run on separate fixtures and never jointly. Scenario: the B-2 HOLED fixture's missing section is at-floor granularity → the reviewer suppresses it under the floor rule → **the founding failure recurs while every rule is obeyed.** *(major)*
5. **Composed-charter variants are untested.** Under D8 a reviewer whose position/concurrency triggers do not fire receives a charter with block 7 excised — a *different assembly* whose block ordering B-5/B-6 never measured. *(minor; see F8)*
6. **The provenance-record "un-run" consequence has no enforcer** in Architect's loop (L98–107). *(folded into F2)*

Explicitly checked and **no gap found**: the fork-fidelity regression class is covered by C-02+C-02b *modulo* F1; the ~85% exclusion is covered by C-09b+C-24; the draft-banner class by C-16a/b.

## Label audit (CH9/CH10) — required section

**Advisory criteria (2 of 26).**
- **C-19 (length)** — reason stated: no defensible numeric threshold pre-build, no baseline reviewer-cost measurement. **Legitimate** under ST1.5c; inventing a number would be the guessed metric the loop refuses. (A *comparative* bar was available — F21, nitpick.)
- **C-18b (no Layer-2 config field list)** — reason: element 3's contract does not exist, so a mechanical bar would measure against an unwritten sibling. **Partially a dodge.** The reason is sound for a *mechanical* bar but ST1.5a offers a second means — a human-judged rubric with a named judge and a written pass condition — which is available today. This is the **only** unenforced half of X4, and X4 traces to C1, the project's #1 named historical failure mode. **F20, minor.**

**Per gating criterion — which governed path I confirmed would be exercised:**
- **Text-property criteria** (C-01, C-02, C-02b, C-03, C-03b, C-04, C-06b, C-09b, C-13, C-16a, C-16b, C-20, C-21, C-22, C-24): governed path *is* the charter's text; `check.sh` positive per-site assertions on normalized text exercise it. **Confirmed adequate**, subject to F1 (C-02b's direction) and F5 (C-03b's membership list).
- **Behaviour-governing criteria with an executed half** (C-05→B-2, C-06a→B-1, C-07→B-3, C-09a→B-4, C-14→B-7, C-17→B-5, C-23→B-6): the arm exercises the governed path. **Confirmed**, subject to F4's power problem.
- **C-10 (earned-clean clauses, factual/fidelity/completeness) — FAILS the challenge.** Gating; the property it governs is *reviewer behaviour* (does a clean verdict get earned?); the planned verification is text presence in `check.sh` (`2-plan.md` §3 row 1). H5 (`stage-8.md` L42–53) is unconditional: a gating criterion "is satisfied only by running a case that exercises *the actual path it governs*", and names **"an inspection standing in for an execution"** as one of three dispositions that look like progress and are not satisfaction. The run applied exactly this reasoning to upgrade C-14, C-17 and C-23 and did not apply it here — and C-10 guards the single failure the whole loop exists to kill (fork source L46: rubber-stamping "is the guard against the reviewer reasoning from the artifact alone"). **F10, major.**
- **C-08, C-11, C-12** — same shape (gating, behaviour-governing, text-verified). C-08 and C-12 additionally have no actor (F2, F3). C-11 is the weakest of the three but is partially self-enforcing via the arms' own record duty.
- **C-M1–C-M4** — the mutation test governs the checker; its verification is executing the checker on mutants. **Confirmed adequate**, but the failure disposition is unbounded (F11).

## Ratification audit (CH11/CH12) — required section

**Spot-verified against the transcript myself (>2 as required):** record **1449** (all five items, character-exact), record **1175**, record **55**, record **1128**, record **1258**, record **1274**, record **1188**, and — to test the §9 claims about presented options — records **51**, **1124**, **1171**, and 1125–1127 (queue/system records, no owner turn).

| Record | Author's classification | My audit |
|---|---|---|
| **R-1** (1449 #1) | Ratified → D14, arm B-5 | **Correct.** The words *"if the experiment is to test where the granularity floor should sit, than the experiment should actually try moving the floor"* select option (a) and prescribe the fix. The escalation-standard elaboration ("a defect in measurement apparatus is fixed, not escalated") is a fair entailment of *"That wasn't a call that warranted my attention at all."* No inflation. |
| **R-2** (1449 #2) | Ratified; selects none of three, a fourth | **Correct on selection.** *"It gets implemented however it is implemented in guarded-change"* is directive and disambiguates. **CH12 inflation found:** D10's added claim that *"the human named is the same human the loop already stops for, reached through the delegated-runner halt path (RAT3) — the path this very ruling travelled"* is an operative commitment (a mechanism + a division of responsibility) **not present in and not entailed by** the owner's words, and it is **false about Architect**: RAT3 lives in `Architect/guarded-change.architect.md` L172–175 and governs *this build run*; Architect's runtime provides only `Human_gate` (L16), division-only, `depth <= gate_depth`. **F2, major.** |
| **R-3** (1449 #3) | "Ratified on placement", ⚠ RAT2 on disposition | **Self-classification is generous but defensible on placement** — *"That \*was\* part of what Combine did"* does locate the duty at the merge step. It under-reports that the operative half of the sentence, *"make up your mind"*, is a **return of the decision to the author**, not an arbitration. The RAT2 flag on the disposition is honest and correct. **But the flagged elaboration creates a contradiction with R-2's own port (F3), and a second, unflagged inflation rides with it: B14's "cheap: verify a few, not all" (fork source L63) becomes exhaustive in S15/C-12 (F17).** |
| **R-4** (1449 #4) | NOT a ratification; D12 is author/orchestrator | **Correct, and it is the less convenient classification** — claiming ratification would have been easier. `sed -n '1449p'` item 4 is *"I don't know what the fuck rat1/2 even ARE"*; RAT1/RAT2 are guarded-change internals (`stage-3.md` L55–82) and never owner vocabulary. **One false statement in the record:** §9 says *"The axis was **not defaulted into the runner's recommendation**."* The outcome **is** the runner's recommendation — option (a), "inline ~30 lines", adopted at D12 with "Cost: ~30 lines, accepted." Only the justification changed. **F15, minor.** |
| **R-5** (1449 #5) | Ratified → D13/X6 | **Correct.** *"That thing was for the old version, discard it"* selects out unambiguously. §0.6's withdrawal of F7 as support for the Union rule follows correctly. |

**Pre-existing rulings — one fails the audit.**

**Record 1175 / D1 (completeness as a distinct sixth lens).** `1-spec.md` §9 (L210–214) and `0-baseline.md` §0.5 D1 present records **1124** and **1171** as the presented options and conclude they *"**strengthen** D1's independence claim: the owner was twice shown the fold-it option and asked only that the *definition* survive. **D1 stands, and now shows its work.**"* I read all three records. The evidence runs the other way on substance:

- Record **1124** (assistant): *"needs trimming: its separate sixth Completeness lens folds into the single red-team mandate."* The owner's next turn is record **1128** — about granularity. **He never answered 1124 on this axis at all.**
- Record **1171** (assistant, the turn 1175 answers): *"Your single red-team's mandate already covers both passes' ground"* … *"Worth keeping from it: the discipline bullets … and the severity ranking … **The rest of the charter is trimmable.**"* The sixth Completeness lens is described earlier in that same message as part of that charter.
- Record **1175** (owner): *"regarding redteam: **Agreed for the most part**, but the new charter should **also** include the definition of three tiered completebess definition."*
- Record **55** item 4 (owner): *"Agreed, fork; **not sure what you mean by lens though**."*

So the only two owner touches on this axis are a non-comprehension of the term "lens" and a **partial assent to the trim proposal**, with a single carve-out for the *definition*. The word "also" attaches the definition to the keep-list, not to a lens. Characterising the same records as *strengthening* D1 is the convenient resolution RAT1 exists to catch. The *narrow* claim — that the placement is unratified and therefore author-owned — survives; the *offered support* does not, and RAT1's remedy for a partial answer on a flagged axis is **re-ask, not default**, which was not done. **F14, major.**

Records **55, 1128, 1148, 1258, 1274** as relied on: **audited, valid** — verbatim confirmed, mapping confirmed, no inflation found. §9's B-F04 correction to record 55's attachment is itself correct (verified at record 51's decision #2 and record 55 item 6).

## Unverifiable claims I could not check

1. **Whether `check.sh`'s assertion class can kill C-M1(d) hedge mutants at all.** No probe patterns exist yet (`oracles/` is empty — verified by `ls`). A normalized substring probe for an asserted phrase cannot distinguish "X" from "X, where practical." Whether this is soluble depends on unwritten regex construction. Flagged rather than asserted; it is the load-bearing assumption behind F11.
2. **Whether the two "different models" in the model-pinning rule are available and behave differently enough** for a HOLED/INTACT difference to be attributable. Not checkable pre-build.
3. **Whether `Union` is given the charter.** `~/Documents/Architect.md` L20 says only "cold agent"; the spec says the charter is read by "every cold reviewer Architect dispatches." `Union` is a cold agent but not a reviewer. The spec does not resolve it either way; I report it as an unresolved routing question inside F2 rather than as a settled defect.
4. **Whether the pass-1 reviewer findings I was handed (A-F*, B-F*, C-*) are faithfully summarised.** I did not read `3-redteam-plan.md` (out of set), so I audited each *fix* against source rather than against the original finding text.

## Findings table

| # | severity | lens | artifact:line | finding | why it matters |
|---|---|---|---|---|---|
| **F1** | major | factual / logical | `1.5-criteria.md`:40; `2-plan.md`:68–74 | `forkdiff.sh` is specified to diff shipped-vs-fork, "strike out every matched rule span, and **list the residue**", where "every residue line is either **an Architect addition** … or a finding" — i.e. **shipped-side residue only**. B19's failure class is a **fork-source** rule missing from the inventory; a shipped-side residue listing cannot surface it. The claim "Had it existed in pass 1 it would have surfaced B19" is false. Under the alternative bidirectional reading the spec has no disposition for the one declared DROP (B15's A/B clause would be listed as "a finding"). | C-02's probe set comes from the inventory (A-F3's blindness); C-02b was the *only* independent instrument. A fork-source rule absent from both the inventory and the charter is invisible to both — exactly B19's shape. The run's regression bar (`1.5-criteria.md` Part D: "the regression arm is the fork-fidelity pair C-02 + C-02b") retains the construction blind spot it was rebuilt to remove. |
| **F2** | major | fidelity | `0-baseline.md`:265 (D10); `1-spec.md`:140–142 (S14/S16); `1.5-criteria.md`:48,53,52 | **Five ported/added charter rules have no substrate in `~/Documents/Architect.md`.** (i) "demoting a blocker or major requires the **human tie-break**" — the only human mechanism is `Human_gate` (L16), which takes `(pair<string> _division, string _task, int _depth)`, fires only at `depth <= gate_depth`, and adjudicates **divisions**, not severities. D10's claim that this human is "reached through the delegated-runner halt path (RAT3)" cites `Architect/guarded-change.architect.md` L172–175 — **this build run's** delegation, not Architect's runtime. (ii) "contest a severity only via a logged **`decisions.md`** entry" — Architect has no such file (`ls -R Architect` confirms; `Memo_write` L33 persists only `{done, iter, task, plan, division}`). (iii) "a record missing any element ⇒ **un-run**" — L98–107 has no enforcer. (iv) RAT1's "the axis is **re-asked**, not defaulted" — no re-ask path exists. (v) `Union` performs the spot-verify — nothing routes the charter to `Union`. | The spec's own standing rule (`1-spec.md` §4): "Disagreement with [`~/Documents/Architect.md`] is a defect in the artifact, not in it." Pass-1 finding A-F4/B-F11 (2/3) was *"no mechanism exists to reach the human"*; D10 declares it **resolved** on a mechanism from the wrong layer, so the finding is **moved, not removed** → stands at its original severity. C-22 sweeps only `stages/*.md` pointers, so the whole class recurs one level out — the under-generalisation D5 names. |
| **F3** | major | logical | `1.5-criteria.md`:48 (C-08) vs :53 (C-12); `1-spec.md`:140 vs 141 | The shipped charter will state, as two gating rules, that (a) "a **silent unilateral demotion** is a gate violation and the reviewer's routing stands" and (b) `Union` marks an unresolvable-citation finding unsubstantiated so it "**does not pass to `Severity` as blocker\|major**". (b) *is* a silent unilateral demotion of a blocker\|major with no human tie-break. Neither document reconciles them; no carve-out is drafted. | Both are owner-touched (R-2 ratified, R-3 ratified-on-placement). A reviewer handed two contradictory operative rules resolves it arbitrarily; a `Union` that obeys (a) never filters bad citations, a `Union` that obeys (b) commits the gate violation (a) defines. This is a *new* defect created by the pass-1 fixes, not a carried one. |
| **F4** | major | unstated assumptions | `1.5-criteria.md`:75–80, 88–91; `2-plan.md`:129–133, 144–146 | Part B cites **ST1.5d** and implements half of it. `stage-1.5.md` L72–75: "Where the effect is probabilistic (recency/precedence usually shift a *rate*, not flip a switch), the criterion **states the pass rate it expects and the number of runs that establishes it** … rather than relying on a single probe." The criteria implement "not a single probe" (n=2) and **drop the rate requirement entirely**, then impose within-arm agreement as the pass rule. For a genuine effect shifting the finding rate from 0.1→0.6, P(both HOLED agree ∧ both INTACT agree ∧ arms differ) ≈ **0.29** — a ~71% false-negative rate on a real effect. Separately, `2-plan.md` §4's "block **relocation**, not deletion — so no text is lost and **the only variable is position**" is false: relocation is a removal plus an insertion and changes three adjacencies, and §4's confound-recording rule covers **deletions** only. | B-5, B-6, B-7 are the executed halves of three gating criteria; `verified = no` blocks "done" (H5/H7). The most likely terminal state of the plan as written is a false-negative sweep → one rebuild each → surfaced to the human. And a bounce on C-17 is a **second** bounce on class {gate 4 · `1.5-criteria.md` position criterion} (`decisions.md` L226), which trips the cap. The plan's answer to pass 1's blocker is under-powered against its own cited rule. |
| **F5** | major | factual | `1.5-criteria.md`:42 (C-03b) vs `0-baseline.md`:137,144,150,154 vs `2-plan.md`:49–55 | **Three documents state three different CHANGE sets.** `0-baseline.md` §0.3: CHANGE = {B02, B09}; B15 = "CARRY (with one declared DROP)"; **B19 = CARRY**. `1.5-criteria.md` C-03b: "per CHANGE rule (B02 …; B09 …; **B19** restated per-caller)". `2-plan.md` §1.3: changes to declare = five→six lenses (B02), severity in-file (B09), **closed set restated per-caller** (B15/D3′). C-03b hard-types its membership list instead of reading it, which is precisely the defect C-02 was fixed for one row above ("The count is read from `0-baseline.md` §0.3, **never restated here**"). Compounding: the CARRY/CHANGE boundary is undefined — B14 is CARRY "plus a named consumer", which adds an operative duty (D11) and is a difference a reader of the shipped charter will never see declared. | C-03b exists to fix A-F8, the finding that "the CHANGE class's 'difference declared' half was checked nowhere." As written it checks a hand-typed list that disagrees with its own source table, so a CHANGE the table records (B15) goes undeclared and a CARRY (B19) is required to be declared as a change. Same C5 hand-typed-vs-generated divergence, one criterion over — the under-generalisation D5 names. |
| **F6** | major | logical / fidelity | `0-baseline.md`:253 (D3′); `1.5-criteria.md`:52 (C-11) | D3′ restates the closed set as *"the task you were given, the plan you were given, the granularity floor you were given, plus **whatever review-context paths your caller supplies**."* The author's defence — "true under any signature and pre-empts no owner decision" — is exactly the problem: a rule true under any signature **bounds nothing**. The fork source's B15 (L74–77) *enumerates*: "the named stage artifacts + the config's `redteam_context` + the spec's touched-files list + carried-forward findings." C-11 verifies "the closed-set rule stated conditionally and per-caller" by text presence, so a tautology passes it. | The closed set is the only rule that makes an author-injected context leak detectable; the fork source treats a leak as a confound serious enough to prohibit outright in harness arms (L77–79). Pass-1's 3/3 finding was that D3 granted an input the signature can't supply; D3′ removes the false grant and replaces the enumeration with an unbounded one. Defect **relabelled**, so it stands at the original severity. |
| **F7** | major | factual | `2-plan.md`:162–166 | §5 states each reviewer receives "the priority-ordered `redteam_context` paths — **now including the Layer-2 config and the guarded-change stage files**". The config at HEAD (sha256 `42f289a5…0429c`) lists **eight** paths; neither `Architect/guarded-change.architect.md` nor any `Guarded_change/stages/*.md` is among them. `git show d044654 -- Architect/guarded-change.architect.md` touches only entry 4's note and the ~85% strike — **no paths were added**. The closed set is being widened outside the config, undeclared, and `1-spec.md` §8 puts the config on the **explicitly-NOT-touched** list, so this run cannot legally amend it. | This is live right now: my own closed set contains five files not in `redteam_context`. B15 requires that "any supplementary author-authored context must be **quoted in the record as such**" — no such declaration exists, so a later auditor cannot separate legitimate context from injected context. The run is violating, in its own dispatch, the gating rule (C-11) it is about to ship. |
| **F8** | major | logical / fidelity / position | `0-baseline.md`:263 (D8); `1-spec.md`:7–8, 143 (S17); `2-plan.md`:29 (block 7) | D8 states "a conditional lens is stated in the charter with its trigger, and is *given* to a reviewer **only when that trigger fires**." `~/Documents/Architect.md` defines **no prompt-composition step**: `Spawn_redteam(_task, _plan, _granularity)` (L24) spawns, and nothing assembles. Meanwhile `1-spec.md` §1 says the charter is "read **verbatim** by every cold reviewer Architect dispatches", and C-16a/C-13 put both conditional lenses in the file. Either the whole file is given (D8 is inert and unimplementable) or something composes (Architect has no such actor). No criterion observes it. | D8 is the fix for **B19**, the pass's highest-convergence finding (3/3). The fix adds the rule and leaves it without an executor, and creates a second-order position problem: an excised block 7 shifts every following block, producing a *composed* assembly whose ordering B-5/B-6 never measured. Defect **moved**, so it stands at B19's severity. |
| **F9** | major | unstated assumptions (position lens) | `2-plan.md`:31 (block 9), 34–38 | Block 9 — "the two callers" + the definition of "3 independent cold agents" — is placed **ninth of ten**, and §1.1's closing paragraph declares that "the other blocks' positions are ordinary editorial judgement." The rationale used to justify block 3's position ("a reviewer that meets the lenses first has already formed a view on vagueness before being told the bound") applies with **more** force here: the reviewer learns *which caller it is and what artifact it is reviewing* only after eight blocks. For the `Divisible` split reviewer this is acute — blocks 3–8 are all written about "the plan", while that caller reviews a **division** (`Architect.md` L14). B-7 is fixture-varying against the shipped charter (§4), so it cannot detect mis-aiming by position. | The config states plainly that "a charter that only aims the first caller is **incomplete**" (`guarded-change.architect.md` L163–166), and the second caller is `Divisible`, whose sub-loop runs "until no major issues are found" (L14) with no cap. Declaring block 9's position editorial while testing blocks 3 and 10 is internally inconsistent by the plan's own criterion for which positions carry a behavioural claim. |
| **F10** | major | CH9/CH10 (label audit) | `1.5-criteria.md`:51 (C-10); `2-plan.md`:108 | C-10 is **gating**, its property is reviewer behaviour ("a clean verdict is *earned*"), and its entire planned verification is a text assertion in `check.sh` (`2-plan.md` §3 row 1 lists C-10 in the `check.sh` block with no arm). `stage-8.md` H5 (L42–52) is unconditional for gating criteria and names "an inspection standing in for an execution" as one of three non-satisfactions. The run applied exactly this reasoning to upgrade C-14, C-17 and C-23 and did not apply it to C-10. | The earned-clean clause is the guard against reviewer rubber-stamping — the fork source calls it "the guard against the reviewer reasoning from the artifact alone and rubber-stamping it (**the failure this whole loop targets**)" (L46–48). It is the one criterion whose failure mode is invisible from the text, and it is the one behaviour-governing criterion left without an executed half. |
| **F11** | major | CH9 / logical | `1.5-criteria.md`:136 (C-M1); `2-plan.md`:96, 101–102, 196 | The **fixture** rebuild is bounded to one, logged, then surfaced to the human (Part B) — the fix for B-F08. The **probe** rebuild is not: "A surviving mutant means the probe matches a phrase, not a rule — **the probe is rebuilt**" / "rebuilt in place (logged, H6)" / "A survived mutant is a checker defect (rebuild in place, log)". No bound, no ceiling, same self-administering runner, and the rebuild is the *only* escape from a failing gating criterion (C-M1 is gating and gates every Part-A result). | B-F08's finding was "an unbounded, cap-exempt, self-administered rebuild" — attempt-1 failure mode #6, "a gate satisfiable by the party it constrains" (`ATTEMPT-2-STATE.md` §8). The fix was applied at the fixture layer and not at the probe layer, where the same actor, the same incentive and the same escape hatch exist. Defect **moved, not removed**. |
| **F12** | major | CH8 | `1.5-criteria.md` Part B (no criterion); `Architect.md`:14, 22, 106 | No criterion observes (a) **severity calibration** — B-3 checks presence, B-6 checks count, nothing checks level, and `Severity()` passing systematically-inflated majors means `task` never empties with no backstop cap (record 1258); or (b) **`Divisible`'s internal loop terminating** — L14 "looping until no major issues are found", called at L58/L83/L107, with B-7 testing only whether the split reviewer *catches* a gap. | Both are non-termination paths in a loop the spec itself (`1-spec.md` §2.1) says the charter alone terminates. The charter adds four aggression-licensing sections while its only counterweight (B18) is tested for *count*, not *severity*. Unmeasured blast radius on the run's single most load-bearing property. |
| **F13** | major | CH8 / fidelity | `1.5-criteria.md` (no criterion); fork source L42–46 (carried as C-10); transcript records **55** item 1, **1128** | The carried B01/B12 pair assumes every plan under review has a citable "underlying source (code, data, prior docs)" and treats a clean factual verdict with zero source citations as **un-run and re-run**. The owner explicitly scoped Architect to plans that "could be used by me to do something entirely outside a computational context" (record 55 item 1) and gave *standing→walking→opening the door* as his own worked example (record 1128). For such a plan the factual lens is unclearable; with no backstop cap, re-run is unbounded. No criterion, no charter provision, not even an OOS note. | The single most consequential unmeasured behaviour: a carried rule interacts with an owner-stated scope to produce non-termination on the owner's own example. It is also the one place where a *carried* fork-source rule genuinely needed re-aiming for Architect (as B03/B04/B16/B17 were) and was carried unexamined. |
| **F14** | major | fidelity (CH11) | `1-spec.md`:210–214; `0-baseline.md`:250 (D1); records 1124, 1171, 1175, 55 | §9 presents records 1124/1171 as *strengthening* D1. Read at their indices, they run the other way on substance: 1124's fold proposal drew **no owner response** on that axis (the next owner turn is 1128, about granularity); 1171 — the turn 1175 answers — argues "your single red-team's mandate already covers both passes' ground" and "**the rest of the charter is trimmable**", keeping only the discipline bullets and severity ranking; and 1175 replies *"**Agreed for the most part**, but the new charter should **also** include the **definition** of three tiered completebess definition."* At record 55 item 4 the owner had already said *"not sure what you mean by lens though."* The only owner assent on record leans toward the **fold**, with a carve-out for the *definition*, not the lens. | RAT1's operative rule: a **partial** answer on a flagged axis is "not ratified" and "the axis must be **re-asked, not defaulted**." D1 defaults to a distinct sixth lens against the drift of the owner's only words, and the artifacts characterise the adverse records as supporting it. B02 is a CHANGE to a carried rule (five→six lenses) resting on this; and the run's own §9 preamble says "a run that states a discipline it does not practise on its own rulings has not earned it." |
| **F15** | minor | fidelity (CH11) | `1-spec.md`:200 (R-4); `0-baseline.md`:267 (D12) | R-4 states "The axis was **not defaulted into the runner's recommendation**; it was reclassified as research owed." The outcome **is** the runner's recommendation — §9's own options list gives "(a) inline ~30 lines" and D12 adopts exactly that ("Cost: ~30 lines, accepted"). Only the justification changed. | The R-4 classification (non-answer, author decision) is correct and honestly the *less* convenient one; but this sentence is a false statement inside the run's own model ratification record, in a document that is about to inline RAT1 as a shipped duty. Fixable in place by deleting the clause. |
| **F16** | minor | factual | `0-baseline.md`:265; `1-spec.md`:140; `1.5-criteria.md`:48 | Two citation defects in the D10 port. (1) SEV2 is cited as `stage-4.md` **L26–28**; the rule runs L26–**29** — the cited range truncates mid-sentence ("a person / **rules.**"). (2) D10 says the rule is "ported from guarded-change **verbatim**, not re-derived", but S14 and C-08 state **paraphrases**, and C-08's checker will assert the paraphrase — so nothing gates the verbatim-ness the decision claims. Additionally, SEV3's head clause (L31–33, "the gate routes on the **reviewer's** stated severity") is not carried, only its demotion tail. | The owner's ruling was *"copy over the severity mechanism from guarded change"*; a port claimed verbatim and checked as a paraphrase can drift without any instrument noticing. Same "claim and oracle share one source" structure as F1, at the port layer — the run built C-02b to fix this for the fork source and did not extend it to the two other verbatim ports (`stage-4.md`, `stage-3.md`). |
| **F17** | minor | factual / fidelity (CH12) | `1-spec.md`:141 (S15); `1.5-criteria.md`:53 (C-12); fork source L59–64 | B14's source text says the consumer "checks a **sample** of the cited file:lines … (cheap: **verify a few, not all**)". S15/C-12 make it exhaustive: "a finding whose citation does not resolve **is** marked unsubstantiated." §0.3 records B14 as **CARRY** "plus a named consumer" — the sample→exhaustive shift is not declared anywhere, so C-03b will not require it in the provenance blockquote either. | An undeclared modification to a CARRY rule is the exact "silent third category" C2 forbids, and it is an unratified operative inflation on top of R-3, whose ratification covered *placement* only. |
| **F18** | minor | factual | `0-baseline.md`:294–299, 311, 347–349; `decisions.md`:90, 153 | Three stale claims, all machine-checkable. `Architect/README.md` L31–32 and `Architect/ATTEMPT-2-STATE.md` L87–96 **no longer state** the ~85% figure as fact — both carry visible 2026-07-28 corrections. So OOS-1 ("states the unsourced '~85%' figure as settled design fact (L87–88)"), OOS-5 ("`README.md` L31 states the unsourced ~85% singleton rate") and §0.6's "`README.md` L31 and `ATTEMPT-2-STATE.md` L87 **remain**" are all false at HEAD. §0.6's pasted `$ grep -rn "85%" …` block, presented as machine output, does not reproduce: the hits are now at `README.md:32`, `ATTEMPT-2-STATE.md:89` and `guarded-change.architect.md:87`, with different text. | §0.6 is the section whose pass-1 lesson was precisely that "it was the one number … that had not been machine-generated," and whose authority rests on its claims being mechanically checked. A stale pasted grep presented as current is a C4/C5-class defect recurring in the document that names the class. One re-run of the grep fixes it. |
| **F19** | minor | factual | `0-baseline.md`:75–78; fork source L3–6, L65–67 | Two arguable inventory gaps, given the run's own standard that "a rule this file states that B01–B19 still misses is a finding." (1) Fork source **L3–5** states an operative composition rule — "This is the **ONE copy** of the red-team charter's common core. Stage 3 and stage 6 both read it, **then add their stage-specific bullets**" — excluded as "plumbing" that "states no review rule". The run's own spec treats exactly this property as load-bearing (`1-spec.md` §1: "the **single copy** … read **verbatim** by every cold reviewer"), and B19 was missed for the same reason: an operative clause sitting inside a span classified by role. (2) B15's scope clause "wherever in the run it occurs (stage 3/6, a targeted post-6 check, **a harness-embedded reviewer arm**)" (L65–67) is not carried into C-11's element list. | I re-derived the inventory against all 103 lines and found no *other* miss — B01–B19 is materially complete, which answers the author's ask #6 affirmatively. But the "every rule" claim is a checkable claim, and the exclusion rationale for L1–6 is the same rationale that produced M-01. |
| **F20** | minor | CH9 (label audit) | `1.5-criteria.md`:61 (C-18b) | C-18b's advisory reason ("element 3's config contract does not exist, so a mechanical bar would measure against an unwritten sibling") is legitimate against a *mechanical* bar but ignores ST1.5a's second means — "a named human judge, a written scale, and what counts as pass" — which is available today and is how `stage-1.5.md` L33–35 says a non-mechanizable criterion stays gating. | C-18b is the only unenforced half of X4, and X4 traces to C1, "the project's #1 named historical failure mode" (scope drift). The pass-1 finding was that a reason covering one sub-clause was stretched over three; the split fixed the stretch but left the surviving sub-clause advisory when a gating form was available. |
| **F21** | nitpick | missed opportunity | `1.5-criteria.md`:62 (C-19) | C-19 reports length with no bar because "no defensible numeric threshold exists pre-build." A *comparative* bar was available at zero cost — the fork source is 103 lines and is already the declared regression bar. | The plan itself argues length is load-bearing ("a prompt every reviewer pays for on every spawn", §1.1 block 1). A relative bar would make that argument checkable without inventing an absolute number. |

**Scope verdict (author's ask #7).** The growth 19 → 24 criteria + 7 arms is **the red-team's findings becoming the next task, not scope drift**: every new criterion (C-02b, C-03b, C-06a's additions, C-07's blocker cell, C-18 split, C-20, C-21, C-22, C-24) and every new arm (B-5, B-6, B-7) targets the charter or its verification, and none touches elements 2–6. C1 holds. What the charter **genuinely owes and did not get**: the non-computational-plan case (F13) — traceable to owner words at records 55 and 1128 and pushed out of scope by nobody, it was simply never noticed. The real scope risk is the opposite one: **34 cold agents (28 arms + 6 reviewers) to verify one ~200-line prompt** reproduces the attempt-1 pattern the config names in its own opening — "the *measurement apparatus* became the thing that kept failing" — and the plan's mitigation for R2 (declare a reduced pass → `verified = no`) has no path to "done", only a path to a halt.

## Worst severity

**major** — no blocker. Fourteen majors, six minors, one nitpick. The pass-1 **blocker** (C-17 verified by a non-varying arm) is genuinely closed on the property that made it a blocker: B-5 now moves the floor, as the owner prescribed at record 1449 item 1. What remains at major is a cluster with one shape — **five of the nine claimed pass-1 fixes moved their defect rather than removing it** (F1 for A-F3, F2 for A-F4/B-F11, F6 for the 3/3 D3 finding, F8 for B19, F11 for B-F08) — plus three genuinely new defects the fixes created (F3, F5, F7) and three coverage gaps the criteria set never reached (F12, F13, and F10's proxy).

**Routing note for the consumer.** F4 targets `1.5-criteria.md`'s position-criterion apparatus. `decisions.md` L226 records the bounce count at gate 4 on class {gate 4 · `1.5-criteria.md` position criterion} as **1**, and SEV4 defines the class as "same gate + same targeted artifact section, regardless of wording." If you route F4 backward it is plausibly a **second** bounce on that class and trips the cap for a human tie-break. I am reporting the finding at its honest severity and leaving the class call to you; F1, F2, F3, F5, F7, F13 are independent of that class and route on their own.
