Review complete. Below is my full report.

---

## Context files read + sha256

| Path | sha256 | Read |
|---|---|---|
| `Architect/changes/charter-2026-07/0-baseline.md` | `d6805c37a98a2f3c8d93efe4021012f109c62885a1cda93592fb464f97c81381` | all (256 L) |
| `Architect/changes/charter-2026-07/1-spec.md` | `f20ce0f8b396cc07b59da8e02087f713b96039c459886b70d7428ae90df8e74b` | all (182 L) |
| `Architect/changes/charter-2026-07/1.5-criteria.md` | `28b2beb63d3bb20f914ee6e99fd87d80e0f6506eb5c75a05ac75215e10bf6b8c` | all (128 L) |
| `Architect/changes/charter-2026-07/2-plan.md` | `da670965e49e78b6668d9d3a76f8006f98ea4c4247e4c2a4409f05a4632c19fa` | all (189 L) |
| `Architect/changes/charter-2026-07/decisions.md` | `a1e0749522a156ee3617f0b9b1dfd158949eba173c2cf9a13d0726e25a476034` | all (103 L) |
| `/home/zero/Documents/Architect.md` | `986512f56876531b1e8596c89208dadefd8bf47517d725d56d5a0269c58bb78b` | all (115 L) |
| `Guarded_change/stages/charter.md` | `0e73bacf3af87d8e852e3a9723deda8cdd54f102de54af3880456f7024adc590` | all (103 L) |
| `Dragonfly/stages/charter.md` | `c5d1e2068f35760e373517e243003800c264c64bf3d93d6d8ea6b951990af870` | part (L1–20) |
| `Architect/stages/charter.md` | `6a1981f3fd4db5a496ed23928b90ca8b575c30d5afb3765f792e09d543a19212` | all (163 L) |
| `Architect/ATTEMPT-2-STATE.md` | `540f5d3d6bc32e73eb47563a474c827795581c25087aff5b85508bc7338f37fb` | all (151 L) |
| `…/45cb99a2-543d-4447-a3e3-2a38963b0775.jsonl` | `e98fd7166879babd96a2bc0e5a93e9775936741b75ed7f514f7c0f7474197289` | part — records 55, 1128, 1148, 1170–1176, 1250–1258, 1274 (1386 lines total) |
| `/home/zero/architect-dogfood-2026-07-24/FINDINGS.md` | `94cb55e86b3fbb1dd9cdf76f3956dcc62ac7aae6f84831f29642f8123ddb13c8` | all (189 L) |
| `Architect-Attempt-1/stages/` | `charter.md` = `6134cad8d5edceaa4be6a39bd7eaec430c9236360ddb41ff38511d6a983c7819` | directory listing only; no file read |
| `Architect/changes/charter-2026-07/records/stage3-prompt.md` (my charter) | `ae556a4a579bf84b46aa5401a53f5fc764bb3b9b41d9ab19bc0e068931d55b73` | all |

**Sources I used that are NOT in my given closed set** (declared per the charter's closed-set rule):

| Path | sha256 | Why |
|---|---|---|
| `Architect/guarded-change.architect.md` | `a3dd59fc80e727fad8246948447fcbcee2d359ac8b4a7d519d59752321bac511` | The run's own Layer-2 config. The artifacts cite it as normative (`0-baseline.md` L4, L215; `1.5-criteria.md` L53) and `1.5-criteria.md` Part B declares a deviation *from it* — I could not audit that deviation without reading it. Load-bearing to findings F9, F6. |
| `Guarded_change/stages/stage-4.md` | `175061c27bb6980ff76c6358d403299d343c409b2952308ffea7485fff880675` | `0-baseline.md` P8/D4 cites `stage-4.md` L31–36 as the source of the imported demotion rule; verified. |
| `Guarded_change/stages/stage-3.md` | `16dbb31b9ccd2ee83296bf24e0941e05556d3a545f55901e8f28e519b6554df3` | The fork source L33 points RAT1/RAT2 here; verified they exist. |
| `Architect/README.md` | `1c2c6f0290256337035da96a0a81c87f158347ad639cde89607f515ca962a2d3` | Surfaced by the D7 grep verification I was instructed to run. |
| `/home/zero/architect-hardening-loop/LOOP-STATE.md` | `d01cb5b50890550ddbd6d6fa0b5c7fe68c59e0967d3157a5cf80c93250c3715c` | Grepped only (not read) — it is one of the two files `0-baseline.md` §0.6's grep targets; I re-ran that grep. |

---

## Lens 1 — Factual

**F9 (major). `0-baseline.md` §0.6 L224: "No singleton rate is recorded anywhere on disk" is false, and I verified it false.** I ran the author's grep verbatim and reproduced their output exactly (one hit, `FINDINGS.md:123`, unrelated) — so the *narrow* claim holds. But the broad claim does not. Widening the search:

```
Architect/guarded-change.architect.md:77:  ...including the measured singleton rate (~85% of findings caught by exactly one
Architect/README.md:31:  wrong for findings: measured across attempt 1's runs, ~85% of findings were caught by exactly one reviewer,
Architect/ATTEMPT-2-STATE.md:87:  **wrong for findings**: ~85% of attempt 1's findings were caught by exactly one reviewer, and several
```

`0-baseline.md` OOS-1 (L246) records only the third of these. Two consequences, and the second is the one that matters:

1. The baseline asserts a universal negative on evidence covering two files, in the one document whose entire authority is that its claims are mechanically checked.
2. **`Architect/guarded-change.architect.md` L75–79 is the `redteam_context` note for `FINDINGS.md` — it is text handed to every cold reviewer this run dispatches**, and it states the figure as *"the measured singleton rate … that the Union-not-majority-vote rule rests on."* D7's stated justification (§0.6 L232: "it is the kind of number a reviewer would reasonably take as measured fact") is therefore defeated in practice: X1 strips the number from the charter while the run's own dispatch context keeps asserting it as measured. Fixing the config is element 3's job and correctly out of scope — but *recording it as an OOS note* is in scope and required by C1's own words (`1-spec.md` L84), and it is not recorded.

**Verdict on D7 itself (task item 3): the rejection is correct and I uphold it.** The figure is genuinely unsourced; `FINDINGS.md`'s convergence counts (L35 "3/3", L46, L60 "5 independent hits", L70, L89, L101, L106, L112, L120–138) run the other way, exactly as §0.6 says — I checked each cited line. The replacement justification (`Architect.md` L20 `Union` "DISCARDS NOTHING… A finding one reviewer caught is signal") **is** sufficient: it is a mechanical guarantee about what happens to a lone finding, which is strictly stronger motivation than a recall statistic, because it removes the reviewer's reason to model its peers at all.

**F13 (minor). `1.5-criteria.md` C-06/C-02 L29 — the count is wrong.** C-02 says "Each of the **17** baseline rules whose declared intent is CARRY or CHANGE (B01–B18 minus B15's dropped sub-clause)". `0-baseline.md` §0.3 assigns an intent to all 18: B02 and B09 are CHANGE, B15 is "CARRY (with one declared DROP)", the other 15 are CARRY. **All 18 are CARRY-or-CHANGE**; subtracting a *sub-clause* does not remove a *rule* from the count. `2-plan.md` §2.1 L58–62 says `rules.tsv` is **generated from `0-baseline.md`** and its row count is pasted — so a generator reading intents emits 18 and disagrees with the criteria doc's 17. This is the "hand-typed list disagrees with the generated file one directory away" defect (`1-spec.md` C5, L102–103) reproduced inside the criteria document.

**F15 (minor). The imported severity table silently drops a cell.** `Guarded_change/stages/stage-4.md` L17: `**Blocker** | wrong problem / will not work / **unverifiable**`. `Architect/stages/charter.md` L135 defines blocker as "solves the wrong problem, contradicts a settled decision, omits a load-bearing element of the task, or cannot be executed as written" — **"unverifiable" is gone.** The table is imported wholesale under P7/D4 (`0-baseline.md` L180, L203), but the baseline inventoried only the table's *existence*, never its cells, so no CARRY/CHANGE/DROP intent exists for this content and C-07 (`1.5-criteria.md` L35) checks only "all four levels + the loop semantics", not the definitions. In Architect specifically, "unverifiable" is the severity that catches the founding-failure shape (a plan whose completeness cannot be established) — and `1.5-criteria.md` L127–128 makes unverifiability a stop-for-human, with no severity anchor to route it.

**F22 (nitpick). `0-baseline.md` §0.1 L38 says the fork source is "(104 lines)".** `wc -l` returns 103, and the file's last byte is a newline after L103 (`The reviewer is graded on precision…`). Every L-citation into it checks out; only the count is off by one.

**Verified correct** (checked, no finding): the §0.1 hash chain (I re-ran `sha256sum` — `0e73bacf…adc590` matches at HEAD); the §0.2 grep output reproduces exactly; the two un-marked rule spans (L10–14, L16–17) are correctly identified; `Architect.md` L14/L16/L20/L22/L24/L62/L100–103/L106 all say what `1-spec.md` §2–§3 claim; `decisions.md`'s 8-of-8 path validation (I opened all 8); every rule-ID the criteria cite (ST1.5c/d/e/f, H3, H6, RAT1/2/3, CFG3, SEV3, SEV4) exists in `Guarded_change/`; `FINDINGS.md` F4 L60–68, F7 L89–99, F9 L106–110, tier-3 L133 all say what is claimed of them.

## Lens 2 — Logical

**F3 (major). `2-plan.md` §2.1 L58–59 — the inventory is both the claim and the oracle.** `rules.tsv` is generated *from `0-baseline.md`*; `check.sh` asserts every row; C-02/C-03 are declared the run's regression arm (`2-plan.md` L100). So the fork-fidelity check can only ever verify that the charter states what the baseline *noticed*. Any rule the fork source states and B01–B18 missed is invisible to the checker **by construction** — the two instruments fail together, in the same direction, from the same source. C-M1's mutation test does not help: it proves each assertion can fail, not that the assertion set is complete. This is a single point of failure for the entire regression bar, and F2 below is a live instance of it. The cheap fix is a second, independently-derived probe — e.g. a mechanical diff of the shipped charter against the frozen fork source with every matched span struck out, so residue is *visible* rather than inferred from a list.

**F8 (major). The CHANGE class has no verification of its "difference declared" half.** `1-spec.md` C2 (L86–88) promises three exhaustive classes: CARRY (still stated), CHANGE (stated differently, **and the difference declared**), DROP (named in the blockquote). C-01 (L28) requires the blockquote to carry a carried-list marker and a deliberately-not-carried marker; C-03 (L30) covers the single DROP; C-02 (L29) lumps CARRY and CHANGE together as mere presence. **Nothing requires, or checks, that any CHANGE is declared anywhere the reader of the shipped charter can see it.** B02 (five lenses → six) and B15 (closed-set membership redefined, F14) are declared only in `0-baseline.md`, a run-internal document that no future reader of `Architect/stages/charter.md` will open. Concrete scenario: someone diffs the shipped charter against `Guarded_change/stages/charter.md @ 8d73e5d`, finds the closed set now reads "the node's task, its plan, the granularity floor … plus the run config's review context" instead of "the named stage artifacts + `redteam_context` + the spec's touched-files list + carried-forward findings from `decisions.md`", and reads a provenance blockquote claiming the discipline bullets were carried. That is the *same false-provenance defect* the run was created to catch in the draft (`0-baseline.md` §0.4 L192), reproduced one class up. The Dragonfly precedent shows the house fix: its blockquote names the closed-set rule explicitly among what was "deliberately not carried" (`Dragonfly/stages/charter.md` L10–12).

**F14 (minor). B15's closed set is a four-member enumeration; the inventory compressed it to two words, and D3 silently drops two members.** Fork source L74–76: "the named stage artifacts + the config's `redteam_context` + **the spec's touched-files list** + **carried-forward findings from `decisions.md`**." `0-baseline.md` B15 (L90) records only "Reviewer input is a **closed set**". D3 (L202) re-specifies it as three signature arguments + review-context paths. The touched-files and carried-forward-findings members vanish. Their Architect analogs are arguably nil, which is why I rate this minor — but that is exactly what a declared DROP is *for*, and C-02's presence probe for B15 will pass either way.

**F13 (minor)** — see Lens 1, the 17-vs-18 count.

**F17 (minor). An unratified "only" in the termination argument.** `1-spec.md` L43–44: "With no cap, **the floor as stated in the charter is the only thing preventing non-termination.**" Owner record **1258** (verbatim, read at index 1258): *"I think trust the blocker/major filter, fix it later if it is an issue."* The owner named the **blocker/major filter** as the thing to trust; `Architect.md` L22 confirms `Severity()` drops minors, which is independently terminating (a round producing only minors ends the loop). The spec's "only thing" excludes the mechanism the owner actually cited. I note the phrasing is inherited verbatim from `guarded-change.architect.md` L156 ("so the floor is the only thing preventing non-termination") — so it is a pre-existing config claim, not an invention of this spec — but the config is author-written, not owner-authored, and neither the baseline nor the spec flags it. Also no OOS note routes the fix.

## Lens 3 — Missed opportunity

**F1's fix is nearly free (see Lens 4).** C-17 needs a third arm type — charter-ablating, floor-before vs. floor-after, same fixture. The author has already built exactly that apparatus for B-3/B-4 and self-flagged it as a deviation; the one criterion that *requires* charter-ablation is the one where they used fixture-variation. Adding a fifth arm pair costs two more cold agents against a plan already budgeting eight.

**A mechanical fork-diff would close F2 and F3 together** (see Lens 2) — strike out every matched span, list the residue.

**The provenance blockquote occupies the highest-primacy slot in a prompt and spends it on build metadata.** `2-plan.md` §1.1 block 1 puts it at the head, mirroring Dragonfly. That is right for Dragonfly, whose blockquote is 5 lines. The draft's is 10 lines and carries a "REVISED 2026-07-25 for attempt 2 … the old two-pass structure is gone" narrative (`Architect/stages/charter.md` L16–20) that is build history, not reviewer instruction. §1.1 reasons carefully about recency for blocks 3, 6, 7 and 8 and not at all about block 1. Given C-19 concedes token cost is paid on every spawn, the reviewer-facing content ought to lead and the provenance ought to be a compact head note or a tail appendix.

## Lens 4 — Unstated assumptions & risks

**F1 (blocker). C-17 is a gating criterion with no valid verification — and the plan's own text closes both routes.**

- `1.5-criteria.md` C-17 (L46): the text half is a line-number assertion; "per ST1.5d and H3 a position criterion is satisfied only by **execution**, never by re-inspecting the assembled text, so C-17's positional assertion alone does **not** satisfy it; **B-1 is the executed half.**"
- `2-plan.md` L108–110 restates it: "C-17 is a position-lens criterion, so it is satisfied **only** by B-1's execution. The checker's line-order assertion is recorded as supporting evidence and is explicitly **not** sufficient."
- **But B-1 does not vary position.** `1.5-criteria.md` B-1 (L75): HOLED = "a plan containing a step **above** the floor"; INTACT = "the **same** plan with that one step rewritten to sit **at** the floor; everything else identical." `2-plan.md` L122 confirms: "fixture-varying for B-1/B-2." **Both arms receive the same charter, with the floor in the same place.**

Failure scenario, concrete: build the charter with the floor placed *after* the six lenses. Run B-1 exactly as specified. The HOLED arm still files the vagueness finding (the floor's text is present in its charter), the INTACT arm still files none. B-1 passes. C-17 — "the granularity floor is positioned before the lens block" — has been "verified" by an experiment that is provably insensitive to the position it governs. This is precisely CH9's target (a verification satisfied by a proxy) and, worse, it is the position lens's own signature failure — "all the information is still present" accepted as a clean verdict, which the charter core (L88–89) names as *not* a clean verdict.

Since the plan itself declares the text half insufficient, C-17 currently has **no** verification at all. Under `1.5-criteria.md` L127–128 — "If any gating criterion is later found unverifiable pre-ship, that is a stop-for-human and halts this runner (RAT3) — it is not silently downgraded to advisory" — this is an unrecognized stop-for-human sitting inside the document that declares the rule. Blocker: unverifiable, and it defeats the specific lens the charter (L169–170 of my prompt) says fires on this change.

**F10 (major). B18 moves out of the fork source's tail-recency slot; nothing declares it and nothing tests it.** Fork source L103 — "The reviewer is graded on **precision** (are its findings real?), not on how many it raises" — is the **final line of the document**, after every discipline bullet. `Architect/stages/charter.md` folds it mid-list into the "No issue found" bullet (L88–89), and `2-plan.md` §1.1 makes the tail slot **block 8, the two callers**, with the discipline bullets at block 5. B18's intent is `CARRY` with no note (`0-baseline.md` L126). C-02 verifies presence only. So: an element that did not itself change is displaced from last position to mid-document — the exact trigger the charter core spells out ("an added tail block displaces the old last element") — and the plan's §1.1, which gives a positional rationale for blocks 3, 6, 7 and 8, gives none for this. Failure scenario: the precision counterweight loses recency in a document that has *added* four new sections of aggression-licensing material (six lenses, three tiers, floor, severity table); reviewer body-count rises; `Union` discards nothing; `Severity` passes every inflated major through; `task` never empties — which, with no backstop cap (record 1258), is the non-termination mode. Ranked by impact, not by whether text was lost.

**F12 (major). Nothing covers the case where `_granularity` arrives empty, vacuous, or inoperable.** `Architect.md` L24 passes `_granularity` as a bare string; there is no default, no validation, and no `Divisible`/`Spawn_redteam` precondition anywhere in the 115-line spec. The draft charter (L31–32) says "You are given a granularity floor" — an assumption, stated as fact. `1-spec.md` §2.2 (L44) makes the floor "the only thing preventing non-termination" and `guarded-change.architect.md` L157 says "treat any weakness in it as at least **major**." Failure scenario: a node deep in the tree calls `Spawn_redteam(task, plan, "")`. The reviewer reads a charter asserting it has a floor, has none, and reverts to unbounded vagueness-hunting — the Manual Samuel regress owner record **1128** exists to prevent, with no cap to stop it. The charter costs one sentence to close this ("if you were given no floor, or the floor you were given is not operable against this plan, file *that* as a blocker"); it is a natural extension of the draft's own "if the floor itself is wrong for this task, say *that*" (L41–42). No S-item, no criterion, no arm.

**F11 (major). B-2's fixture depends on two artifacts that do not exist in vetted form.** `1.5-criteria.md` B-2 (L76): HOLED = "a plan missing a **load-bearing section named by neither** the 7-section spine **nor** the plan-type's Layer-2 required list." Constructing "named by neither" requires both lists to be fixed. The spine is **element 2** and the Layer-2 config contract is **element 3**; `1-spec.md` C1 (L81–82) excludes both, and `ATTEMPT-2-STATE.md` L67 records `templates/seed/*` as "Attempt-1 artifacts, carried over. **Not yet revised for attempt 2.**" `2-plan.md` L125 says fixtures are "small **self-contained** plan documents" — so the fixture invents its own spine. But the shipped charter, per C-05 and the draft L65, states tier (i) as "**the 7 sections** every plan node must fill" — a specific normative count. Failure scenario: the INTACT arm reads "the 7 sections" in its charter, receives a fixture with an invented 4-section spine, cannot reconcile them, and either flags the mismatch (a finding neither arm's pass condition anticipates) or treats tier (i) as unsatisfiable and skips to tier (iii) in *both* arms — a same-verdict outcome that the discrimination rule (L57–60) will misattribute to a fixture defect and send into a rebuild loop. Neither the criteria nor the plan says which spine the fixture instantiates or how it relates to the charter's "7".

**F18 (minor). Part D's concurrency stand-down rests on a false premise, and it is this project's known recurring false premise.** `1.5-criteria.md` L116–118: "This change introduces no accessor or read-modify-write window over shared mutable state; **the artifact is a single document written once.**" The trigger is not about the artifact's file-count — B17 as re-aimed by the baseline itself (`0-baseline.md` L125) fires where **the plan** alters concurrency over shared state, and this plan spawns 8 behavioral arms (`2-plan.md` L118) plus 3 stage-3 reviewers (L137) concurrently, and specifies `decisions.md` as an append-only counter read back at every gate ("`decisions.md` is the record the count is read from, so every gate appends an entry", L163). I challenged the conclusion and it survives: records are one-file-per-arm (`records/<criterion>-<arm>.md`, L129), arms are read-only on shared state, and the runner serializes `decisions.md` writes — **so the lens genuinely does not fire.** But the stated reason is a non-sequitur that would license the same stand-down in a run where it does fire, and `FINDINGS.md` L123–124 records this project already producing that exact failure: "a root reviewer **contests the build's own stage-6 red-team** for standing the concurrency lens down on a false premise." Per the run's own P9/D5 ("recurrence means under-generalization"), the fix is the correct reason, not a better-worded wrong one.

**F20 (minor). The behavioral arms' model and agent-type are unpinned, and F7 makes that a livelock risk.** `2-plan.md` §4 fixes count (8), spawn-separateness and input isolation, but never the model or agent type; `decisions.md` L85 pins two models for the *stage-3* reviewers only. `FINDINGS.md` F7 (L89–99) is the measured result that "three instances of *one model* … have **correlated** blind spots." If a HOLED/INTACT pair runs on the same model and both miss the plant, the discrimination rule (L57–60) mandates "the fixture is rebuilt … and re-run" — attributing to the fixture what is a reviewer-population property. Under `2-plan.md` §6's cap (2 bounces at the same gate on the same finding class), that is a route to halting on a gating criterion that no fixture edit can fix.

**F23 (nitpick). `decisions.md`'s "verbatim, as read" table truncates without elision.** Records 1148, 1258 and 1274 each carry material the table omits with no `…` marker: 1148 continues *"The crash recovery, not entirely sure what you're wanting on that one. As to redteam, remind me what the shape of it was for the first attempt at this?"*; 1258 continues *"Red-team the charter? Aren't you planning to run this entire thing through guarded-change?!"* (the sentence that authorizes this very run); 1274 opens *"I literally thought you were just collecting answers prior to starting a guarded change run O_O"*. Nothing omitted changes any ruling — I checked each — but a table headed "Owner's words (verbatim, as read at the record)" that is silently partial weakens the one artifact class RAT1 depends on.

## Lens 5 — Fidelity

Loaded operational terms pinned, with the concrete mechanism each was pinned to:

| Term | Pinned mechanism (from `~/Documents/Architect.md`) | Artifact implements that mechanism? |
|---|---|---|
| "3 independent cold agents" | 3 × `Spawn_redteam` at L100–103; the count is in the code | **Yes.** S13/C-15/D6 pin it to "three separately-spawned subagents". D6 correctly declares the "…and none with each other" clause as an author strengthening (owner record 55 defines no independence). No proxy. |
| "the red-team's findings become the next task" | L106 `task = Severity(Union(redteam.get_issues))`; L62 loop condition | **Yes.** S6/S7/C-07 pin severity as the loop's filter, and C-14 requires "the red-team going quiet is the completion condition." No proxy. |
| "granularity floor" | the `_granularity` string threaded to L14 / L10 / L24; L1–8 names it a three-way bound | **Partly — see F12.** Content pinned; delivery unpinned; the empty-floor case has no mechanism. |
| "human" (in the demotion rule) | `Human_gate` (L16), the sole human mechanism in the spec | **No — see F4.** |
| "whoever consumes the review" (spot-verify) | `Union` (L20), `Severity` (L22), `Node` (L106) | **No — see F5.** |
| "the reviewer's closed input set" | the call signatures `Spawn_redteam(_task,_plan,_granularity)` L24 and `Divisible(_task,_granularity)` L14 | **No — see F7.** |

**F4 (major). The demotion rule invokes a human for which the spec defines no call site.** S14 (`1-spec.md` L140) and C-08 (`1.5-criteria.md` L36, **gating**) require the charter to state that "demoting a blocker or major additionally requires the **human owner**." Pinning "human owner" to a mechanism: `Architect.md` contains exactly one human mechanism, `Human_gate` (L16), whose signature is `bool Human_gate(pair<string> _division, string _task, int _depth)` — it takes a **division**, is invoked at exactly one site (L79–85), and fires only at `depth <= gate_depth` (default 2). It cannot receive, and is never invoked on, a severity. The rule's actual source is `Guarded_change/stages/stage-4.md` L31–36, where "the human tie-break" is a *gate-time* actor in a loop that has gates. Architect has none — `Severity()` (L22) is a cold agent that filters mechanically and returns. Failure scenario: a reviewer at depth 5 files an inflated blocker; `Union` keeps it (L20 discards nothing); `Severity` returns it; it becomes `task`; the node re-plans against it; the next round produces another. There is no point in the specified control flow at which any human can demote it, and no cap (record 1258). So the rule is either inert — a gating criterion mandating text with no referent — or it is an *implicit demand for an unspecified human interrupt* in an autonomous recursive loop, which is a design commitment the owner never made. D4's justification (`0-baseline.md` L203) explains why the rule belongs *in the charter* rather than a stage file; it never asks whether the actor it names exists. That is the untested half.

**F5 (major). The named spot-verify consumer is specified in a way that forbids it from acting.** B14's carry (`0-baseline.md` L122) adds "a named **consumer**: in Architect the consumer is `Union()`/`Severity()` and the node that calls them (`Architect.md` L20, L22, L106)", and C-12 (**gating**) checks the charter "names who performs it (not left assigned to nobody)". Pinning the term: `Union` is specified as "merges issues, **DISCARDS NOTHING**; dedups only exact restatements" (L20) — a merger that discards nothing structurally cannot drop a finding whose citation is fabricated. `Severity` "returns only the blocker|major issues" (L22) — it filters on the severity field, not on citation validity. `Node` at L106 performs an assignment. **All three named consumers are incapable of the duty by their own specification.** Failure scenario: reviewer B fabricates `Architect.md L142` (the file has 115 lines) and rates it blocker. `Union` keeps it verbatim. `Severity` returns it. It becomes the next `task`, and the node re-plans against a citation to a line that does not exist. `FINDINGS.md` L133 recorded this duty as "**assigned to no stage**" in attempt 1; naming an agent that cannot perform it satisfies C-12's text while leaving the defect in place — a proxy substituting a name for a mechanism, which is exactly what lens 5 exists to catch. The honest options are to route it to `Human_gate`, to make it a duty of the *reviewer* on its own output (self-verify before filing), or to declare it unimplementable in this design and record an OOS note against the spec — not to name `Union`.

**F7 (major). D3 rejects a widening for one caller and re-commits the identical widening for the other, whose signature is narrower still.** (Task item 1.) D3 (`0-baseline.md` L202) resolves the closed set as "the three spec'd arguments + the run's review-context paths", and states the parent-plan/seam inputs "**only for the `Divisible` split-review caller**", with the rationale that "adding parent-plan/seam to *every* plan red-team is an un-spec'd widening of a signature the owner wrote; scoping it to the caller that needs it is not."

But `Architect.md` L14 is `pair<string> Divisible(string _task, string _granularity)` — **two** arguments. It does not receive the parent's plan either. D3's own stated principle applies verbatim, and more forcefully, to the caller it exempts. The rationale offered ("L14's 'WITHOUT either sub-task falling below _granularity' and the seam are intrinsic to what is being reviewed") is a design argument, not a spec citation, and it conflates two different things: the **seam** *is* intrinsic — `Divisible` derives the split, so the seam is internal to the call and needs no new input — while the **parent's plan** is genuinely external and genuinely absent from the signature. So D3 grants exactly the un-spec'd input it identified as un-spec'd, under a rationale that only covers the input that was never a problem.

On the review-context half: adding it to the charter's closed set means the shipped charter will state a rule the runtime cannot satisfy — `Spawn_redteam` has no config or context parameter (OOS-3 concedes this, `0-baseline.md` L252–254). Per my charter, where an artifact disagrees with `~/Documents/Architect.md`, that file wins and the disagreement is a finding. Deferring it to OOS-3 as "an owner-spec question, not a charter question" does not dispose of it: it is the *charter* that asserts the closed set, and it is element 1 that ships the assertion. The available honest form costs nothing — state the closed set conditionally ("your task, your plan, your granularity floor, plus any review-context paths your caller supplies"), which is true under either signature and does not pre-empt the owner's decision. **My verdict on the three options in the prompt: it is a defensible reading for `Spawn_redteam`, an un-spec'd widening for `Divisible`, and a live artifact-vs-spec contradiction on the review-context path.**

**F6 (major). B-4's ablation is predicted to run backwards.** (Task item 2, second half — see the Label audit for the config-conformance half.) `1.5-criteria.md` B-4 (L78): INTACT arm gets the shipped charter, HOLED arm gets it "**with the union / file-it-even-if-lone rule deleted**"; both get a fixture "containing a defect a reviewer would plausibly assume someone else will catch"; pass condition "INTACT arm files the plant; HOLED arm omits it or hedges it away."

Self-censorship requires the reviewer to believe peers exist. `2-plan.md` L119–121 is explicit: "Each arm receives exactly **two things**: its fixture and its charter copy. No author commentary, no hints, no summary of what the arm is testing." So the *only* text in either arm's entire input that discloses the existence of other reviewers is the union bullet itself — `Architect/stages/charter.md` L82–83: "nothing you file is discarded for being unconfirmed by **the other reviewers**." **Deleting that bullet deletes the arm's only reason to think it has peers.** The HOLED arm is then a solitary reviewer with no basis for "someone else will catch it," and has *less* reason to self-censor than the INTACT arm, not more. The predicted outcome is same-verdict (both file the plant), which under the discrimination rule (L57–60) is `verified = no` plus a mandated fixture rebuild — for a defect no fixture edit can fix, because the confound is in the ablation design. Under `2-plan.md` §6's 2-bounce cap that is a route to a halt on a gating criterion. The fix is to give both arms an identical, minimal peer-existence statement outside the ablated text, so the ablation isolates the *instruction* rather than the *premise*.

**F21 (minor). B-3's ablation confounds two variables.** B-3's HOLED charter has "the severity-mandatory rule **and the severity table** deleted" (L77). Pass condition: "HOLED arm files at least one finding with no severity." Deleting the table also deletes the vocabulary. A HOLED reviewer that invents its own scale ("high/medium/low") has attached severities — same behavior as INTACT on the measured axis — producing a non-discriminating pair attributed to the fixture. Ablate the mandate alone, or state that ad-hoc severities count as "no severity" and why.

**Clean on these axes, stated so the all-clear is real:** D6's strengthening of record 55 is correctly declared as the author's, not the owner's. D7's rejection of P5-stat is correct on the merits and its replacement authority is sufficient. D1 does not inflate record 1175 (see the Ratification audit). The one-pass structure is faithful to `Architect.md` L98–107 and correctly refuses attempt 1's two-pass (`Architect-Attempt-1/stages/` contains `stage-3-completeness-critic.md` and `stage-4-adversarial-redteam.md`, confirming what X3 excludes).

## Coverage challenge (CH8) — required section

Behaviors this change could plausibly alter that **no criterion observes**:

1. **The position of every block except the floor.** `2-plan.md` §1.1 specifies an 8-block order and reasons positionally about six of them. **C-17 observes exactly one adjacency** (floor < lenses), and even that is unverified (F1). Unobserved with concrete impact: B18's demotion from tail-recency (F10); the conditional lenses moved to their own block after the unconditional bullets (block 6) where a non-firing trigger "costs the reader nothing" — an untested claim, when `FINDINGS.md` L123–124 records a reviewer standing a conditional lens down on a false premise as a real failure of this project; and the head slot spent on provenance narrative. Scenario: any of these lands and the shipped charter passes every criterion, because presence is all that is measured. Impact: **major** — this is a prompt, the position lens is declared to fire, and the observation covers one of eight blocks.

2. **The conditional lenses' *trigger discipline*.** C-13 (gating) checks the two lenses are "stated with their trigger conditions." No arm tests whether a reviewer under this charter correctly *fires* or *stands down* a conditional lens. This is the single behavior this project has measurably failed (`FINDINGS.md` L123–124) and it is a text-presence criterion. Impact: **major**.

3. **The completeness lens's interaction with the other five.** D1's whole justification (`0-baseline.md` L200) is that a distinct lens prevents completeness being "crowded out." B-2 tests only that tier (iii) fires on a holed fixture — with the charter held constant. Nothing tests the crowding-out claim itself (e.g. lens-6-as-bullet vs. lens-6-as-lens, same fixture), which is the load-bearing hypothesis behind the run's most-scrutinized author decision. Impact: **major**. This is the same missing charter-ablation as F1, and one arm pair would test both D1 and C-17.

4. **The empty/inoperable granularity floor** (F12). Impact: **major**.

5. **Reviewer output volume and the loop's termination.** With no backstop cap (record 1258), a charter that raises finding-count raises the chance `Severity()` never empties. The charter adds a sixth lens, three tiers, an earned-clean clause for it, a severity table and two-caller aiming — all aggression-increasing — while its one counterweight (B18) loses tail position. **No criterion observes finding volume at all.** B-1..B-4 measure per-arm presence/absence of a specific finding, never the total. Scenario: the shipped charter yields, say, twice the findings per round of the draft; every criterion passes; the loop's practical termination degrades. This is cheap to observe — B-1..B-4's records already contain the raw output; a recorded finding-count per arm costs nothing and makes the number visible for the *next* charter revision. Impact: **major**.

6. **Whether the charter is actually delivered verbatim, and identically, to both callers.** C-14 requires both callers addressed. Nothing requires — or observes — that the per-caller aiming be *additive* to an unmodified core. The fork source states this rule (L70–74) and the inventory missed it (F2). Impact: **minor here** (the delivery mechanism is element 4), **major as an inventory miss** — filed as F2.

No other gaps found. Sections 1–5 above are the coverage challenge; this is not a "none found".

## Label audit (CH9/CH10) — required section

Per gating criterion, the governed path and the evidence I checked that it would be exercised.

**Earned clean** — the instrument exercises the path the criterion governs:

- **C-01, C-03, C-04, C-05, C-06a/b, C-07, C-08, C-09a/b, C-10, C-11, C-15, C-16.** Governed path: the rule's text is present at the site that must state it, in the *shipped* file. Instrument: `check.sh` positive per-site assertion on normalized text (`2-plan.md` L70–74), gated by C-M1's per-assertion mutation kill (L81–85). The path is the governed path — no proxy, because these criteria make no behavioral claim; the criteria document is explicit that text presence cannot establish behavior (L52) and routes behavior to Part B. Evidence checked: `2-plan.md` §2.2 (input refusal with a distinct exit code, normalization before matching, absence sweeps run only paired), §2.3 (each assertion must FAIL on its own mutant and PASS on the clean file). The absence sweeps' paired-only rule is honored in both the criteria (L34, L38) and the plan (L73–74, L101).
- **C-M1, C-M2.** Governed path: the checker's own ability to report a failure. Instrument: mutation of the shipped charter per assertion, **plus** a declared negative control whose expected outcome is `SURVIVED` (`2-plan.md` L90–93), so the harness is observed printing a kill *and* a non-kill. This genuinely exercises the governed path and closes the bare-`exit 0` defect it names. The ordering constraint (Part-A verdicts are `verified = no` until the self-test output is on disk, L112–114) is correctly enforced at document level.
- **B-1** as a *content* test of the floor. Governed path: the reviewer's vagueness judgement under the floor's text. Exercised. (It does not govern position — F1.)
- **B-2** as a *tier-(iii)* test, subject to the fixture-availability defect (F11).

**Not earned:**

- **C-17 — blocker (F1).** The governed path is *position*; the nominated instrument varies *fixture*. The plan explicitly forecloses the text half. No instrument exercises the governed path.
- **C-02 — major (F3, F13).** The governed path is "every fork-source rule that must still be stated, is." The instrument's probe set is generated from the same document that makes the claim, so it cannot exercise the path for any rule the inventory missed; and the criterion's stated row count (17) disagrees with the intent table it is generated from (18).
- **C-12 — major (F5).** The governed path is "the spot-verify duty has a performer." The check is that the charter *names* one. A name whose specified behavior forbids the duty satisfies the check and not the path.
- **C-08 — major (F4).** Same shape: the check is that the demotion rule is stated; the governed path is that a demotion route exists. It does not.
- **B-4 — major (F6).** The ablation cannot isolate the charter's contribution because the ablated text carries the premise the behavior depends on.
- **B-3 — minor (F21).** Two variables ablated together.

**Advisory labels challenged:**

- **C-19 (length) — legitimate.** The stated reason ("no defensible numeric threshold exists pre-build; there is no baseline reviewer-cost measurement… inventing one would be exactly the guessed metric this loop refuses") is sound, and the criterion still requires a *measured number* to be reported. Advisory is the honest label here, not a dodge.
- **C-18 (scope containment) — partly a dodge, minor (F19).** The stated reason — element 3's contract does not exist, so a mechanical bar would measure against an unwritten sibling — is legitimate for the "no Layer-2 config field list" clause only. The other two clauses ("no spine section names as normative content", "no router/stage plumbing") are absence sweeps over a known vocabulary, mechanizable at near-zero cost and no more speculative than C-16's banner sweep. Labeling all three advisory over-broadens a narrow exemption — and it exempts the one clause that would catch the C-05/C-18 tension: C-05 (gating) *requires* the charter to state tier (i) as the "universal spine", while the draft renders that as "the **7 sections** every plan node must fill" (L65), a normative count about an unbuilt element. The criterion that could flag that is the one made advisory.

**Representativeness of the pre-ship harness** (CH9's third clause): the claim is that four cold-agent pairs establish the charter's behavioral effect. Two challenges, both filed above: the arms' model/agent-type is unpinned against a measured correlated-blind-spot result (F20), and B-2's fixture cannot be built representatively without element 2 (F11). The **discrimination rule itself is the harness's strongest feature** and I want to record that as a genuine all-clear: "same verdict on both arms ⇒ `verified = no` **whichever verdict it is**" (L57–60) closes the single-arm shortcut R1 names, and it is stated as binding every row rather than per-row.

## Ratification audit (CH11/CH12) — required section

I read **all seven** cited records at their indices in the harness-authored JSONL (1386 lines total) — records 55, 1128, 1148, 1175, 1188, 1258, 1274 — plus the assistant turns the rulings answer (1171, 1254). **Spot-verification result: 7 of 7 quotes in `decisions.md` L41–47 are character-exact against the record.** (The charter asked for two; I did all seven because the whole run's authority rests on them.)

**Record 1175 and D1 — the flagged case. I confirm D1; the audit that produced it is incomplete but its conclusion is right.**

Owner, record 1175 verbatim: *"So you're proposal for crash recovery is basically working up the work_queue? … regarding redteam: Agreed for the most part, but the new charter should also include the definition of three tiered completebess definition"*.

RAT1 requires the **presented options** be cited, not just the answer. Neither `0-baseline.md` §0.5 D1, nor `1-spec.md` §9, nor `decisions.md` cites what the owner was agreeing to — they assert a negative ("says nothing about placement") from the answer alone, which is the half-audit RAT1 exists to prevent. I supplied the missing half: **record 1171** is the assistant turn 1175 answers. It describes attempt 1's charter as "five inherited lenses … **plus a sixth Completeness lens added for Architect**", then separately states "**Completeness was three-tiered:** (i) the 7-section universal spine, (ii) Layer-2 per-plan-type required sections, (iii) a generative critic for what neither list anticipated", then proposes a trim: "Worth keeping from it: the **discipline bullets** … and the **severity ranking** … **The rest of the charter is trimmable.**"

So the axis actually before the owner was **what survives the trim**, and his answer selects one item: the three-tier definition. On the **lens-vs-bullet** axis he was shown a document where completeness was already a sixth lens and said "Agreed for the most part" — which is an *adjacent* answer, not a selection. Resolving that into "the owner ratified the distinct-lens placement" is precisely the partial-answer-resolved-into-the-author's-pick move RAT1 names, and **the author did not make it**. `0-baseline.md` D1 (L200), `decisions.md` L49–57 and `1-spec.md` C9 (L114–119) all state the placement as author-owned and refuse to cite 1175 for it. The corroborating detail `decisions.md` L56–57 adds — that at record **55** the owner wrote *"not sure what you mean by lens though"*, which I verified verbatim — is real and is good evidence that "lens" was not owner vocabulary. **No inflation. D1 is correctly classified, and `ATTEMPT-2-STATE.md` L99–101 (which does state the distinct-lens placement as settled design) is correctly refused as agent-written.** Finding filed as **minor (F16)**: cite record 1171 in the D1 audit, so the negative is demonstrated rather than asserted.

**Is the distinct-lens choice defensible on its own merits?** (Task item 4.) Yes, on a carried rule: B02 — a fork-source rule, `Guarded_change/stages/charter.md` L16–17 — states lenses are kept separate "so one doesn't crowd out the others", and `FINDINGS.md` F7 (L89–99) is the measured evidence that tier (iii) is the fragile one. That is a real argument, not a dressed-up ruling. **But the run never tests it** — see CH8 gap 3. A decision this carefully quarantined from owner authority, justified by a crowding-out mechanism, in a run that has built charter-ablation apparatus for two other criteria, ought to have an arm.

**Elaborations checked for unratified inflation (CH12):**

| Ruling | Elaboration relied on | Verdict |
|---|---|---|
| **1128** (max granularity / Manual Samuel) | `1-spec.md` L43–44 "the floor … is **the only thing** preventing non-termination" | **Inflated — minor (F17).** The owner asked for a max-granularity *option*; at 1258 he named the **blocker/major filter** as the thing to trust, and `Architect.md` L22 makes it independently terminating. "Only" is an operative absolute not entailed. Inherited from `guarded-change.architect.md` L156, so pre-existing — but unflagged, and no OOS note routes it. |
| **1258** (no backstop) | "no backstop iteration cap" | **Clean.** I read record 1254, the turn it answers: "whether `Severity()` never emptying needs a **stop-for-human**, or whether the floor plus the blocker\|major filter is enough to trust." The owner's "trust the blocker/major filter" selects on exactly that axis. Note 1258's second sentence — "Red-team the charter? Aren't you planning to run this entire thing through guarded-change?!" — is the authorization for this run and is omitted from the quote (F23). |
| **1148** (gate depth) | C-14 / P10 "human gate at depth ≤ `gate_depth`", default 2 | **Clean.** "At least to the second level should be a safe default" ↔ `Architect.md` L16 "DEFAULT 2". No inflation. |
| **55** ("three independent cold agents") | D6's "**and no shared reasoning context with each other**" | **Clean and correctly declared.** The owner defined no independence property; D6 (`0-baseline.md` L205) says so in terms and cites dogfood F9 (`FINDINGS.md` L106–110) as the author's evidence. This is the model for how the other decisions should read. |
| **1274** (build scope) | C1, charter-only | **Clean.** "start with each element individually (charter, spine, whatever)" ↔ element 1 only. |
| **1175** | D1 | **Clean — see above.** |
| **1188** (crash recovery) | *nothing* | Listed in `decisions.md` L44 but relied on by no artifact under review. No audit owed. |

**One inflation-adjacent pattern worth naming:** F4 and F5 are both *mechanism* inflations rather than *ruling* inflations — a "human owner" and a "consumer" asserted into existence by a charter sentence, with no call site in the authoritative spec. RAT2's discipline ("operative commitments — a mechanism, an 'only/every/never', a division of responsibility — not present in or entailed by") reads on these as squarely as on an owner quote.

## Unverifiable claims I could not check

1. **Everything about the shipped charter.** It does not exist. Every Part-A verdict, and F10/F11/F12/F15's manifestation, are assessments of a *plan to produce* text. Where I cite `Architect/stages/charter.md` I am citing the unvetted draft as evidence of authorial intent, not as the artifact — the plan's §1.2 adopts most of its rules, but the shipped wording could differ.
2. **Whether `check.sh`'s probes will be strong enough to distinguish "the rule is stated" from "a phrase resembling the rule appears."** `rules.tsv`'s `probe-type`/`pattern` columns (`2-plan.md` L57) have no specified vocabulary. A regex-shaped probe for B13 (a 12-line rule) could pass on a one-line stub. C-M1 does not close this: deleting the asserted *text* kills the assertion, but a *weakened* rewrite is not among the mutants. I could not check this because the generator does not exist.
3. **The B-3/B-4 and B-1/B-2 fixtures.** Not written. F6's backwards-ablation prediction is a mechanism argument from the arms' declared inputs (`2-plan.md` L119–121), not an observation.
4. **`~/architect-hardening-loop/LOOP-STATE.md` contents** — I grepped it to reproduce §0.6's search, but did not read it; it is not in my context list. It has zero occurrences of `85`, which I verified with `grep -c`.
5. **The `Architect-Attempt-1/stages/*.md` bodies.** I listed the directory (confirming `stage-3-completeness-critic.md` + `stage-4-adversarial-redteam.md`, which corroborates the two-pass claim X3 excludes) but read no file.
6. **`~/Documents/Architect.md`'s own status as complete.** I treated it as authoritative per my charter. Where it is silent — no `_granularity` validation (F12), no severity-demotion actor (F4), no citation-validation consumer (F5) — I have reported the silence as a finding against the *plan* for asserting a mechanism into the gap, not against the spec. If the owner intends those gaps to be filled by later elements, F4/F5/F12 become OOS notes rather than charter defects; that reclassification is the owner's to make, not mine.

## Findings table

| # | severity | lens | artifact:line | finding | why it matters |
|---|---|---|---|---|---|
| **F1** | **blocker** | 4 / CH9 | `1.5-criteria.md:46,75`; `2-plan.md:103,108–110,122` | C-17 (position, gating) nominates B-1 as its executed half, but B-1 varies the **fixture** with the charter held constant — it cannot discriminate on position. The plan simultaneously declares the text half insufficient, leaving C-17 with **no** verification. | A gating criterion is unverifiable pre-ship — the criteria doc's own stop-for-human (L127–128), unrecognized. Build the floor after the lenses and B-1 still passes. |
| **F2** | major | 1 / 2 | `Guarded_change/stages/charter.md:70–74` vs `0-baseline.md:90` | B01–B18 **missed a rule**: "The charter given is the METHODOLOGY charter **core** verbatim — … plus any conditional lens whose trigger fires — with task-specific additions **quoted as such**." Absent from the inventory, the S-list, every criterion, and the draft. | A rule never inventoried is dropped without anyone noticing — the exact regression class §0 exists to prevent. It governs what text Architect's **two callers** may add to a charter read "verbatim". |
| **F3** | major | 2 | `2-plan.md:58–59`; `1.5-criteria.md:29` | `rules.tsv` is generated **from `0-baseline.md`**, so the fork-fidelity checker's probe set and the completeness claim share one source; any inventory omission is invisible by construction. C-M1 does not close it. | Single point of failure for the run's entire regression bar. F2 is the live instance. |
| **F4** | major | 5 | `1-spec.md:140`; `1.5-criteria.md:36`; `Architect.md:16,22,79–85` | S14/C-08 (gating) mandate a demotion rule requiring "the **human owner**" — but `Human_gate` is the sole human mechanism, takes a `pair<string> _division`, and fires only at `depth <= gate_depth`. No call site can demote a severity. | A gating criterion mandating a mechanism the spec does not define. Either inert text, or an unspecified human interrupt the owner never authorized (contra record 1258). |
| **F5** | major | 5 | `0-baseline.md:122`; `1.5-criteria.md:41`; `Architect.md:20,22,106` | The spot-verify-citations consumer is named as `Union`/`Severity`/the node. `Union` "**DISCARDS NOTHING**"; `Severity` filters on the severity field. Neither can act on a bad citation. | Satisfies C-12's "names who performs it" with a name that cannot perform it. A fabricated citation propagates into the next `task`. `FINDINGS.md:133` is this defect in attempt 1. |
| **F6** | major | 5 / CH9 | `1.5-criteria.md:78`; `2-plan.md:119–121`; `Architect/stages/charter.md:82–83` | B-4's ablation deletes the union bullet — the arms' **only** disclosure that peers exist. A solitary HOLED reviewer has *less* reason to self-censor, so the pair is predicted same-verdict or backwards. | Gating criterion whose experiment cannot isolate what it claims; the discrimination rule then mandates unbounded fixture rebuilds for a design confound, into the 2-bounce cap. |
| **F7** | major | 5 | `0-baseline.md:202`; `Architect.md:14,24` | D3 rejects parent-plan/seam as an "un-spec'd widening" for `Spawn_redteam` (3 args) and grants it for `Divisible` — whose signature takes **two** args and also never receives the parent's plan. The seam is intrinsic; the parent's plan is not. | The run's self-declared weakest decision re-commits, under an exemption, the exact defect it identifies. Plus the review-context path makes the charter assert a closed set the spec'd call cannot deliver (OOS-3 defers an in-element contradiction). |
| **F8** | major | 2 | `1-spec.md:86–88`; `1.5-criteria.md:28,29,30` | The **CHANGE** class's "difference declared" half is checked nowhere. C-01 requires carried + not-carried markers only; C-02 lumps CHANGE in with CARRY as presence. B02's and B15's differences live only in a run-internal file. | Reproduces the draft's false-provenance defect (§0.4) one class up: a blockquote claiming the bullets were carried, over a silently redefined closed set. Dragonfly's precedent (L10–12) shows the house fix. |
| **F9** | major | 1 | `0-baseline.md:224`; `guarded-change.architect.md:77`; `README.md:31` | "**No singleton rate is recorded anywhere on disk**" is false — verified by grep. It is stated in the run's own config as "the **measured** singleton rate", in `README.md`, and in `ATTEMPT-2-STATE.md`. OOS-1 records only the last. | D7's stated purpose fails in practice: X1 strips the number from the charter while the config's `redteam_context` note keeps asserting it as measured to every reviewer this run dispatches. |
| **F10** | major | 4 (position) | `Guarded_change/stages/charter.md:103` vs `Architect/stages/charter.md:88–89`; `2-plan.md:22–30` | B18 ("graded on **precision**") moves from the fork source's final line to mid-bullet-list; block 8 is now the two callers. `0-baseline.md:126` marks B18 CARRY with no note; §1.1 gives positional rationale for six other blocks and none for this. | Trigger is *any* edit incl. elements that did not change. The precision counterweight loses recency in a charter that added four aggression-licensing sections. Higher finding volume + no cap = the non-termination mode. |
| **F11** | major | 4 | `1.5-criteria.md:76`; `1-spec.md:81–82`; `ATTEMPT-2-STATE.md:67` | B-2's fixture requires "a section named by **neither** the 7-section spine **nor** the Layer-2 required list" — both are unbuilt elements (2 and 3), and `templates/seed/*` is an unrevised attempt-1 artifact. Fixtures are declared "self-contained", so the spine is invented — but C-05/the charter state "the **7 sections**" normatively. | A gating criterion whose fixture cannot be built representatively; a charter/fixture mismatch produces a same-verdict pair that the discrimination rule misattributes to the fixture. |
| **F12** | major | 4 / 5 | `Architect.md:14,24` (no default/validation); `Architect/stages/charter.md:31–32`; `1-spec.md:44` | No rule, S-item, criterion or arm covers `_granularity` arriving empty or inoperable. The charter asserts "You are given a granularity floor" as fact. | `Spawn_redteam(task, plan, "")` leaves the reviewer unbounded — the Manual Samuel regress record 1128 exists to prevent, with no backstop cap. The config (L157) says treat any floor weakness as at least major. One sentence closes it. |
| **F13** | minor | 1 / CH9 | `1.5-criteria.md:29` | C-02 says "**17** baseline rules whose intent is CARRY or CHANGE"; §0.3 assigns CARRY/CHANGE to all **18**. A generator reading intents emits 18 rows. | The hand-typed-list-vs-generated-file defect (C5) inside the criteria document; the row count is pasted into `8-harness.md`, so the mismatch surfaces there or is rationalized away. |
| **F14** | minor | 2 | `Guarded_change/stages/charter.md:74–76`; `0-baseline.md:90,202` | B15's closed set is a **four-member** enumeration; the inventory records "a closed set" and D3 re-specifies two members, dropping touched-files and carried-forward-findings without declaring it. | A DROP inside a CARRY row — the silent third category C2 forbids — invisible to C-02's presence probe. |
| **F15** | minor | 1 | `Guarded_change/stages/stage-4.md:17` vs `Architect/stages/charter.md:135`; `1.5-criteria.md:35` | The imported severity table drops "**unverifiable**" from the blocker definition. The baseline inventoried the table's existence, not its cells; C-07 checks levels + semantics, not definitions. | "Unverifiable" is the severity that routes the founding-failure shape, and `1.5-criteria.md:127` makes unverifiability a stop-for-human with no severity anchor. |
| **F16** | minor | CH11 | `0-baseline.md:200`; `decisions.md:49–57`; transcript **1171** | The D1 ratification audit never cites the proposal record 1175 answers, asserting the negative rather than demonstrating it. I read 1171 and **confirm D1**. | RAT1 requires the presented options *and* the answer. The conclusion survives; the record does not show its work. |
| **F17** | minor | CH12 | `1-spec.md:43–44`; transcript **1258**; `Architect.md:22` | "The floor … is **the only thing** preventing non-termination" — an operative "only" that excludes the blocker/major filter the owner himself named. Inherited from `guarded-change.architect.md:156`. | Unratified absolute in the argument that motivates the charter's most load-bearing section; no OOS note routes the config's copy. |
| **F18** | minor | 4 | `1.5-criteria.md:116–118`; `FINDINGS.md:123–124` | The concurrency stand-down's premise ("the artifact is a single document written once") is a non-sequitur — the plan spawns 11+ concurrent agents and reads back an append-only `decisions.md`. I challenged the **conclusion** and it survives (one writer per file; serialized runner). | Right answer, wrong reason — and this project has already produced a reviewer contesting a concurrency stand-down "on a false premise." Per its own D5, the remedy is the correct reason. |
| **F19** | minor | CH9 | `1.5-criteria.md:47` | C-18's blanket ADVISORY over-broadens a narrow exemption: only the Layer-2-field-list clause depends on unbuilt element 3; the spine-names and router-plumbing clauses are absence sweeps at near-zero cost. | The exempted clause is the one that would catch C-05/C-18's tension — the charter stating "the **7 sections**" normatively about an unbuilt element (F11). |
| **F20** | minor | 4 / CH9 | `2-plan.md:118–121`; `decisions.md:85`; `FINDINGS.md:89–99` | The behavioral arms' model/agent-type is unpinned, while F7 is the measured finding that same-model instances have correlated blind spots. Stage-3 reviewers are model-diversified; the arms are not. | A same-model non-discriminating pair is attributed to the fixture, mandating rebuilds no fixture edit can fix, into the 2-bounce cap. |
| **F21** | minor | CH9 | `1.5-criteria.md:77` | B-3 ablates the severity **mandate and the table** together, confounding instruction with vocabulary. A HOLED arm inventing "high/low" attaches severities. | Non-discriminating pair on a gating criterion, misattributed to the fixture. |
| **F22** | nitpick | 1 | `0-baseline.md:38` | "(104 lines)" — the fork source is **103** lines. All L-citations into it are correct. | Cosmetic, but it is the one number in the identity-verification section that was not machine-generated. |
| **F23** | nitpick | 4 | `decisions.md:41–47` | The "Owner's words (**verbatim, as read at the record**)" table silently truncates records 1148, 1258 and 1274 with no elision marker. Nothing omitted changes a ruling. | Weakens the artifact class RAT1 depends on; 1258's omitted sentence is the authorization for this run. |

## Worst severity

**blocker** — F1.
