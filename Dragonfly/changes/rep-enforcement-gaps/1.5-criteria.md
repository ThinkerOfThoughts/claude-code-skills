# 1.5 — Criteria (accept bar). All gating unless marked advisory.

## Rule-presence & content criteria
- **C1 (gating) — G1 present as `B-REP-4` @ stage-5.** A rule stating a **negative/"clean" result
  cannot eliminate a hypothesis** unless the arm was shown to exhibit the symptom under
  **same-or-weaker parameters**; else it is an **untrusted-negative**. *Verify:* grep `B-REP-4` in
  stage-5.md; rubric confirms (i) negative-can't-eliminate-without-exhibited-instance and (ii)
  same-or-weaker-parameters + (iii) the untrusted-negative label + (iv) the exhibited instance is
  **ledger-evidenced** (per B-REP-3), not a design assertion (per red-team fallback-loophole) + (v)
  a **multi-axis clause** (every varied axis same-or-weaker; a trade-off ⇒ does not qualify).
- **C2 (gating) — G2 present as `B-REG-1` @ stage-5.** A rule requiring the test's **executed
  parameters to match the stage-4 pre-registered design**, else record the deviation and **re-scope**
  the result before trusting it, and treating a run with **no registered design as itself a defect**
  (not a pass by default). *Verify:* grep `B-REG-1` in stage-5.md; rubric confirms
  executed-vs-designed + record-deviation + re-scope + no-registration-is-a-defect.
- **C2b (gating) — B-REG-1's referent EXISTS: stage-4 registers the design (per red-team MAJOR).**
  stage-4.md gains an obligation (`A-4-2`) to **record the design's parameters / stop-rule /
  conditions as the pre-registered design** in **`decisions.md`** (the design-time record, per
  B-TRI-3 — NOT the observation ledger, which is for things examined; corrected after round-2 to
  match the as-built destination). Without this, B-REG-1 checks against a non-existent artifact and
  is vacuously satisfiable. *Verify:* grep `A-4-2` in stage-4.md;
  rubric confirms it registers parameters + stop-rule as the object B-REG-1 compares against.
- **C3 (gating) — G3 present as `B-TARGET-1` @ stage-2.** A rule requiring every investigative step
  to **name the frozen-target node it attributes a share to** (re-reading the frozen ledgers, not
  reconstructing from memory), treating a step that attributes to a **symptom/mitigation property**
  (or names no target) as **drift → incidental-ledger, not pulled**, with a **per-step marker** on
  the observation-ledger row. *Verify:* grep `B-TARGET-1` in stage-2.md; rubric confirms
  attribute-a-share + re-read-not-reconstruct + drift⇒incidental + the marker.
- **C4 (gating) — B-TARGET-1 composes with, not contradicts, the incidental-findings rule, and their
  tie-breaks agree.** The built text (a) explicitly distinguishes "is a newly-noticed *finding*
  related?" (existing rule, in doubt → in-scope) from "does the *step* advance the frozen target?"
  (B-TARGET-1); AND (b) B-TARGET-1's own **in-doubt tie-break resolves toward inclusion** ("in doubt
  … take it and mark the row uncertain"), matching the incidental rule's direction (per red-team —
  so "symptom-property → park" cannot be read as licence to drop a real cause-tracing step). *Verify:*
  rubric — both the composition sentence and the inclusion tie-break are present; neither weakens the
  existing "in doubt → in-scope" rule.
- **C5 (gating) — convergence tie-in @ stage-6.** stage-6 states an **untrusted-negative (B-REP-4)**
  or an **unresolved design deviation (B-REG-1)** does NOT count as a hypothesis elimination for the
  cap. *Verify:* grep `B-REP-4` and `B-REG-1` in stage-6.md; rubric confirms the no-false-convergence
  content.

## Rep-gate-scope consistency criteria
- **C6 (gating) — rep-gate scope updated to "1, 4, and 5" at both sites.** SKILL L53 and METHODOLOGY
  L64 now read "governs stages 1, 4, and 5"; no "governs stages 1 and 4" survives. *Verify:*
  positive — grep `governs stages 1, 4, and 5` returns 1 hit in each of SKILL.md & METHODOLOGY.md;
  negative — `grep -rn 'governs stages 1 and 4' SKILL.md METHODOLOGY.md stages/` → 0 hits.
- **C6b (gating) — SKILL incidental-findings paragraph carries a B-TARGET-1 pointer.** *Verify:*
  grep `B-TARGET-1` in SKILL.md returns ≥1 hit inside/after the "Incidental findings" paragraph.

## Structural / self-check criteria
- **C7 (gating) — new rule IDs collision-free.** `B-REP-4`, `B-REG-1`, `B-TARGET-1` absent
  pre-change. *Verify:* grep each in the pre-change tree → absent.
- **C8 (gating) — position/behavior preservation.** Additive change (new lettered rules appended to
  existing "cross-cutting rules" sections; a pointer sentence; two in-place scope-word swaps). All
  existing rules (B-REP-1/2/3, B-TRI-*, B-TBG-1, A-6-*, the incidental-findings rule, etc.) read and
  apply exactly as before. *Verify:* `git diff` shows only additions + the 2 scope-word edits (no
  edit inside an existing rule body beyond those 2 sites) + a stage-6 cold reviewer enumeration.
- **C9 (gating) — live == source.** `diff -r ~/.claude/skills/dragonfly <source>` (excl. README,
  *.companion.md, changes) → empty.
- **C10 (gating) — cross-doc consistency.** No rule stated in >1 place diverges; the only
  multiply-stated fact touched is the rep-gate scope (C6).

No advisory criteria. No numeric baseline → stage 8 conformance-only.
