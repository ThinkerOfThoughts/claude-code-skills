# 1.5 — Criteria (accept bar). All gating unless marked advisory.

## Rule-presence criteria
- **C1 (gating) — R-A present as `B-VER-1`.** stage-2.md gains a cross-cutting rule that requires
  every factual claim (code/data/timeline) to be **verified against source before entering a ledger
  or conclusion**, explicitly incl. (i) the agent's own just-made claims and (ii) a definition/fact
  in a **memory note**. *Verify:* grep `B-VER-1` in stage-2.md; rubric (operator) confirms (i)+(ii)
  both stated.
- **C2 (gating) — R-B present as `B-TIME-1`, anchored + enforced.** stage-3.md states: establish the
  symptom's first-appearance version AND each candidate factor's introduction version, and **discard
  as root any factor that post-dates the symptom** (post-dating ⇒ amplifier only). stage-7.md
  enforces it in the depth check ("root, or a post-dating amplifier?"). *Verify:* grep `B-TIME-1` in
  both files; rubric confirms the discard-as-root rule + the stage-7 enforcement.
- **C3 (gating) — R-C fidelity: 5th lens + triage check.** (a) charter.md numbered lens list has a
  5th, named **Fidelity** lens asking whether the artifact implements the owner-specified mechanism
  or a proxy (pin loaded terms; proxy ⇒ untrusted until owner confirms). (b) A `B-FID-1` fidelity
  check is added to the diagnostic-artifact triage (stage-1.md, referenced at stage-7.md). *Verify:*
  grep `^5. **Fidelity` in charter.md; grep `B-FID-1` in stage-1.md & stage-7.md; rubric confirms
  the mechanism-vs-proxy content.
- **C3b (gating) — the earned-clean fidelity guard + aim + spot-check-pins are present (per red-team
  MAJOR + M1).** The lens is only non-vacuous with its guards. charter.md must contain: (i) the
  earned-clean fidelity discipline bullet (grep `clean \*fidelity\* lens must be earned`); (ii) the
  fidelity aim in "Diagnosis-specific aiming" (grep `fidelity challenge`); (iii) the spot-check-pins
  clause appended to the "Spot-verify" bullet (grep `spot-check that the named`). All three found → pass.
- **C4 (gating) — R-C cost-surprise present as `B-COST-1`.** stage-5.md states a surprising resource
  signal (cost/tokens/latency/call-count) is a **fidelity tell** → investigate whether the
  implementation matches intended design, not merely report the number. *Verify:* grep `B-COST-1`;
  rubric confirms.

## Count-surface criteria (robust oracle — Run-0 lesson: markdown/wrap defeat naive grep)
- **C5 (gating) — all 6 count sites positively read "five"; no stale "four…lens" survives.**
  - **(a) POSITIVE** — each of the 6 post-strings present (whitespace/markdown-tolerant): charter
    `the five lenses, the discipline` (L3) and `five **separate** lenses` (L21); SKILL `five lenses,
    evidence discipline` (L83); METHODOLOGY `five lenses + discipline` (L88); stage-1 `five lenses +
    discipline + provenance` (L7) and `five lenses + evidence discipline` (L55). All 6 found.
  - **(b) NEGATIVE robust sweep** — `for f in SKILL.md METHODOLOGY.md stages/*.md; do tr '\n' ' '
    <"$f" | sed 's/\*\*//g' | grep -oiE 'four[a-z ]{0,18}lens' && echo STALE:$f; done` → 0 STALE.
  - **Self-test (stage-8):** the same sweep on `git show HEAD:<file>` pre-images must FIRE on all 4
    stale files (charter, SKILL, METHODOLOGY, stage-1) — proving the oracle is not a no-op.

## Structural / consistency criteria
- **C6 (gating) — new rule IDs don't collide.** `B-VER-1`, `B-TIME-1`, `B-FID-1`, `B-COST-1` do not
  already exist pre-change. *Verify:* grep each in the pre-change tree → absent.
- **C7 (gating) — B-EVID-1 preserved, not replaced.** R-A strengthens but does not delete/reword
  the existing `B-EVID-1` occurrences. *Verify:* `git diff` shows no deletion of B-EVID-1 text.
- **C8 (gating) — position/behavior preservation (position lens).** These docs are prompts. The
  change is **additive** (new lens at list-end; new lettered rules appended into existing
  "cross-cutting rules" sections; in-place count-word swaps). Behavior to preserve: lenses 1–4 and
  every existing rule (B-REP-*, B-TRI-*, B-GBP-*, B-EVID-1, etc.) still read and apply exactly as
  before; no numbered cross-reference is misaligned by appending lens 5. *Verify:* `git diff` shows
  no edit inside lenses 1–4 or existing rule bodies (only additions + count words) + a stage-6 cold
  reviewer confirms it can still enumerate lenses 1–4 and the pre-existing rules unchanged AND the 4
  new rules/lens now present.

## Standing self-check criteria (mandatory — artifact is this skill)
- **C9 (gating) — live == source.** `diff -r ~/.claude/skills/dragonfly <source>` (excl. README,
  *.companion.md, changes) → empty.
- **C10 (gating) — cross-doc rule consistency.** Every rule/count stated in >1 place stays
  consistent; the only multiply-stated fact touched is the lens count (covered by C5). *Verify:*
  `git diff` review confirms no other shared rule text diverges.

No advisory criteria. No numeric baseline → stage 8 is conformance-only.
