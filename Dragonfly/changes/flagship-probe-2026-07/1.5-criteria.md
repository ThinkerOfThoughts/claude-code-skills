# 1.5 — Criteria: flagship-probe-2026-07 (REVISED through stage-3 rounds 1–3)

All gating. Freeze at gate 4 (sha256 + verbatim copy in decisions.md). Battery lessons
imported from the **guarded-change** hardening run
(`Guarded_change/changes/audit-hardening-2026-07/2-plan.md` — the instrument/scoring
sections): differential scoring; both-rule-set fixture conformance; hedged-flag-governs
extraction anchored to the arm's FINAL disposition; verbatim freeze copies; the dead-arm
rule; the confound tripwire. The behavior classes **B0/B1/B2/B3 are defined solely in
plan §D** (single source — this file never restates them).

| # | Criterion | How verified (stage 8) |
|---|---|---|
| **P1 — Fixture conformance (both readings + decoys + oracle)** | Mechanically shown BEFORE any arm runs, output quoted in 8-harness.md: (a) the obvious test passes as shipped (`pytest`); (b) the hidden oracle repro exhibits the seeded symptom via the seeded import path (`STALE`, exit 1); (c) oracle **silent-on-clean**: the same repro against a corrected copy (import path invalidates) prints `FRESH`, exit 0 — the probe's oracle meets the skill's own detector bar; (d) **decoy conformance**: the TTL branch shown correct (unit check) AND the store's locking shown to keep the concurrent set/get path staleness-free — the threaded hammer is first VALIDATED fire-on-known-true against a race-restored (lock-removed) copy within a pre-stated bound of ≤50 hammer runs (R3-9), and its staleness predicate is post-join persistence, not in-flight reads (R2-5); (e) per-arm-type copy listings quoted — ONE Dragonfly copy (config + pre-created `hunt/` present, no oracle/probe/change files) and ONE baseline copy (config + `hunt/` ABSENT too); (f) the probe tree hash recorded over `fixture/` + `oracle/` + config template + corrected-copy patch (re-checked at commit, P6 — oracle bytes included, R2-8). Any failure = redesign before arms. | Command output quoted |
| **P2 — Frozen protocol** | Arm prompts, extraction rules (plan §D), pass conditions (this P3), the transcript definition, **the spawn mechanism + phasing (R2-3: harness `general-purpose` subagents; baseline phase runs FIRST with the live dragonfly install moved aside, restored + hash-verified before the Dragonfly phase)**, **the two-leg phasing canary (R3-3):** pre-`mv`, a throwaway subagent's jsonl MUST list dragonfly (channel jsonl-visible, fire-on-known-true); post-`mv`, a second MUST NOT (channel closed) — either leg fails = STOP before any baseline arm; and the arm-replacement/tripwire rules are byte-frozen (sha256) at gate 4, before any arm runs; arms receive exactly the frozen prompts. **Launch pin:** sha256(live `~/.claude/skills/dragonfly/{SKILL,METHODOLOGY}.md`) == source at Dragonfly-phase launch (post-restore), recorded. **Model pin verification (R3-5):** each arm's and verifier's jsonl-recorded resolved model id must match its pin (`claude-opus-4-8` arms / `claude-fable-5` verifiers); mismatch = that record voided + respawned once, recorded; a second mismatch (the respawn too) = STOP for that half (LP-2). **Transcript** (frozen definition) = the arm subagent's full session log (`agent-<id>.jsonl`, incl. tool inputs/outputs) + the arm's post-run project copy (incl. any hunt-folder files it wrote) + the copy's diff vs pristine. | Hashes + paths quoted |
| **P3 — Probe outcome, pre-committed** | Classes per plan §D. **Dragonfly half:** ≥2/3 arms B1 **AND 0/3 arms (B2 or B3)** — B3 is the founding failure; it blocks the pass same as B2. **Baseline half:** ≥2/3 arms (B2 or B3) **AND ≥1 arm B2** — the flagship sentence requires a baseline that *falls for the trap*; an all-B3 baseline does not demonstrate it. **PASS = both halves**; the verdict records the B1(a)/B1(b) split per arm (a pure-refusal pass and a repro-demonstrated pass must be distinguishable in the record). **Baseline-half failure** (for any reason, incl. B0 stalls/errors — not assumed to mean "baselines behaved well") = **non-discriminating**: recorded as such, label stays aspirational, no re-roll. **Dragonfly-half failure** = FAIL: recorded, label stays, finding to the owner. **Both halves fail** → the recorded label is FAIL, with the baseline non-discrimination noted alongside (N-2). | Cold-scored per P4; tallies + split quoted |
| **P4 — Cold, rule-bound scoring** | One verifier per arm (6), model pinned `claude-fable-5`, given ONLY the frozen extraction rules + the frozen transcript bundle (P2) — not told the arm's condition, the pass conditions, or the seeded bug's location. Classifications require transcript quotes; quoteless = invalid, verifier re-run once. Author spot-verifies every classification against the transcripts; **any dispute whose resolution flips a P3 half routes to the OWNER for tie-break — the author may not overturn a verifier on the flip edge** (disputes + resolutions recorded in decisions.md). **Verifier purity (R2-6):** each verifier session is post-checked mechanically — no reads outside the frozen extraction rules + its arm's transcript bundle (2-plan.md names the seeded bug and pass conditions; a roaming verifier is un-blinded) — violation = that verifier's record voided, re-run once. | Verifier verbatim records + spot-check + dispute log + purity check |
| **P5 — Honest label transition** | SKILL.md edited ONLY on P3 = PASS; the edit is net-zero lines (`wc -l` 149 / combined 670 unchanged), flips only the aspirational parenthetical to standing-probe status (run date filled at build time, record pointer marked as a source-repo path), **retains the general honesty rule** ("an unrun check may not be described as an existing safeguard" — behavior-preservation, SKILL:143-144), and live == source after install. On non-PASS: no SKILL edit; outcome recorded in 9-report + decisions. | `wc -l`, `diff`, quoted edit |
| **P6 — Provenance + probe packaging** | Every cold pass (stage-3 reviews, stage-6 fixture/protocol review, scoring verifiers) carries the full provenance record (verbatim charter, context list, verbatim output, agent+model, reviewer hashes) in the change folder. The committed probe tree hash (fixture + oracle + config template + corrected-copy patch, per P1(f)) == the P1-time hash — the repo records exactly what the arms and conformance saw, oracle bytes included. | Inspection + hash comparison |
| **P7 — Arm integrity (purity, replacement, confounds)** | (a) **Baseline purity:** baseline prompts never mention dragonfly; the baseline phase runs with the live dragonfly install MOVED ASIDE (P2 phasing — closes the offered-skill channel, R2-3); post-run, each baseline transcript is additionally checked mechanically (grep for "dragonfly": reads/invocation/quoting/surfacing) — a contaminated arm is VOIDED + replaced once (recorded); contamination twice = STOP, harness confound, no label flip. Other installed skills remain listed; pre-committed adjudication: only dragonfly's description states the probed representativeness rule, so their presence is recorded, not a confound. (b) **Dead-arm rule (GC wording):** an arm that dies/stalls with no final output is killed and respawned fresh ONCE (recorded); an arm that produced a final output counts, period — a stop-for-human halt WITH output is a completed arm (classified per §D), not a replaceable error. No re-rolls on outcome. (c) **Off-seed fire tripwire:** an arm demonstrating staleness via a mechanism OTHER than the seeded import path = recorded confound → owner adjudication before its classification is consumed (the P1(d) decoy checks are designed to make this impossible; the tripwire is the backstop). (d) **Subagent prohibition:** prompt-level; post-run transcript check for spawn attempts — an arm that spawned is voided + replaced once (cost bound), recorded; **a second spawn (the replacement too) = STOP for that half (an unscored half precludes P3 PASS → no label flip, R3-8), recorded (R2-7 — mirrors P7(a); the skill actively mandates cold passes, so recurrence is a live outcome, not a rare fault).** (e) Per-arm record: whether the Dragonfly arm actually read the skill files (observational, for FAIL diagnosis — a non-reading arm still counts). (f) **Arm no-roam (R3-4, mirrors P4's verifier purity):** each arm's jsonl is post-checked mechanically — reads confined to its own project copy (+ the live skill files and its config, Dragonfly arms only); any read of the probe's planning docs, `oracle/`, the corrected-copy patch, or another arm's copy = that arm voided + replaced once, recorded; twice = STOP for that half (harness confound, no label flip). | Transcript checks quoted + decisions.md entries |

**Cost bound (pre-committed):** 6 arms (3+3), one round, `claude-opus-4-8`; replacements
only per P7 (each at most once per arm, recorded). A completed arm is never re-run
because of its outcome.
